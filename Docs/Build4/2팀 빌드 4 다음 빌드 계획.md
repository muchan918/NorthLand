# 2팀 빌드 4 다음 빌드 계획

- 팀명: 2팀 (유유아)
- 기준 빌드: Build 4
- 다음 빌드 목표 일자: 2026-08-14

## 현재 완료된 것

- **전투 공간** — 절차적 맵 생성(마스터 시드 연결), **타워 14종 획득 가능**(직접배치 7 / 합성 전용 7. 데이터상 SO는 15개지만 `lightning_tower`가 미배선이라 획득 불가), **타워 구조 부품 조립화**(액션 리스트 · HitEffect · 비행 축 SO 이관 · 합성 효과 계승), 타워 합성(**커맨드 패턴** — 클릭 즉시 소모 · 취소 시 원복), **배치 · 합성 되돌리기(Undo)**, **드래그 사각형 그룹 선택**, 버프 타일
- **경영 공간** — 영토 확보 시 고유 자원 자동 수급, 생산 건물 3종 업그레이드, 본진 패널(주민 증가 + 건물 레벨 상한 해제), 마법 연구소(스킬 기본 수치 + **레벨별 착탄 이펙트** + **사거리 상승폭 대폭 상향** — Lv5 유효 반경 3 → 13.5), 연금술사의 집(마나석 ↔ 자원 교환), **주민 군중**(NavMesh 이동 · BT 행동 15종 · 3인 대화 · 밤낮 출입 · 호버/선택 아웃라인)
- **런 관리** — **마스터 시드**(시스템별 파생 · 요청/최종 시드 분리 · 타이틀 직접 입력), **저장 및 이어하기**(버전 봉투 + 마이그레이션 · 낮 시작 자동 저장 · 진행/경영/전투/성장 전체 복원 · 런 종료 시 삭제)
- **몬스터** — Behaviour Tree 기반 AI, 중간보스, 최종 보스(본진 돌진 · 방어 태세 · 마력 봉인 · 지속 소환), 공중 유닛
- **스킬** — 감전 · 버프 2종, 보상 특수효과 4종 + **레벨 상한(3)**, **보상 카드에 레벨 · 수치 변화 표시**, 원기둥 아우라 인디케이터
- **연출 / 비주얼** — 타워 배치 등장 · 합성 소모(수렴/역재생) · 전투맵 타일 등장 연출(**전부 아트 방향 확정 전 임시 버전**), **툰 셰이딩 파이프라인**(경영 공간 머티리얼 115개 변환 · 픽셀 필터 적용 — **전투 공간과 UI는 아직 이전 톤**)
- **편의성 / UI** — x1·x2·x4 배속, ESC 설정 패널(언어 · 타이틀로 · 게임 종료), 아웃라인 규칙 통일, 사거리 표시 + 타워 툴팁, 낮 종료 확인 팝업, UI 레이어 표준화, **합성 족보 패널(테스트용)**, **UI 에셋 적용 — 상시 노출 화면 한정**(상단 자원 · 일차/웨이브 · 본진 HP, 하단 타워 바 · 스킬 슬롯, 배속 · 되돌리기 · "다음" 버튼)
- **버그 수정** — 웨이브 진행 순서를 리스트 등록 순서 기반으로, 타일 높낮이별 스킬 인디케이터, 설정 · 다음 버튼 클릭 시 마우스 상호작용 취소, 산탄 터널링 · 부메랑 중복 타격

**요약**: 한 판이 재현되고 이어지며, 경영 공간에 주민이 살고 화면에 룩이 생겼다. 전투 공간은 타워 14종 · 되돌리기 · 드래그 선택까지 들어가 조작 밀도가 올라갔다. **남은 것은 절반만 적용된 축의 마무리(UI 킷 · 툰 셰이딩)와, 구조가 애매한 축(자원 · 영토)의 개편이다** — 빌드 3에서 계획했다가 이월된 항목들이 그대로 다음 범위다. 룩과 UI가 **절반만 적용된 상태**라는 점이 이번 빌드의 특징이다 — 툰 셰이딩은 경영 공간만, UI 에셋은 상시 노출 화면만 끝났다. 여기에 **콘텐츠 확장(타워 · 스킬 특수효과)과 주민 ↔ 건물 상호작용**이 새 축으로 붙는다. 튜토리얼은 UI · 이름 · 밸런스가 굳은 뒤에 만들어야 다시 만들지 않으므로 **베타로 미룬다.**

## 남은 것

- 전투 / 밸런스
  - **번개사슬 타워(`lightning_tower`) 획득 불가** — SO만 있고 레시피 · 프리팹 미배선
  - **직접배치 로스터 축소(11 → 7종) 팀 사인오프 미완료** — 개틀링 · 스나이퍼 · 화염궁수 · 소이캐논이 패널에서 빠진 상태 (WL-156)
  - 타워 14종의 스탯 · 역할 전수 카탈로그 미작성(획득 경로 표만 있음). 합성 족보 · 합성 추가 비용은 GDD에서 TBD
  - 버프 타일의 위치 · 수치가 매 판 달라지지 않음 — 시드 연결 대상에서 빠져 있음
  - **캐논 타워가 공중 유닛에게 데미지를 넣지 못한다** — 타겟팅은 되나 피해가 적용되지 않는다. 빌드 3부터 미해결
  - **마법 연구소 사거리 배율을 크게 올렸다(Lv5 유효 반경 3 → 13.5)** — 타일 5유닛 기준 지름 약 5.4타일. 밸런스 검증 없이 연출 가시성 위주로 정한 값이라 재조정 여지가 있다
  - 보스 패턴 전반 수정 — 방어 태세 · 마력 봉인은 빌드 3부터 ⚠️ 로 남아 있음
  - 소다 타워의 기절 소스키가 대상당 1슬롯 공유라 2기째 기여가 0 (WL-141)
  - 타워 철거 없음 — 되돌리기가 유일한 회수 수단. 철거 도입 시 되돌리기 불변식 선행 처리 필요 (WL-148)
- 경영
  - 주민 밀도 의존 수치 5종(춤 반경 · 이웃 수 · 조우 반경 · 대화 거리 · 문 개수)이 테스트 씬 기준 — **정본 씬에서 춤이 한 번도 안 나올 수 있다** (WL-153)
  - 주민 드래그 배치(들려서 따라오기 · 드롭 배치) 미구현 — 현재 선택까지만
  - 주민 선택 상한이 "표시만 막는" 구현이라 주민 정보 패널이 붙으면 어긋난다 (WL-158)
  - 밤 일괄 수거 시 대화 세션이 해산 쿨다운을 못 밟음 (WL-159)
- 연출 / 비주얼
  - **툰 셰이딩이 경영 공간에만 적용됐다** — 전투 공간 머티리얼은 아직 이전 톤이라 두 공간의 그림이 섞인다(경영 공간만 115개 변환한 규모를 감안할 것)
  - **타워 배치 · 합성 연출이 아트 방향 확정 전 임시 버전** — 확정 대상인지 이대로 갈지 결정 필요
  - 물에 실제로 닿아 있지 않은데 화면상 닿아 보이면 거품이 생김 (빌드 3에서 이월)
- UI
  - **패널 UI가 아직 기본 Unity UI** — 상시 노출 화면은 빌드 4에서 적용 완료. 클릭해서 여는 패널 전부 · 아이콘 · 폰트가 남음
  - 버프 스킬 사용 시 타워 정보 패널이 갱신되지 않음 (빌드 3 이월)
  - 생산 라인의 언어 변경이 적용되지 않음 (빌드 3 이월)
  - 연금술사의 집에서 특수 자원 교환 시 해당 열이 활성화되지 않음 (빌드 3 이월)
  - 게임 다시하기 후 마우스 매니저 구독 버튼이 동작하지 않음 (빌드 3 이월)
  - 최대 축소 시 주민이 점으로 보임 — 오쏘 카메라라 거리 보정이 무효
- 콘텐츠 / 네이밍
  - 자원 이름을 에셋 톤에 맞게 변경(나무 · 철 → 사탕 · 초콜릿 등) — 아래 §3 자원 개편과 함께 처리
  - 오디오(BGM · SFX) 전무
- 기술 부채
  - 스킬 시전면 y가 지면 · 몬스터 부양 높이와 별도 상수로 복제돼 있음 (WL-068 · WL-149)
  - 맵 공개 분모(`totalRounds` = 30)와 실제 웨이브 수(7)가 갈려 최종 웨이브에서도 경로의 30%만 공개됨 (WL-150)
  - 스킬 AoE 적중 버퍼가 `Collider[16]` 고정 — 초과분을 조용히 버린다 (WL-157)
  - 주민 BT 튜닝 수치가 에디터 어셈블리 상수에 있어 그래프 손 편집이 재빌드에 소실 (WL-151)
  - 드래그 선택 소비처가 두 벌로 인라인 복제됨 — 세 번째 소비처 전에 어댑터 추출 (WL-145)
  - 순수 자원 지급 창구(`ManagementController.Grant`)가 가드 없이 열려 있음 (WL-147)

## 다음 빌드 때 할 것

이번 빌드의 방향은 **"콘텐츠를 늘리고, 절반만 끝난 것을 마무리한다"** 이다. 시스템은 대체로 들어갔고 한 판이 재현 · 영속되지만, **룩과 UI가 절반만 적용돼 화면 안에서 톤이 섞이고**(툰 셰이딩은 경영 공간만, UI 에셋은 상시 노출 화면만) 반복 플레이를 지탱할 선택지가 얇다. 이번에는 (1) 절반만 끝난 룩 · UI를 마무리하고, (2) 빌드 4에서 이월된 구조 개편(자원 · 영토)을 처리하고, (3) **타워 · 스킬 특수효과 종류를 늘려 판마다 다른 선택이 나오게 하고**, (4) **경영 공간의 주민 ↔ 건물 상호작용**을 붙이고, (5) 랜덤화와 밸런스의 남은 구멍을 메운다.

> **튜토리얼은 이번 범위에서 제외한다 — 베타에서 진행한다.** 안내 층은 UI · 자원 이름 · 밸런스가 확정된 뒤에 만들어야 다시 만들지 않는다. (참고: 빌드 4에서 마스터 시드 직접 입력이 들어가 **튜토리얼 전용 고정 시드**를 쓸 수 있게 됐으므로, 착수 시 별도 튜토리얼 맵은 필요 없다)
> **설정 확장**은 GDD에 명세가 없다 — 구현과 함께 GDD에 해당 절을 신설할 것.
> §1 UI 에셋 · §2 설정 확장 · §3 자원 개편 · §4 영토 개편은 빌드 3에서 계획했다가 **두 번째로 이월**된 항목이다. 이번에도 밀리면 범위를 줄여서라도 착수 여부를 먼저 정할 것.

### 1. UI 에셋 적용

빌드 4에서 **상시 노출 화면까지 적용이 끝났다.** 남은 것은 **클릭해서 여는 패널 전부**와 아이콘 · 폰트다. 지금은 한 화면 안에서 새 UI(상시)와 기본 UI(패널)가 섞여 보이므로, 이번 빌드에서 마무리해야 톤이 정리된다. 에셋과 톤 기준은 빌드 4에서 확정됐다.

- **에셋** — 빌드 4에서 쓴 UI 킷을 그대로 확장 적용. 부족한 컴포넌트만 추가 조달
- **남은 적용 대상**
  - 공통: 패널 배경 · 버튼 · 슬라이더 · 토글 · 스크롤바
  - 경영: 건물 정보 · 업그레이드 패널, 본진 패널, 마법 연구소, 연금술사 상점
  - 전투: 타워 정보 패널, 합성 패널, **합성 족보 패널**
  - 기타: 타이틀(**이어하기 · 시드 입력 포함**), 설정, 보상 선택, 게임 오버 / 클리어, 미니맵 프레임
  - ~~HUD: 자원 바, 일차 · 웨이브 표시, 배속 버튼, 본진 HP, Undo 버튼, 타워 패널, 스킬 슬롯~~ — **빌드 4 완료**
- **아이콘** — 자원, **타워 14종**, 건물, 스킬, 상태이상(스턴 · 슬로우 · 화상 · 독) (자원 아이콘은 §3 확정 후, 신규 타워 아이콘은 §6과 세트)
- **폰트** — 게임 톤에 맞게 교체. **ko / en / ja 글리프를 모두 포함**하는지 확인
- **레이아웃 정리** — 기준 해상도 · Canvas Scaler Match 값 통일, 렌더 순서 재점검

### 2. 설정 확장

현재 설정 패널에는 **언어 변경 · 타이틀로 · 게임 종료**만 있다.

- **오디오** — 마스터 / BGM / SFX 볼륨 (오디오 도입과 세트, §7)
- **게임플레이 편의** — 카메라 이동 · 줌 감도, 데미지 표시 · 사거리 상시 표시 토글(여유 시)
- **수동 저장 / 불러오기** — 빌드 4는 낮 시작 자동 저장뿐이다. 설정 패널에서 수동 저장을 열지 결정
- **설정 값 영속화** — 현재 언어만 `PlayerPrefs`에 저장된다. 나머지도 같은 방식으로 저장하고 시작 시 일괄 적용
- 타이틀 씬과 인게임 양쪽에서 동일하게 동작해야 한다

### 3. 자원 개편

`ResourceKind` 8종(나무 · 철 · 식량 · 마나 + 금 · 루비 · 사파이어 · 다이아)인데 **어떤 자원이 어디에 쓰이는지가 불분명하다.** 특수 자원 4종은 수급 경로(영토)만 있고 소비처가 뚜렷하지 않다. 빌드 3에서 이월된 항목이며, **빌드 4에서 타워가 14종으로 늘고 합성 전용이 7종이 되면서 소비처 설계 여지가 커졌다.**

- **소비처 매트릭스 확정** — 자원 × 사용처(타워 배치 · 타워 합성 · 건물 건설 · 건물 업그레이드 · 본진 업그레이드 · 스킬 업그레이드 · 연금술사 교환) 표를 만들어 GDD에 싣는다. **소비처가 없는 자원은 만들지 않는다.**
- **역할 분리** — 기본 자원(상시 수급, 저렴한 반복 소비) / 마나석(웨이브 보상, 성장 통화) / 특수 자원(영토 보상, 고급 해금 전용)이 겹치지 않게
- **특수 자원 4종의 용도 부여** — **합성 전용 상위 타워의 추가 비용**이 유력한 자리다(현재 합성 전용 8종은 전부 `Cost:[]`)
- **이름 · 아이콘 개편** — 에셋 톤에 맞게. `ResourceKind` enum 값 순서는 직렬화 인덱스 때문에 **변경 금지** — 표시 이름은 `NameKey`로만 바꾸고 3개 언어 갱신
- **세이브 스키마 영향 확인** — 자원은 절대값으로 저장된다. 종류를 줄이면 구 세이브 마이그레이션이 필요하다

**결정 필요**
- 자원 종류를 8종 그대로 갈 것인가, 줄일 것인가
- 특수 자원 4종을 각각 다르게 쓸 것인가, 하나의 "고급 자원"으로 묶을 것인가
- 새 이름 목록(ko / en / ja)

### 4. 영토 개편

확보하면 고유 자원이 매일 자동 수급되는 것 외에 할 수 있는 일이 없다. 빌드 3에서 이월.

- 손볼 후보
  - 영토마다 성격 부여 — 자원 종류 · 양뿐 아니라 특수 효과(생산 보너스 · 방어 · 해금 등)
  - 확보 이후의 상호작용 — 영토 자체를 개발/업그레이드할 수 있는지
  - 확보 비용과 조건 — 현재는 인접이면 확보 가능. 자원 소모 · 일차 제한 · 웨이브 클리어 요구 등
  - 전투 공간과의 연결 — 확보한 영토가 전투에 미치는 영향(현재는 없음)
  - 그래프 생성 규칙 — 노드 수 · 분기 · 거리(빌드 4에서 시드 연결은 끝났으므로 생성 규칙만 남았다)
  - 시각적 표현 — 확보/미확보/확보 가능 상태 구분
- **세이브 영향** — 영토는 노드 ID로 저장된다. 생성 규칙을 바꾸면 같은 시드에서도 ID 대응이 달라질 수 있으니 마이그레이션 여부 확인

### 5. 랜덤화 잔여 · 밸런스

빌드 4에서 **전투맵 · 영토는 마스터 시드에 연결됐다.** 남은 대상을 마무리하고 난이도 하한/상한을 정한다.

- **남은 랜덤화 대상**
  - **버프 타일 위치 · 수치** — 파생 시드 연결 안 됨. 빌드 3부터 ❌ 로 남아 있는 항목
  - 웨이브 구성(몬스터 조합 · 스폰 순서), 웨이브 클리어 보상 후보
- **밸런스**
  - 타워 14종 스탯 전수 카탈로그 작성 + 역할 중복 정리. **감전 스킬의 레벨별 사거리 · 데미지도 같은 테이블에 올린다** — 빌드 4에서 연구소 사거리 배율을 최대 2.0 → 4.5로 올렸고(Lv5 반경 13.5), 타워 사거리와 함께 봐야 고레벨 구간의 체감을 판단할 수 있다
  - 합성 족보 확정(GDD §5.8 TBD) — 합성 추가 비용과 §3 특수 자원 연결. §6에서 타워를 늘리면 족보도 함께 확장된다
  - 웨이브 수 확정 및 맵 공개 분모 통일 (WL-150)
  - `lightning_tower` 획득 경로 배선 또는 폐기 결정
  - 직접배치 로스터 축소 사인오프 (WL-156)

**결정 필요**
- 웨이브 구성 · 보상까지 랜덤화할 것인가, 맵 · 타일만 둘 것인가
- 난이도 하한/상한을 어떻게 보장할 것인가(생성 후 검증 기준)

### 6. 콘텐츠 확장 — 타워 · 스킬 특수효과

한 판의 구조는 완성됐지만 **판마다 다른 선택이 나오게 하는 재료가 얇다.** 보상은 4종뿐이라 서너 웨이브면 다 보이고, 타워도 역할이 겹치는 축이 있다. 종류를 늘려 빌드가 갈리게 만든다.

**보상 스킬 특수효과 확장** (현재 4종 — 화상 · 폭탄 · 추가시전 · 버프화상)

- 3택1 후보 풀이 4종이면 **레벨 상한(3)까지 올리는 데 12회 선택이면 전부 만렙**이라 후반 보상이 비어 버린다. GDD §5.6의 "상한 3"과 함께 **적정 종류 수를 역산**할 것 — 웨이브 수(§5 확정)와 세트로 판단한다
- 추가 후보 방향 — 감전 축(연쇄 · 감전 지속 · 처치 시 폭발), 시전 축(쿨다운 감소 · 시전 범위 확대), 버프 축(지속시간 · 타워 사거리 버프)
- **추가 비용은 구조상 매우 낮다** — `SkillEffect` 파생 1개 + `GameScene`의 `SkillEffectManager`에 컴포넌트 부착 + 보상 SO 1개. 스킬 본체 · 매니저는 무수정이다(`PlayerSkill.md` §4 절차)
- 주의 3가지
  - `WaveRewardType`에 값을 추가할 때는 **반드시 맨 뒤에** — enum이 int로 직렬화돼 순서를 바꾸면 기존 보상 에셋이 다른 타입이 된다
  - `GetStatSummary()` 구현 필수(추상) + 라벨은 `SkillStatsFormatter` · `NorthLand_Skills` 테이블에 — 수치 없는 카드가 조용히 나가는 것을 막는 강제 장치다
  - **세이브 마이그레이션** — 보상 레벨은 타입별 절대값으로 저장된다. 종류를 늘리는 것은 안전하지만 지우거나 순서를 바꾸면 구 세이브가 깨진다
- **메테오처럼 "이동 + 지연 데미지"가 붙는 연출은 이 축에 넣는다** — 마법 연구소 축(즉발 이펙트 교체)이 아니다. `BombEffect`+`SkillBomb`이 이미 그 패턴을 확립해 뒀다

**타워 종류 확장** (현재 14종 — 직접배치 7 / 합성 전용 7)

- 빌드 4의 **부품 조립화**(액션 리스트 · `HitEffect` · 비행 축 SO 이관 · 합성 효과 계승) 덕분에 **기존 부품 조합만으로 신규 타워를 만들 수 있다** — 새 타워마다 새 클래스를 쓰던 구조가 아니다. 이번 확장은 그 구조의 첫 수확이다
- 방향 — 지금 없는 역할부터 채운다(광역 디버프, 대공 전용, 고티어 단일딜 등). §5의 역할 중복 정리와 **같이 진행할 것**, 아니면 겹치는 타워만 늘어난다
- **직접배치 / 합성 전용 어느 쪽을 늘릴지 먼저 정한다** — 직접배치는 초반 선택지, 합성 전용은 후반 목표다. 현재 로스터 축소(11 → 7종)가 사인오프 전이라(WL-156) 그 결론이 선행 조건이다
- `lightning_tower` 배선 또는 폐기 결정을 이 작업과 묶는다(§5) — 지금은 SO만 있고 획득 불가다
- 신규 타워마다 **아이콘 · 이름 · 설명(ko/en/ja)** 이 따라온다(§1 아이콘 작업과 세트)

**결정 필요**
- 특수효과 · 타워를 각각 몇 종까지 늘릴 것인가 — 웨이브 수(§5)와 보상 상한(3)에서 역산
- 신규 타워를 직접배치로 열 것인가, 합성 전용으로 둘 것인가
- 특수효과 추가가 3택1 후보 풀 희석으로 이어지지 않는지(원하는 빌드로 못 가는 문제)

### 7. 오디오

전무하다. 룩이 확정되고 UI 킷이 들어가는 시점이 맞춰 넣기 좋다.

- BGM — 낮 / 밤 2트랙 최소, 페이즈 전환 시 크로스페이드
- SFX — 타워 배치 · 합성 · 되돌리기, 타워 발사 · 명중, 몬스터 사망, 스킬 시전, 버튼 · 패널 열림, 웨이브 클리어 / 게임 오버
- 볼륨 설정(§2) · `AudioMixer` 그룹 구성, 배속 상태의 피치 처리 방침
- 에셋 조달 및 라이선스 확인

### 8. 주민 ↔ 건물 상호작용

빌드 4에서 주민이 살기 시작했지만 **화면의 주민과 경영 시스템이 아직 이어져 있지 않다.** 패널에서 `+`를 눌러도 숫자만 줄고, 화면의 주민은 그대로 돌아다닌다. 배정이 "무슨 일이 일어났는지 보이지 않는" 조작으로 남아 있다.

**직접 조작 — 선택 후 건물 클릭**

- 드래그 사각형 또는 클릭으로 **주민을 선택**(빌드 4에서 선택·아웃라인까지 구현됨) → **생산 건물을 클릭**하면 선택한 주민이 그 건물로 배정된다
- 배정된 주민은 **NavMesh로 건물 입구까지 걸어가 안으로 들어간다.** 즉시 사라지지 않는다 — 밤에 건물로 귀가하는 기존 동작과 같은 경로를 재사용한다
- 여러 명을 선택했으면 건물 정원만큼만 들어가고 나머지는 선택이 유지된다
- 배정 불가(정원 초과 · 밤 · 대상이 생산 건물이 아님)일 때 **아무 일도 안 일어나는 대신 이유가 보여야 한다** — 커서 · 아웃라인 색 · 짧은 토스트 중 택1
- 해제 조작도 함께 정한다 — 건물을 클릭해 빼내는지, 패널 `-` 만 쓰는지

**패널 배정에도 연출을 붙인다**

- 패널에서 `+`를 눌러도 **유휴 주민 중 하나가 실제로 그 건물로 걸어 들어간다.** `-`를 누르면 건물에서 나온다
- 지금은 숫자만 바뀌어 배정이 체감되지 않는다. 두 경로(직접 조작 · 패널)가 **같은 결과**로 수렴해야 한다
- 걸어가는 도중 낮이 끝나거나 배정이 취소되면 어떻게 되는지 정할 것 — 이동 중 상태를 시스템이 알아야 한다

**선행 과제 (구조)**

- `AssignVillager`가 `void`라 **성공 여부를 알 수 없다** — 직접 조작은 실패 피드백이 필수라 반환 계약부터 고쳐야 한다
- 주민 선택 상한이 "표시만 막는" 구현이라 정보 패널·배정이 붙으면 어긋난다 (WL-158)
- 배정된 주민의 **저장·복원** — 현재 세이브는 배정 수만 절대값으로 갖는다. 이동 중 상태를 저장할지, 낮 시작 시점(자동 저장 시점)엔 이동이 끝나 있다고 볼지 결정

**나머지 주민 축**

- **밀도 의존 수치 5종 재실측** — 정본 씬 기준으로. 춤이 사문화됐는지 먼저 확인 (WL-153)
- 밤 일괄 수거 시 대화 세션이 해산 쿨다운을 못 밟는 문제 (WL-159)
- 후속 콘텐츠 후보 — 직업 · 공연 · 구경(R13/R14)

**결정 필요**
- 조작 모델 — 선택 후 건물 클릭 / 들어서 드롭(드래그 앤 드롭) 중 어느 쪽인가. 타워 배치가 이미 "버튼 → 고스트 → 클릭" 이라 **일관성 기준으로 판단할 것**
- 배정 해제를 화면 조작으로 열 것인가, 패널 `-` 만 둘 것인가

### 9. 룩 마무리 — 전투 공간 · 연출

빌드 4에서 툰 셰이딩이 **경영 공간에만** 적용됐다. 같은 화면에서 낮/밤을 오가는 게임이라 두 공간의 그림이 갈리면 바로 보인다.

- **전투 공간 머티리얼 변환** — 경영 공간 115개를 변환한 규모를 감안해 일정을 잡을 것. 타일 · 타워 · 몬스터 · 투사체가 대상
- **픽셀 필터 확인** — 렌더러 피처 설정을 **PC · Mobile 파이프라인 에셋 양쪽**에 적용했는지 점검(현재 `PixelationSettings_PC`만 확인됨, CLAUDE.md 규약)
- **타워 배치 · 합성 연출의 아트 방향 확정** — 현재는 임시 버전이다. 이대로 확정할지, 룩에 맞춰 다시 만들지 결정
- 물에 닿지 않았는데 화면상 닿아 보이면 거품이 생기는 문제 (빌드 3에서 이월)
- 최대 축소 시 주민이 점으로 보임 — 오쏘 카메라라 거리 보정이 무효

## 사전에 확정해야 할 것

- **이번 빌드 범위 우선순위** — §1 UI 킷 · §3 자원 개편이 두 번째 이월 항목이다. 여기에 §9(전투 공간 툰 셰이딩)가 더해지는데, 빌드 4에서 절반만 적용된 축이라 **미루면 톤이 섞인 화면이 한 빌드 더 나간다**. §6 콘텐츠 확장 · §8 주민 상호작용은 이번에 새로 들어온 축이므로 이월 항목과의 우선순위를 먼저 정할 것
- **튜토리얼을 베타로 미룬 결정 재확인** — 안내 층은 UI · 자원 이름 · 밸런스 확정 이후가 맞는지
- 타워 배치 · 합성 연출을 임시 버전으로 확정할지, 룩에 맞춰 다시 만들지 (§9)
- 자원 소비처 매트릭스와 새 이름 목록 — §3이 §1(아이콘) · 세이브 스키마의 선행 조건
- **특수효과 · 타워를 몇 종까지 늘릴 것인가** (§6) — 웨이브 수와 보상 상한(3)에서 역산해야 하므로 웨이브 수 확정이 선행
- **주민 배정 조작 모델** (§8) — 선택 후 건물 클릭 / 들어서 드롭 중 택1. 타워 배치 조작과의 일관성 기준으로 판단
- 영토 개편의 목표와 이번 빌드 범위, 노드 ID 마이그레이션 필요 여부
- 랜덤화 잔여 범위(버프 타일 · 웨이브 · 보상)와 난이도 검증 기준
- 직접배치 로스터 축소(11 → 7종) 사인오프 — 반려 시 개틀링 · 스나이퍼 패널 복귀
- 합성 족보와 합성 추가 비용(특수 자원 사용 여부)
- 전체 웨이브 수 확정 — 맵 공개 페이스와 승리 조건의 단일 출처
- UI 킷 · 폰트 · 오디오 에셋 조달 및 라이선스
- 기준 해상도 및 지원 해상도 목록
- 세이브 포맷 변경 시 마이그레이션 정책(구 세이브 무시 vs 변환) — v1이 이미 배포됐다
