# WatchList — 활성 이슈 원장 (PR 리뷰 필수 입력)

> **목적**: PR 리뷰 간 '기억'. 리뷰 봇은 매 리뷰에서 이 목록과 대조하고 해결/악화/신설을 보고한다.
> **이 파일에는 미해소 항목만 둔다.** 해소·폐기 항목은 `WatchList-Archive.md`로 이동한다
> (삭제하지 않는다 — 재발 감지용). 번호는 재사용·재정렬하지 않는다.
> **갱신 규칙**: 봇이 리뷰 코멘트의 "📒 WatchList 갱신 제안" 절에 항목을 제안하면, 팀이 검토 후
> 이 파일에 커밋한다. 봇이 직접 커밋하지 않는다. 해소되면 아카이브로 옮긴다.
> **상태를 갱신할 때는 기존 서술을 고쳐 쓴다** — "갱신(PR#X)"을 덧붙이지 말 것. 이력은 git과 아카이브가
> 갖는다(이 누적이 한 줄을 3,800자까지 부풀렸고 2026-07-30 감사에서 압축했다).
> **형식**: WL-번호 | 상태(OPEN/PARTIAL) | 담당 | 지금 무엇이 문제인가(코드 근거) | 해소 조건
>
> **감사 이력(2026-07-30)**: 141항목 단일 원장(124KB)을 **전 항목 코드 대조 감사** 후 활성/아카이브로 분리하고
> 각 항목을 현재 상태만 남겨 압축했다(이력은 git과 아카이브가 보유).
> `⚠️ 검증 상태` 표시 항목은 해소로 판정됐으나 독립 반증을 거치지 않아 활성에 남겨 둔 것이다.

- **WL-001** | PARTIAL | KIM-SUNGSOO/muchan918 | Tower/Enemy는 TowerAsset/EnemyAsset 소비 완료(Tower.cs:10, Enemy.cs:9), Enemy SO 8종 값 전부 기입. 잔여: lightning_tower(TowerType=Chain)만 Attack/ChainRadius/MaxChainTargets 전부 0 — 배치해도 무동작(Assets/Resources/ScriptableObjects/Towers/lightning_tower.asset). Soldier 스탯 이관 축은 병사 리젝으로 폐기(SoldierData.cs·Personal/SUNGSOO/SoldierData.asset 잔재만 남음, 제거 대상) | lightning_tower 수치 기입 + 공격 계약 정본(TowerAsset.*.Attack) 팀 합의
- **WL-002** | OPEN | N0WST4NDUP | 매니저 수명주기 규약 문서가 없어 `static Instance` 싱글톤 19종이 씬 스코프 / DontDestroyOnLoad / RuntimeInitializeOnLoadMethod 3패턴으로 난립(GameSceneManager.cs:17, OutlineInteractionDriver.cs:36, MouseManager.cs:20·92, TooltipUI.cs:15). #261이 `SelectionBoxView`(SelectionBoxView.cs:35-41 — RuntimeInitializeOnLoadMethod→DontDestroyOnLoad 자가 부팅, `Instance` 없이 `MouseManager.Instance`를 pull)를 추가해 자가 부팅 오버레이가 `TowerTooltipView`와 2종이 됐다. #302가 `ResidentSelectionCoordinator`(`:81-89`)로 **세 번째 사례**를 추가했고, 이쪽은 `LateUpdate`에서 `ManagementController`를 찾는 씬 탐색까지 들고 있어(못 찾으면 120프레임 백오프) 모든 씬에 상주 비용이 붙는다. #361이 `AudioManager`(AudioManager.cs:80-92)로 **네 번째**를 추가했다 — 이쪽은 씬 탐색 없이 씬 컴포넌트(`SoundCue` 계층)가 매니저를 pull하는 방향이라 상주 비용이 없고 `GameSceneManager` 선례를 그대로 복제했다. #341이 `ResidentDragCoordinator`(`:62-72`)로 **다섯 번째**를 추가했고, 이쪽은 세 번째와 같은 형태이되 `LateUpdate` 씬 탐색 대상이 **2종**(`ManagementController`·`ResidentSpawner`, `:145-153`)이라 컨트롤러가 없는 씬(TitleScene·전투)에서도 120프레임 백오프 탐색이 상주한다(둘 다 찾으면 조기 반환하므로 비용은 유계). #444가 `KeyboardManager`(`Assets/Scripts/GameManager/KeyboardManager/KeyboardManager.cs:91-99`)로 **여섯 번째**를 추가했다 — 씬 탐색이 없고 바인딩 목록이 static이라 상주 비용은 `Update`의 `Keyboard.current` 조회 하나이며 `AudioManager` 계보다. #406이 `UiClickSfx`(`Assets/Scripts/GameManager/UiClickSfx.cs:43`)로 **일곱 번째**를 추가했다 — `AudioManager`·`KeyboardManager`와 같은 계보(씬 탐색 없음, 상주 비용은 `Update`의 `Mouse.current` 조회 하나)이고 중복 파괴 가드도 있다. 같은 #406이 낸 `Sfx`는 **정적 클래스**라 여기 세지 않는다(부팅도 씬 배치도 없이 `Resources.Load`로 뱅크만 집어온다) — 자가 부팅을 늘리지 않고 같은 목적을 푸는 선례라 규약 문서를 쓸 때 참고할 것. 새 패턴은 아니지만 규약 부재 상태에서 사례가 늘고 중복 부팅 가드가 없다. Docs/Core에 부트스트랩 문서 부재. MouseManager._camera 씬 참조 끊김은 sceneLoaded→SetCamera(Camera.main)로 해소됨 | 부트스트랩/매니저 규약 문서화 + TooltipUI·TowerInfoUI·BuildingInfoUI 등 임시 UI 싱글톤 UIManager 흡수. **자가 부팅 6종 중 셋(코디네이터)은 #371에서 골격을 추출하며 함께 닫힌다** — 그때 규약 문서를 같이 쓸 것
- **WL-005** | PARTIAL | KIM-SUNGSOO | 전투 레이어 3종(Enemy/Soldier/PlayerBase)은 ProjectSettings/TagManager.asset:15-17에 등재 완료 — 미설정 무증상 위험 해소. 잔여: 대상 탐지를 LayerMask로 할지 Tag로 할지 미확정 상태로 Tower.cs:15 / Enemy.cs:13 / Soldier.cs:9에 `TODO(TBD)` 주석이 남아있고 CombatSystem/COMBAT_TBD.md:9가 팀 컨벤션 회의를 대기 중(코드만으로 닫히지 않음) | 회의에서 LayerMask/Tag 확정 후 TODO(TBD) 일괄 제거 + COMBAT_TBD.md 갱신
- **WL-006** | OPEN | muchan918 | 에셋 로딩 이원화 유지 — DataTable CSV 5종은 `Resources.Load<TextAsset>`(TowerTable.cs:13 외 4개), Localization은 Addressables 1.25.0(com.unity.localization 1.5.12 의존). 종전 근거였던 `OutlineSmoothMeshRegistry.cs`(SO 로딩까지 Resources 축에 얹던 사례)는 삭제돼 범위가 CSV·Localization 둘로 다시 좁아졌다 | 로딩 전략 단일화 결정(Resources 유지 vs Addressables 전면 이관)
- **WL-008** | PARTIAL | sunjin1222 | Run 단위 마스터 시드 소유자 도입 완료 — `RunSeedDeriver.Derive(master, tag)`(고정 FNV-1a)로 CombatMap/Territory 시드를 파생하고 `RunBootstrapper`가 두 생성기보다 먼저 주입한다. `RunSeedData`에 요청/최종 사용 시드를 기록하며 fallback 선택도 `RequestedSeed` 기준으로 결정적이다. Play에서 같은 마스터 시드의 전투맵·버프 타일·영토 동일 재현과 일반 새 게임의 매 Run 랜덤화를 확인했다. 잔여: 정본 씬에 배치되지 않은 구 MapBuilder 경로의 전역 `UnityEngine.Random`(`MapRandom.cs`, `LavaGenerator.cs`, `StageTilePathBuilder.cs`, `StageConnectionManager.cs`) 및 웨이브/보상 등 후속 시스템 시드 파생 | 구 MapBuilder 삭제 여부 결정 + 저장/보상 작업에서 시스템 태그 확장
- **WL-009** | OPEN | sunjin1222 | '스테이지' 용어 충돌 잔존 — 코드의 `StageBuilder`/`StageWaypoint`(StageWaypoint.cs:12)는 맵 블록 단위인데 GDD.md:98은 런 단위 스테이지. 신맵에 WaypointGenerator/WaypointOrderer가 추가돼 맵 쪽 '웨이포인트' 명칭이 더 확산. 병사 배치 지점(GDD §5.4)과의 충돌은 병사 리젝으로 소멸 → 기존 트리거 "병사 착수 전 리네임"은 무효 | 맵 블록 단위 용어 리네임(Stage→Block 등) 합의 시점 재설정
- **WL-011** | OPEN | N0WST4NDUP | 선택 통지 이중 경로 유지 — 이벤트(MouseManager.cs:243 발행, TooltipUI·OutlineInteractionDriver 구독)와 별개로 ISelectable 구현체가 패널을 직접 호출(Tower.cs:262, SelectableTest.cs:28 `TowerInfoUI.Instance.ShowInfo`). 두 경로가 같은 선택을 각자 처리해 해제 누락이 발생(TowerMergeCoordinator.cs:35·MouseManager.cs:182 주석에 증상 기록) | 정본 경로 결정(이벤트 단일화 또는 직접 호출 폐기)
- **WL-012** | OPEN | 미정 | GDD §8 미확정 항목 다수 잔존 — 몬스터 테마, 병사 도입 여부(팀은 리젝했으나 GDD.md:270이 아직 미확정 체크박스), 스테이지/보스 구성, 밸런싱 수치, 건물 해금 목록이 미체크(Docs/GDD.md:269-275). 해당 영역 코드는 결합을 느슨하게 유지할 것 | GDD §8 항목 확정(특히 병사 리젝의 GDD 반영) 시 해제
- **WL-014** | OPEN | 미정 | Get/Instance 무가드 역참조 잔존 — `DataTableManager.Get(...).Get(...)` 체인(BuildingInfo.cs:31, ManagementController.cs:711)과 `MouseManager.Instance.` 직접 호출(PlacementButton.cs:22, TowerPlacer.cs:265, SkillButtonView.cs:123). 일부 호출부만 `?.` 적용(ResidentSelectionCoordinator.cs:159, TowerPlaceCommand.cs:57. Tower.cs:517-520은 `?.`+근거 주석으로 **가드 쪽 사례로 넘어갔다**)이라 관행 미정착. 규약 문서는 **어디에도 없다** — 유일한 근거였던 TerritoryGraph.cs 주석이 #337 영토 시스템 삭제와 함께 사라졌다 | 호출부 null 가드 규약 문서화 + 기존 호출부 일괄 적용
- **WL-015** | OPEN | muchan918 | 밸런싱 수치가 CSV 밖 SO에 authoring — TowerTable.csv 헤더에 수치 컬럼이 없고 damage/range/splash/chain/오라 수치는 TowerAsset.cs:39-96(AttackFields·Buff/DebuffAuraFields) 인스펙터 수기 입력. 건물·마법연구소·교환소·본성 레벨 테이블도 BuildingAsset.cs:284,304,339,370의 `UpgradeLevels`(SO). 계약 #2(수치=CSV)와 긴장하며 미기입 시 조용히 무동작(lightning_tower.asset 전 필드 0) | 폴리모픽 스탯의 데이터 원본 합의 + 값 기입
- **WL-017** | PARTIAL | muchan918 | 지갑 소유·소비 게이트웨이는 확정·문서화 완료 — ManagementController.cs:70이 `_wallet`을 씬 스코프로 소유하고 :174/:188 `CanAfford`/`TrySpend(IReadOnlyList<ResourceCost>)`가 원자 차감 창구(타워 배치·합성·건물 업그레이드·주민 증축 전부 경유), SystemMap.md:262 등재. 환원 짝(`Grant`, :213)은 WL-147이 따로 본다. 잔여: 전역/씬 간 공유(다른 씬 소비처 접근) 방식만 미확정 — WL-002 부트스트랩 규약에 종속돼 코드만으로 닫히지 않음 | 전역 매니저/부트스트랩 규약 확정 시 최종화
- **WL-018** | PARTIAL | muchan918 | 밤 종료·승패 판정의 정식 경로 3종 배선 완료 — 웨이브 클리어→`MonsterSpawn.EndNightIfNight`(MonsterSpawn.cs:251→259), 최종 웨이브→`WaveCompletionCoordinator`의 `TriggerVictory`(:60), 본진 파괴→`PlayerBase.GameOver`→`TriggerGameOver`(PlayerBase.cs:67). 잔여: 임시 버튼 3종이 NightActionPanelView.cs:93/:104/:114에 남아 밤마다 활성(:83-85)돼 정식 경로와 중복 트리거 가능 | NightActionPanelView의 웨이브 성공/실패·보스 처치 버튼 제거 + DayNightManager.cs:70 테스트 주석 정리
- **WL-019** | PARTIAL(해소 검증대기) | KIM-SUNGSOO | Tower.Update가 호스트 한 곳에서 밤 게이팅 수행(DayNightManager.CurrentPhase!=Night이면 ActivePhase=NightOnly 행동 스킵, Instance null 가드 포함). 정식 웨이브 판정도 연결됨(MonsterSpawn.EndNightIfNight, WaveCompletionCoordinator). 임시 밤 종료 트리거 잔여는 WL-018 소관. 재발감지: 개별 ITowerBehaviour가 DayNightManager를 직접 폴링, ActivePhase 미선언 행동 추가 (근거: Tower.cs:353-362) | ⚠️ 2026-07-30 감사에서 해소로 판정됐으나 독립 반증을 거치지 못했다. 다음 리뷰가 근거를 확인하면 아카이브로 옮긴다
- **WL-020** | OPEN | muchan918·n0wst4ndup | DayNightLightingController가 RenderSettings.ambientMode/skybox/ambient 3색을 씬 전역으로 기입. 경영·전투가 같은 GameScene에 공존해 전투 공간 조명·스카이박스까지 함께 바뀐다(팀 계약 #4 공간 분리 위반 소지). 공간별 분리 코드 없음. **#136으로 영향 범위가 넓어졌다** — 전역 기입 대상에 `NightVolume.weight`(화면 전체 그레이드)와 물 틴트가 추가됐고, `Night Wipe` 풀스크린 패스(#101)도 카메라 단위라 두 공간을 구분하지 않는다. 화면공간 효과는 애초에 공간별 분리가 불가능하므로 **"공간별 라이트/볼륨 분리"라는 원래 해소 방향이 이 축에는 성립하지 않는다**. 밤 프리셋은 전투 가독성을 고려해 튜닝했으나(`VisualLookPipeline.md` §3.3.1) 전투 공간 실측은 `MapBuilder`가 런타임 생성이라 플레이 모드에서만 가능 | sunjin1222(맵빌더)와 조명 영향 범위 합의 — 화면공간 축은 "두 공간이 같은 룩을 공유한다"를 명시적으로 승인하거나, 공간 전환 시 프리셋을 바꾸는 별도 축이 필요
- **WL-021** | PARTIAL | N0WST4NDUP·muchan918 | 라인 생성원이 아직 인스펙터 `BuildingAsset[] _productionBuildings`(ManagementController.cs:18, BuildModel :596-640) — 배치된 건물 인스턴스가 아니다. 건물 배치(#27) 미구현(BuildingPlacer 부재). **#341이 같은 전제를 쓰는 소비처를 하나 더 얹었다** — `BuildingInstanceRegistry`(`:19`)가 `Dictionary<BuildingAsset, Transform>`으로 SO를 씬 인스턴스의 키로 삼는다. 즉 `LineIndexOf(BuildingAsset)`(`:368`)와 **한 쌍**이 됐다. ⚠ **이 전제 자체는 결함이 아니다** — 생산 건물은 3종 고정이고(GDD §5.7) 「건물 건설」은 *"새 건물을 지어 작업을 해금"*이라(GDD §5.2) 같은 건물을 여러 채 짓는 개념이 없다. 해금으로 건물 **종류**가 느는 것도 새 SO라 무관하다. 깨지는 것은 **같은 SO의 인스턴스가 둘이 되는 경우**뿐이다. 해소분: BuildingAsset 이관·TryCreate 실사용·라인별 주민당량 분화, 확장 자원은 Supply 고정 row(SupplyDaily:898)로 충족 | #27 착수 시 **먼저 "같은 건물을 여러 채 지을 수 있는가"를 확정한다.** 아니면 SO 키는 그대로 유효하고 이 항목은 라인 생성원 축만 남는다. 맞다면 `LineIndexOf`와 `BuildingInstanceRegistry`를 **함께** 인스턴스 기반으로 옮긴다 — 한쪽만 옮기면 배치 −1 퇴장이 늘 첫 번째 건물에서만 나오고 경고는 씬 로드 때 한 번만 지나간다(업그레이드 소유 단위=타입 확정 전제, BuildingUpgrade.md §2)
- **WL-023** | PARTIAL | sunjin1222·N0WST4NDUP | 정본 GameScene에는 CameraController2만 배치돼 '카메라 2개 병존'은 완화(CameraController.cs는 동일 폴링 패턴으로 미사용 잔존). 남은 문제: CameraController2가 Mouse/Keyboard.current를 직접 폴링해(CameraController2.cs:114-141,156-210) MouseManager의 '입력 단일 창구' 계약 밖에 있고, SystemMap.md에 카메라 입력 소유권 기재가 전혀 없음 | 미사용 CameraController.cs 정리 + 카메라 입력 소유권 합의를 SystemMap §4.1에 등재
- **WL-024** | OPEN | sunjin1222·N0WST4NDUP | 줌이 `Lens.OrthographicSize`에 하드결합돼 있고 발행이 `CameraController2.ZoomMouseWheel` 인라인이라 `ApplyZoom` 격리 seam이 없다. **#138이 같은 자리에 `OnZoomChanged`(변경 후 절대 오쏘 사이즈) 발행을 추가해 소비 축이 둘이 됐다** — `PixelationZoomBinder`(매 `LateUpdate` 폴링, `[ExecuteAlways]`라 편집 모드에서도 돌아야 해 이벤트 이관 불가·의도된 병존)와 `ZoomDrivenVisibility`(push + 붙을 때 1회 pull). 새 이벤트의 xml 주석이 "휠이 유일한 발행처, 다른 경로를 추가하면 그쪽에서도 발행해야 한다"를 자체 경고하지만 강제 장치는 없다 — 부드러운 줌·대상 줌인·세이브 복원 중 어느 것이 `Lens`를 직접 써도 힌트가 조용히 옛 상태에 머문다(증상은 "가끔 줌 힌트가 안 켜진다"라 원인에서 멀다). #28 구도 확정 기록은 여전히 없으나 `InteractionOutline.md`가 직교 전제로 작성돼 사실상 직교로 수렴 | `ApplyZoom(float)` 추출로 클램프·Lens 기입·이벤트 발행을 한 곳에 모음(#28 투영 확정과 별개로 지금 가능, 소비처가 둘일 때가 가장 싸다) + #28 투영 방식 문서 확정
- **WL-025** | RESOLVED 후보 | KIM-SUNGSOO | 3번째 재발 지점(ProjectileFlight.cs:274 매 프레임 OverlapSphere)이 이번 델타에서 CheckSphere 전환으로 사라졌다. 원 항목의 나머지 2곳(Projectile.cs ApplyArea/FindNearestUnhit)은 그대로 열려 있어 완전 해소는 아니다 — 그 2곳만 남은 것으로 범위를 좁혀 유지 | ApplyArea/FindNearestUnhit 2곳을 공유 NonAlloc 버퍼로 교체
- **WL-026** | PARTIAL | KIM-SUNGSOO | CC 가동률 상한이 코드 기본값에 갇혀 있던 문제는 해소 — `StunStatus.ImmunityWindow`(`HitEffect.cs`, 기본 0.4 · `[Min(0f)]`)가 창을 타워 SO에서 저작하고, `ApplySlow`가 **부여 시점에 `activeStunImmunity`로 스냅샷해 `EndStun`까지 들고 간다**(만료 시점 조회가 아니라서 "이 면역 창이 누구의 규칙인가"가 흐려지지 않는다). `stunImmunityWindow = 1.4f`(StatusEffectHandler.cs:75)는 핸들러를 미리 부착하고 `ApplySlow`를 직접 부르는 호출자용 폴백으로만 남아 실플레이 경로에서 쓰이지 않는다. soda 기준 대상당 가동률 상한이 23% → `0.7/(0.7+0.4)`≈64%로 열렸다. 잔여: GDD §7에 CC(스턴/둔화) 축 + 보스 면역/저항 규칙 사인오프가 여전히 없고, **0.4라는 값 자체가 구현자 추정**이다(플레이모드 실측 미수행) | GDD §7 CC 사인오프(WL-012 연동) + 면역 창·스턴 지속 플레이모드 실측
- **WL-027** | OPEN | KIM-SUNGSOO·muchan918 | 몬스터 밸런싱 수치가 CSV 밖 SO 인스펙터에만 있다 — EnemyTable.csv 헤더는 EnemyID,NameKey,EnemyType,RoleKey,DescriptionKey뿐이고 MaxHp/MoveSpeed/AttackDamage/AttackRange/AttackInterval(EnemyAsset.cs:34-44 CombatFields)에 더해 #453의 자폭 저작(EnemyAsset.SelfDestruct.Enabled/Damage, EnemyAsset.cs:62-69)까지 SO 전용 축으로 늘었다. Enemy.cs가 SO를 단일 소스로 읽어 다운스트림([Feature] 몬스터 스폰 시스템 #14/[Feature] 몬스터 이동 시스템 (웨이포인트) #15/[Feature] 몬스터 공격 시스템 (본진 피해) #16)이 테이블로 스탯을 조회할 수 없고, 자폭 데미지는 규약 ④ 예산의 입력이라 밸런싱 일괄 조정 대상이다 | 공통 스탯 + 자폭 필드의 CSV 승격 여부 합의([Feature] 몬스터 데이터 설계 #26 오너) — 코드 변경 전 합의 대기
- **WL-029** | OPEN | N0WST4NDUP | 경영 영토 모델 16개 파일이 전부 네임스페이스 없이 전역(`^namespace` grep 0건; TerritoryGraph.cs:17/TerritoryNode.cs:11/TerritoryController.cs:11/TerritoryDefinition.cs:18 등 일반명 타입). 전투 영토(#1)는 아직 미착수(CombatTerritory 심볼 0건)라 충돌은 잠재 상태 ⚠ **근거 파일 전부가 없다**(2026-08-22 줄참조 감사) — 영토 모델 파일이 #337 영토 시스템 삭제와 함께 제거됐다(`TerritoryGraph`·`TerritoryNode`·`TerritoryController`·`TerritoryDefinition` 모두 0건). 전역 네임스페이스 충돌 위험 자체가 사라졌으므로 **아카이브 후보** — 상태 변경은 팀 판정. | #1과 모델 공유/분리 합의 + Territory 네임스페이스 격리(#1 착수 전 선행)
- **WL-034** | PARTIAL | N0WST4NDUP | 신맵 경로는 단일 출처화됨 — TowerPlacer.Awake:93-99가 `CombatMapGenerator.Settings.TileSize`로 인스펙터 값을 덮어쓴다. 잔여: `TowerPlacer.tileSize`(=5f, :37)와 `StageBuilder.TileSize`(=5f, StageBuilder.cs:15)가 여전히 독립이고 구맵은 :104-107 경고만이며, 좌표 변환식 사본이 TowerPlacer.cs:255-257,:370 / CombatMapTileSpawner.cs:267 / StageMapSpawner.cs:45에 중복. **#265가 `TowerPlacer.TileSize` public 게터를 열어 배치 밖(연출부 `TowerFusionController`)까지 이 사본을 소비하기 시작했다** — 접근자가 늘고 소비 축이 배치 경계를 넘었다. **🔥 2026-08-04 실제 발현: 맵 루트(`MapBuilder`)를 Y 59.45° 회전시키자 사본만 깨졌다.** `CombatMapTileSpawner`는 로컬 공간 + `TransformPoint`라 자동으로 맞았고, `TowerPlacer`의 사본은 월드 X/Z 산술이라 셀 하이라이트가 타일과 59.45° 어긋났으며 이웃 셀은 5.95유닛 벗어났다(`OverlapSphere` 반경 2.4 초과 → 타일 미발견). 타워 9종이 전부 1×1이라 위치 오차가 0으로 상쇄돼 **하이라이트 방향만 증상으로 드러났다** — 다중 셀 타워가 추가되면 배치 자체가 불가능해질 상태였다. 1차로 앵커 타일 회전을 기준축으로 고쳤다가, 그 방식이 "타일 로컬 회전이 항상 0"이라는 전제에 기대는 것이라(타일 프리팹에 랜덤 yaw가 들어오면 셀마다 축이 튄다) 2차로 `CombatMapTileSpawner.CoordinateRoot`를 공개해 **축의 출처를 `tileSize`와 같은 곳으로 통일**했다(`TowerPlacement.md` §3 좌표). **다만 위치 변환식 자체는 두 구현이 여전히 별개**라 이 항목은 그대로 열려 있다 | WL-007 공용 셀↔월드 변환 유틸로 흡수(연출부도 그 유틸을 참조하도록 함께 이전). 참고: TowerPlacer.cs:35 주석의 'WL-032'는 WL-034 오기
- **WL-038** | PARTIAL | SUNJIN | 파괴 일원화는 완료(Enemy.cs:83·337 RouteCompleted 구독, :274-279 isDying 가드, MonsterSpawn.cs:351-373 스폰 필수 검증). 잔여는 집계 오차: `MonsterStateMachine.cs:161`의 `Destroy(gameObject, destroyDelay=2f)` 때문에 `MonsterSpawn.AliveMonsterCount(=childCount, :329)`에 보스 자신과 사망 연출 중 몬스터가 섞이고, 이 값이 `EnemySpawnMinionsAction.cs:93`의 MaxAlive 판정 입력이라 P4 소환 상한(authoring 30)이 의도보다 빡빡하다 | #235 Play 실측으로 MaxAlive 보정(BossNodeReference.md:217 체크박스) 또는 집계를 '전투 중 생존'으로 분리
- **WL-039** | OPEN | SUNJIN | `Assets/Scripts/Monster` 전체에 `namespace` 선언이 0건 — `MonsterState`/`MonsterStateMachine`(MonsterStateMachine.cs:5·13)·`MonsterAnimation`(:3)·`MonsterMove`가 전역 이름공간에 그대로 있다. CombatSystem은 `NorthLand.Combat`으로 격리됐는데 Monster만 남아 이름 충돌 위험이 이쪽에 몰린다 | `NorthLand.Monster` 격리 + 참조부(Enemy.cs:33·289, StatusEffectHandler.cs) using 갱신
- **WL-040** | OPEN | 팀 공통(온보딩) | 정본 씬이 Imported(중첩 repo) 에셋에 의존하는데 동기화 보장 규약이 여전히 없다 — `GameScene.unity`가 `Imported/@NorthLand/Prefabs/Management/Management2.prefab`(경영 본진 전체)을 참조하고, `MonsterSpawn.cs:28 gatePrefab`→`Props/BaseGate.prefab`(GameOver 경로), `ProductionLineView.cs:22-28`→`UI/ProdRow.prefab`도 상주. 미동기화 시 본진·성문·자원 UI 실종. **#302로 종속 범위가 크게 늘었다**: `CandyLand.prefab`에 보행 지면 33개의 `Ground(10)` 레이어 지정·`ResidentWaypoint` 10개·`ResidentDoorPoint` 21개·`NavMeshSurface`·`NavMeshLink`(초콜릿 다리)가 들어갔고, **베이크 산출물(`NavMesh-NavMesh.asset`)도 프리팹 모드 베이크라 `Imported/@NorthLand/Prefabs/Management/CandyLand/` 안에 있다**. `Marshie` 프리팹에는 `CapsuleCollider`(선택 레이캐스트의 전제)와 `NavMeshAgent` 반경 0.6이 있다. 즉 메인 repo만 받으면 NavMesh·웨이포인트·문·주민 콜라이더가 전부 없어 **주민의 이동과 선택이 통째로 죽는다**. 두 저장소의 머지 순서도 규약이 없다. 문서는 SystemMap.md:329-330 리뷰어 주석뿐이다. **#445가 사례를 하나 더 추가하면서, 처음으로 이 축의 침묵을 로그로 닫았다** — 정보 패널 「합성 후보」 칸 프리팹(`/Prefabs/UI/TowerTargetSlot.prefab`)이 미동기면 `TowerInfoUI._mergeSlotPrefab`이 null이 되어 블록이 그냥 안 뜨는데, `HasMergeSlotWiring`이 「무엇이 null인지 + Imported 동기화 확인」을 **1회** 경고하고(타워 클릭마다 도는 경로라 매번 짖으면 콘솔이 죽는다) §4에 동기화 계약도 등재했다(Imported `85fd857d3` 이상). 즉 개별 사례 처방은 「§4 계약 등재 + null 시 1회 경고」로 형태가 굳었고, 남은 것은 그것을 온보딩/CI 규약으로 올리는 일이다 | Imported 동기화를 온보딩/CI 규약으로 명문화(Docs/Core에 항목 추가) + 두 저장소 머지 순서 규약(Imported 선행)
- **WL-041** | PARTIAL | n0wst4ndup | 지정된 잔여 실행은 완료 — 죽은 사본 `Assets/Personal/SUNGSOO/Font/`가 6888b00에서 전부 삭제됐고 충돌 마커도 0건. 남은 것 둘: (1) `Pretendard-Regular SDF`가 아직 2벌 — `TextMesh Pro/Fonts/`(abd3dbb3, Static 43.7MB, 실제 참조)와 `Resources/Fonts & Materials/`(f84cee8f, 2.1MB)인데 후자는 씬·프리팹·TMP Settings 어디서도 미참조인 고아. (2) 근본 원인 잔존 — `PretendardJP SDF`가 Dynamic(m_AtlasPopulationMode:1)이라 아틀라스 churn이 계속 커밋된다 | 고아 사본 삭제 + JP Static 재베이크
- **WL-043** | PARTIAL | n0wst4ndup·SUNJIN | 3택1 보상은 별도 컨트롤러로 분리 완료(`Assets/Scripts/Reward/`, `WaveCompletionCoordinator.cs:24-85`가 `WaveCleared`→보상→`EndNight` 소유). 잔여: 매-웨이브 마나석 지급이 여전히 경영 정산 훅 인라인(`ManagementController.cs:864` `_wallet.Add(ResourceKind.Mana, _manaPerWaveClear)`). GDD §5.6이 '별도 트랙'으로 명시해 위반은 아니나 웨이브 종료 보상이 두 진입점에 흩어져 있다 | 마나 트랙도 보상 컨트롤러 경유로 이관하거나 GDD에 '경영 정산 소유'를 명시해 종결
- **WL-046** | OPEN | n0wst4ndup | 영구 이름 씬 `Assets/Scenes/Branches/SweetLand.unity`가 번호 스냅샷 전용(주간 삭제 대상) 폴더에 그대로 있고 PR#114(6888b00) 이후 미수정. `EditorBuildSettings.asset` 미등재·`GameSceneManager.cs:42-45`는 TitleScene/GameScene만 로드해 인게임 미도달. 정본 GameScene은 그동안 Imported `Management2.prefab`으로 본진을 따로 구성해 이 씬은 고립됐다 | 필요 구성만 GameScene에 반영하고 파일 삭제(또는 정식 위치 이동) — 그때까지 주간 정리 제외
- **WL-047** | OPEN | muchan·n0wst4ndup | `Packages/manifest.json:16` terrain-tools 5.3.2가 유지되고 전제가 더 나아갔다 — `Management_Island.asset`(TerrainData)이 Imported `Management2.prefab`에 물리고, 그 프리팹이 **정본 `GameScene.unity`에 인스턴스로 들어가** Terrain(!u!218)+TerrainCollider(!u!154)가 정본 씬에 라이브다(GO `Terrain`, layer 0/Default). GDD·SystemMap에 'Terrain' 언급은 0건 | 경영 지면을 Terrain(비주얼 전용)로 갈지 노드 그리드로 갈지 합의 후 GDD §5.3·SystemMap 반영 + 배치 판정 지면과 Terrain Collider 분리 명문화
- **WL-048** | PARTIAL | n0wst4ndup | 게이트웨이(`ManagementController.CanAfford`/`TrySpend`, :124-152)는 완성이고 존재하는 모든 소비처가 경유한다(타워 배치 TowerPlacer.cs:281·300, 업그레이드 :351, 주민 증축 :435, 합성 TowerFusionController.cs:60·75). 잔여 조건 '건물 배치'는 판정 불가 — 경영 건물 배치/건설 시스템 자체가 미구현(BuildingPlacer 계열 심볼 0건, 건물은 씬 고정 배치 후 업그레이드/교환만)인데 GDD §5.2는 설계로 유지 | 건물 건설 구현 시 이 게이트웨이 재사용(별도 차감 로직 발명 금지)
- **WL-049** | PARTIAL | SUNJIN | 성공 판정이 여전히 스폰 태스크 내부의 `await UniTask.WaitUntil(() => monsterParent.childCount == 0)`(`MonsterSpawn.cs:231`) 하나뿐이고, 주석(:219-224)도 '타워 사거리 밖에서 성문을 두들기는 몬스터가 남으면 웨이브 성공 경로로 종료 불가(GameOver로만 수렴)'를 인정. 진전: `WaveCompletionCoordinator.cs:80-85`가 EndNight 소유, 웨이브 데이터 없음은 즉시 클리어(:135-147). 미해결: 맵 상한 도달을 종료로 처리하는 경로 없음(StageBuilder.cs:124 주석 처리) | 판정을 웨이브 컨트롤러로 이관 + 맵 상한도 종료 처리(WL-018과 세트). 주의: 코드 주석이 이 항목을 WL-037로 오참조(:91·134·224)
- **WL-050** | PARTIAL | SUNGSOO | 상호 원복은 해소 — `TowerStats`(TowerStats.cs:6·18·44·80)가 소스당 묶음 교체 + 서로 다른 소스 합산 + `Evaluate` 단일 합성으로 통일하고, `Tower`의 `damageMultiplier`/`attackSpeedMultiplier`/`activeBuffs`는 제거·`ApplyBuff`(Tower.cs:332)는 어댑터만 남았다. 잔여: 중첩 규칙 비대칭 — 타워 버프는 합산인데 적 감속은 `MoveSpeedComposer.cs:66-68`에서 `speedDebuffProduct *= factor` 곱산. 또 '실측 8종 통과' 근거가 저장소에 없다(NUnit `[Test]`·테스트 asmdef 0건) | 합산/곱산 의도 확정(WL-127) + 원장 회귀 테스트를 실제 테스트 어셈블리로 커밋
- **WL-052** | OPEN | n0wst4ndup | (WL-046과 본문·해소 조건이 동일한 중복 등재 — WL-046으로 통합) | 상세·해소 조건은 WL-046 참조
- **WL-053** | OPEN | muchan·n0wst4ndup | (WL-047과 동일 축 중복 등재 — WL-047로 통합) | 상세·해소 조건은 WL-047 참조
- **WL-054** | PARTIAL | SUNGSOO | 회의 결과에 따라 성문 체력의 단일 기준을 신규 `Gate_02` 프리팹의 `PlayerBase.maxHp: 200`으로 확정했다. 보스 P1 충돌 피해 126은 성문 HP 200의 **63%**이며, 관련 보스 문서도 같은 기준으로 정렬했다. `PlayerBase.cs:9`의 기본값 100은 변경하지 않았으므로 실제 성문 밸런스는 반드시 `Gate_02` 프리팹의 직렬화 값 200을 기준으로 한다. 잔여: 신규 프리팹의 `PlayerBase` 레이어(9), 콜라이더, 몬스터 교전 및 성문 파괴 시 GameOver 발화 실행 확인 | `Gate_02` 프리팹 기준 플레이 검증 + WL-049 판정 정합
- **WL-056** | PARTIAL | muchan918 | 웨이브 클리어 마나석 지급이 `ManagementController.HandleNightToDay` 인라인에 있어 밤 종료 트리거 이관(WL-018)과 무관하게 생존하고, GDD §5.6이 3택1 보상과 별도 트랙으로 명시해 선점 위험은 해소. 잔여: §5.6 보상 시스템 착수 시 이 인라인 지급을 교체 가능한 보상 훅으로 분리해야 3택1 특수효과와 재작업 없이 맞물림 | §5.6 착수 시 웨이브 종료 보상을 보상 컨트롤러 진입점으로 분리
- **WL-059** | OPEN(악화) | muchan918 | 경영 개인 씬 사본이 **6개로 늘었다** — `Assets/Personal/muchan/Scene/`에 ManageSpace·ManagePrefabScene·ManageTerritoryScene·_v2·**_v3(신규)** + `Personal/SUNGSOO/Scene/ManageSpace-Sungsoo`. 정본 GameScene 병합은 SceneWorkflow §4로 이뤄지고 있으나 사본이 소진되지 않아 WL-028(정본 이원화) 재발 통로가 유지된다 | 개인 사본 소진 + 신규 v번호 생성 시 정본 반영 후 즉시 폐기 규약화
- **WL-061** | PARTIAL | muchan918 | `headquarters`→`castle` 개명은 완료(`castle.asset` 존재, headquarters.asset 없음). 잔여: BuildingTable.csv 6행(woodcutter_house/mine/farm/castle/alchemist_house/magic_lab)에서 빠진 병사 건물 3종의 **SO가 고아로 잔존**(`church.asset`·`training_camp.asset`·`military_school.asset`) — 병사 시스템 리젝으로 복원 가능성이 사라졌으므로 삭제 판단만 남음 | 고아 SO 3종 삭제 여부 확정(병사 리젝 정리와 동반, GDD §5.4 잔재 정리)
- **WL-063** | OPEN | sunjin1222 | 몬스터 발 높이 단일 출처 부재 — `CombatMapTileSpawner.cs:48` `monsterWaypointYOffset = 6f`가 Road 프리팹 지오메트리·TileHeight에서 파생되지 않은 매직 상수인데 경로/스폰 Y를 결정한다(:639, :677 사용). 프리팹·맵 Y 변경 시 몬스터 파묻힘/부양이 조용히 발생 | 프리팹 상단 높이/TileHeight에서 오프셋 파생 또는 단일 출처 문서화 + 성문 사거리 높이 정합 확인(SUNGSOO)
- **WL-064** | PARTIAL | N0WST4NDUP | 엣지 배 연출 배선은 완료(`TerritoryGraphView.cs:24` `_shipPrefabs`, `:43` `_drawEdgeLines=false`, 미배선 시 :175 LogWarning). 잔여: 엣지 표시 규칙을 `IsRevealed`(Owned+Selectable, GDD §4.2)에서 `IsOwned`(Owned 전용)로 좁힌 것이 이슈#93 완료기준④와 갈리는 **보이는 정보 규칙 변경**이라 팀 사인오프 필요 ⚠ **근거 파일이 없다**(2026-08-22 줄참조 감사) — `TerritoryGraphView.cs`가 #337로 삭제돼 배 연출·엣지 표시 규칙 자체가 사라졌다. **아카이브 후보** — 상태 변경은 팀 판정. | 사인오프 후 §4.2를 "배=Owned 전용, 노드 공개=revealed 유지"로 정정
- **WL-065** | OPEN | KIM-SUNGSOO | 정식 타워/투사체 프리팹이 개인 WIP 폴더 `Assets/Personal/SUNGSOO/SweetLand Prefab/`(존재 확인)에 상주하는데 프로덕션 SO(`Resources/ScriptableObjects/Towers/archer_tower.asset` 등)가 런타임에 이를 참조 — CLAUDE.md·SystemMap §4(Personal=WIP 전용) 위반, 폴더 정리 시 프로덕션 참조 깨짐 | 공용 경로(Assets/Prefabs/Tower)로 이관 후 SO 재배선(폴더명 공백 제거 포함)
- **WL-067** | OPEN(전제 정정) | N0WST4NDUP | 전투맵 타일 계약 이원화 — 스킬 조준(MouseManager)은 신맵 `CombatMapTileView.TileType`을, 타워 배치검증(`TowerPlacer.cs:239`)은 `BattleTile.Kind`를 질의한다. **정정: BattleTile 미부착이라는 기존 전제는 틀렸다** — `Assets/Imported/@NorthLand/Prefabs/Tile/`의 GrassTile 4종·ground_cube에 부착돼 있어 배치검증은 동작한다(WL-004 유효). 남은 문제는 두 계약 병존 자체 | TowerPlacer를 CombatMapTileView 질의로 이관해 타일 종류 단일 출처화
- **WL-069** | PARTIAL | muchan918 | 도로 전용 스킬 시전 게이팅은 제거됨(전투 타일이면 종류 무관, 이슈#103의 "전투 바닥으로만 제한"과 일치). 잔여: 전투 타일 한정이라 GDD §5.5 "원하는 위치를 클릭"과는 여전히 차이 | 전투 타일 밖 시전 허용 여부 + §5.6 장판형 보상과의 관계 팀 사인오프
- **WL-071** | PARTIAL | N0WST4NDUP | 상단 자원 HUD 4종 고정 문제는 패널 고정 행 이관으로 완화(탑바 비활성, 보유량은 `ProductionLineView._balanceText` 표시). 잔여: (a) 탑바 HUD 오브젝트가 삭제가 아니라 비활성 상태로 잔존, (b) 특수 자원 4종 초과 시 스크롤/카테고리 UI 부재, (c) 지갑 표기가 Imported ProdRow 배선 의존(WL-040) | 탑바 HUD 삭제 + 동적 자원 목록 UI 방향 논의 후 GDD §4.1·Resources.md 반영
- **WL-073** | OPEN | n0wst4ndup | 우클릭이 카메라 드래그와 배치/조준 취소에 이중 점유 — `CameraController2.MoveDrag`(:156)는 UI 위 여부만 보고 MouseManager 모드를 질의하지 않고, 같은 프레임에 `MouseManager.UpdatePlacement`(:261)·`UpdateSkillTargeting`(:290)이 취소를 수행한다(raw Mouse 폴링 2원화). 조준 중 우드래그 시 취소+카메라 이동 동시 발생. Idle 상태의 선택 해제는 빈 곳 클릭으로만 처리한다 | 카메라 드래그를 MouseManager 상태 질의로 억제 or 카메라 드래그를 중버튼으로 이전
- **WL-074** | OPEN | muchan918(뷰 측 n0wst4ndup) | 자원 카테고리 지식이 데이터가 아니라 뷰에 하드코딩 — `ManagementPanelView.SpecialKinds()`(:189-197)가 `kind != Wood/Iron/Food/Mana`로 특수 자원을 판정하고 `ResourceTable.csv` 헤더는 아직 `ResourceID,NameKey,Kind`(Category 컬럼 없음, `ResourceData.cs:16-21`도 3필드). 신규 기본/통화 자원 추가 시 특수 자원 행으로 조용히 오분류. 표시명 키 `game.resources.{kind소문자}` 조립도 ManagementPanelView:186·TerritoryNodeView:46 + CSV NameKey로 3중 | `ResourceData.Category`(CSV 컬럼) 도입해 뷰가 질의 + 표시명 키 단일 출처화
- **WL-075** | PARTIAL(해소 검증대기) | KIM-SUNGSOO | 투사체 발사가 SO 원값이 아니라 원장 합성값을 넘긴다 — `AttackAction.cs:278` `projectile.Init(target, Damage, …)`이고 `Damage`(`AttackAction.cs:47`)는 `owner.Stats.Evaluate(TowerStat.AttackDamage, fields.AttackDamage)`. `ApplyBuff`의 damageMultiplier가 실데미지에 반영됨. 재발감지: `Init(...)`에 `fields.AttackDamage`/`atk.AttackDamage`를 직접 전달하는 신규 코드 (근거: #164 타워 통합 리팩토링) | ⚠️ 2026-07-30 감사에서 해소로 판정됐으나 독립 반증을 거치지 못했다. 다음 리뷰가 근거를 확인하면 아카이브로 옮긴다
- **WL-076** | PARTIAL | n0wst4ndup | 잔여(b) 해소 — 임시 `TowerWallet`은 `TowerMergeGroup`으로 대체되고 외부 파괴(철거·사망) 정리가 `TowerMergeGroup.Prune(Predicate)`(:63-70) + `TowerMergeCoordinator.HandleActiveChanged`(:233)로 붙어 #183 버튼 stale이 사라졌다. **남은 잔여(a)**: 레시피 카탈로그(전체 열거) 출처가 여전히 뷰 인스펙터 배열 — `TowerMergePanelView.cs:26 [SerializeField] TowerRecipe[] _recipes`를 :63에서 순회하므로 씬마다 누락 가능(#194 완료기준 미충족) | 카탈로그 단일 출처(`Resources.LoadAll<TowerRecipe>` 등) 확정
- **WL-077** | OPEN(축소) | muchan(실행부 소유) | 합성 **실행부의 낮/밤 방어 게이팅** 미추가: `TowerFusionController.TryFuse`에 `DayNightManager` 참조가 0건이다. 완화만 존재 — 코디네이터 `RequestMerge`가 낮에만 통과시키고 `PhasePanelSwitcher.ShowNight`(:47/:55)가 밤 진입 시 진행 중 배치·선택을 취소한다. **선행 조건이던 페이즈 합의는 이미 끝나 있다** — `GDD.md` §5.8이 "합성은 낮 페이즈 전용"으로 확정했고, 이를 미정으로 적던 §8의 낡은 TBD 한 줄은 #263에서 정정했다(그 한 줄 때문에 #263 저자가 이 축을 '설계 미결'로 오인해 미뤘다). **배치 위치 제약은 #263 커맨드 패턴으로 해소** — 소모가 후보 버튼 클릭 시점으로 앞당겨져 재료가 있던 자리에 결과를 놓을 수 있다(GDD §5.8에 규칙 명시) | `TryFuse` 진입에 낮 게이팅 한 줄 추가(defense-in-depth)
- **WL-078** | OPEN | n0wst4ndup | 호버 툴팁 뷰 2원화 유지(범용 TooltipUI vs 타워 전용 TowerTooltipView). 개선: 전용 오버레이 short.MaxValue → 씬 HUD 캔버스 하위 + UILayer.Hud 폴백(:218-254). **악화**: _panel·_panelRect·_icon·텍스트 **5종**이 [SerializeField] 없는 private(:30-39)이라 씬 와이어링이 불가능해 BuildFallback이 항상 실행되고 색·크기가 코드 하드코딩이다(:161-214). #504가 다섯 번째 슬롯 UnlockWave(22f Bold · (1, 0.42, 0.38), :214)를 추가하며 면적이 늘었고, 이번 것은 **의미를 담은 경고색**이라 톤을 조정할 사람이 인스펙터에서 찾을 수 없다. SystemMap §2에 스타일 소유권 항목 없고 §6에도 코드 생성 UI 허용 조건 없음 | 툴팁 뷰 1개 유지 여부 확정 + 슬롯 5종 재직렬화 or 색·폰트·크기·오프셋 SO 외부화 + SystemMap §2/§6 등재
- **WL-080** | PARTIAL | n0wst4ndup·muchan918 | 런 종료 취소가 GDD §5.5 확장 계약(새 효과 = 파생 1개 + 부착, 매니저 무수정)과 어긋남 — `SkillEffect.cs`에 `OnWaveEnd()` 훅이 없고 종료 신호 구독이 **세 곳**에 각각 하드코딩(인스턴스당 OnNightToDay+OnResultDecided 2개): `SkillBomb`·`SkillField`·`SkillDelayedImpact`(#316, #465). 지연·지속 성질의 새 효과를 추가하면 구독 2줄을 복붙해야 하고, 한 번이라도 빠뜨리면 결과 화면 뒤에서 딜이 계속 도는 #200 회귀가 그 효과에만 조용히 생긴다(전기장이 `SkillBomb` 패턴을 복제하며 실제로 재발한 전례가 있다). #319에서 3곳→2곳으로 줄었지만 #465의 별 낙하 지연 착탄 도입으로 다시 3곳이 됐다. 같은 PR이 `SkillEffect.OnLevelChanged()`로 **베이스 훅 + 파생 override** 선례를 만들었다 — 이 항목이 요구하는 해소 형태와 같은 모양이라 복제할 본이 저장소 안에 이미 있다. 부수: 조준 취소는 #391로 해소 — `PhasePanelSwitcher`가 `OnDayStart`에 더해 `OnResultDecided`도 구독해 런 종료(패배·승리) 시에도 고스트를 걷는다. 소유자가 이 클래스 하나로 유지되므로 이 항목이 세는 하드코딩 지점은 늘지 않았다 | `SkillEffect.OnWaveEnd()` 훅 + `SkillEffectManager`가 종료 신호 1회 구독해 브로드캐스트, 조준 취소도 같은 신호에 묶기 — **보상 종류 확장(WL-174)보다 먼저 넣을 것**(확장이 선행하면 복붙 지점이 늘어난다)
- **WL-082** | PARTIAL(영향 확대 #335) | sunjin1222 | 경고는 붙었다 — `CombatMapTileSpawner.GetPrefab`이 프리팹 미할당 시 LogWarning 후 일반 잔디로 폴백해 더 이상 조용하지 않다. **잔여**: 버프 타일 SO(`Assets/Resources/ScriptableObjects/BuffTiles/BT_*.asset`)의 `prefab`이 `Assets/Imported/NorthLand-Imported/@NorthLand/Prefabs/Tile/GrassTile*.prefab`을 가리키며 Imported 동기화에 의존한다. #335에서 같은 프리팹의 `CombatMapTileView.buffIconRoot`·`buffIconRenderer` 자식 배선도 추가되어, Imported 미동기 환경에서는 버프 타일이 일반 잔디로 보이는 동시에 고스트 주변 버프 아이콘도 표시되지 않는다. 스탯 계산은 `BuffTileDefinition.Modifiers`를 읽으므로 외형·아이콘 누락과 별개로 적용될 수 있다 | 전투 타일 프리팹 소유 위치와 NorthLand-Imported 동기화·sparse checkout 계약을 SystemMap §4에 명문화하고, `@NorthLand/Prefabs/Tile/**` 동기화 여부를 검증
- **WL-083** | OPEN | sunjin1222 | 맵 생성 SO `Validate`의 fatal 판정 기준이 미문서화 — 과거 2건(AttackSpeed+Flat 거부로 맵 생성 전면 차단, 버프 타일 `Prefab==null` fatal로 0타일)은 코드로 되돌렸으나(현 `BuffTileSpawnPool.Validate` :43-84에 Prefab 검사 없음) SystemMap §6(:352-378)에 '데이터 오류=fatal, 환경 차이=경고+폴백' 기준이 없다. 검증 테스트도 0본(`Assets/Scripts` 전역 `[Test]`/NUnit 0건)이라 Play 없이는 안 잡힌다 | §6 기준 명문화 + `CombatMapGenerationSettings`/`TileBuffCalculator` EditMode 테스트 1본(팀 최초)
- **WL-084** | OPEN | 미정(전역) | TMP 정본 `Assets/TextMesh Pro/Fonts/PretendardJP SDF.asset`이 여전히 동적 아틀라스 — `m_AtlasPopulationMode: 1`(:107) + `m_ClearDynamicDataOnBuild: 1`(:5190), 1024×1024. 빌드 시 버려지는 데이터인데 `_typelessdata` 1MB 단일 라인이 diff에 실려 동시 편집 시 수동 병합 불가하고, 기능 작업 중 증분 오염이 계속 재발한다 | 정적 아틀라스(정적 서브셋 재-bake) 전환 검토 + '폰트 파일은 의도적 폰트 작업 PR에서만 커밋'을 SystemMap §4에 명문화(현재 §4:331에는 죽은 사본 주석뿐)
- **WL-085** | OPEN | n0wst4ndup/KIM-SUNGSOO | 순수 View 유틸이 소유 시스템 안에 그대로 거주하며 **사례가 3개로 늘었다** — `Assets/Scripts/View/`는 없고 `OutlineHighlight`는 `GameManager/MouseManager/Highlight/`, `RangeCircle`은 `CombatSystem/`, `TowerSpawnEffect`(#264)는 `CombatSystem/Vfx/`. 셋 다 소유 시스템을 모르는 범용 View인데 소유 시스템 폴더에 산다 — 특히 `TowerSpawnEffect`는 **타워를 모르는** 등장 연출인데 Combat(SUNGSOO) 소유 폴더에 있고 소비처는 `TowerPlacer`와 #265다. 실측 소비처: 아웃라인 3곳(TowerGroupSelectable·TowerMergeCoordinator·OutlineInteractionDriver, 전부 MouseManager 하위) / RangeCircle 3곳(Tower·TowerPlacer·EnemyShowTelegraphCircleAction) / TowerSpawnEffect 1곳(TowerPlacer) — 뒤 둘은 시스템 경계를 넘는다. **#265로 `CombatSystem/Vfx/`가 4개 파일이 됐다**(`TowerSpawnEffect`·`TowerDissolveEffect`·`GrainSwarm`·`VfxScaleHold`) — 뒤 셋도 전부 도메인 중립이다. **#281이 압력을 더 키웠다**: `TowerMergeDissolveEffect`가 `TowerDissolveEffect`로 중립화되면서 이름에서도 도메인이 빠졌고, 소비처가 `TowerFusionController` 하나에서 **셋**(+`TowerPlaceCommand`·`TowerMergeCommand`)으로 늘었는데 전부 `GameManager/.../TowerPlacement` 아래라 시스템 경계를 넘는다. 참고로 #281은 같은 판단을 커맨드 축에서는 **실행했다** — `IReversibleCommand`가 두 번째 구현체가 생긴 시점에 `Assets/Scripts/Command/`로 옮겨졌다. Vfx 쪽도 같은 기준(중립 위치 신설 후 일괄 이전)이 그대로 적용된다. 이전 규모가 커진 만큼 한 번에 옮기는 이점도 커졌다. **#276 주민이 소비처를 공간 밖으로 넘겼다** — `ResidentDebugView.cs:2,267`이 `using NorthLand.Combat;`으로 `RangeCircle`을 쓰면서 **경영 공간 코드가 전투 시스템을 컴파일 타임 참조**한다(RangeCircle 소비처 4곳째, 첫 공간 교차). 재사용 판단 자체는 옳다(신규 에셋 0개, URP PC/Mobile 검증된 셰이더) — 문제는 이전 시 갱신 대상이 두 공간에 걸쳐 착수 합의가 더 필요해졌다는 것이다. `TerritoryNodeView`는 아웃라인을 쓰지 않으므로 기존 기록의 4번째 소비처는 사실이 아니다. `TowerSpawnEffect`·`RangeCircle`은 SystemMap §2 등재 완료(#264), `OutlineHighlight`는 여전히 산문 언급만. 이징 헬퍼 중복은 절반만 정리됐다: back-out은 #265에서 `VfxScaleHold`로 모여 두 타워 연출이 공유하지만, `TerritoryNodeStateVisual.EaseOutBack`은 그대로라 여전히 같은 곡선이 두 벌이다 — 중립 위치를 만들 때 함께 흡수할 후보 | 중립 위치(`Assets/Scripts/View/` 등) 신설 후 3개 동시 이전(하나씩 옮기면 `.meta`·참조 리스크를 세 번 진다) + `OutlineHighlight` §2 등재
- **WL-086** | PARTIAL | n0wst4ndup | 배치·페이즈 표면은 해소 — `ClearSelection()`이 유일한 해제 창구로 승격돼 빈 곳 클릭·`BeginPlacement`(:116)·`PhasePanelSwitcher.ShowNight`(:55)가 공유하고, 단일+그룹을 함께 비워 사거리 원·아웃라인·인포·합성 패널이 같이 내려간다. **잔여**: `BeginSkillTargeting`(:133-141)은 여전히 `ClearHover()`만 호출해 스킬 조준 진입 시 이전 타워의 초록 사거리 원·정보 패널이 잔존 | `BeginSkillTargeting`에 `ClearSelection()` 한 줄 추가
- **WL-089** | OPEN | sunjin1222 | 본진 보호 밴드가 침식 전용 — 판정이 `GrassEroder.IsNearBase`(:17-21) private에 머물고 `CombatMapData.IsInBaseProtection()`이 없다. Water는 이를 모른 채 `IsNearRoad(WaterRoadClearance)`만 제외(`WaterGenerator.cs:142`)해 본진 맨해튼 5 이내 잔디가 Water 후보로 남는다(#189 완료기준② 시드 의존 미달, Water 타일 배치 불가로 본진 앞 방어 면적이 흔들림). **2026-08-13 회의 결정으로 `WaterRoadClearance = 0`을 유지하며 도로에 Water가 직접 인접하는 것을 허용한다.** 이는 도로 완충을 의도적으로 비활성화한 것이며 본진 보호 밴드 문제의 해소로 보지 않는다. `RunStabilityTest`(`CombatMapGenerator.cs:455-`)에도 밴드 카운터가 없어 수정 확인 수단이 없다 | 판정을 `CombatMapData.IsInBaseProtection()`으로 승격해 Water/버프타일이 공유 + '밴드 내 non-Grass/non-Road == 0' 카운터 추가(한 이슈로 묶을 것)
- **WL-091** | OPEN | SUNJIN | 리뷰 기준선에 `CombatSpace/Map`이 없다: SystemMap §1(`SystemMap.md:14`)은 구 `CombatSpace/MapBuilder`(7×7 블록)만 등재하고, 정본이 실제로 돌리는 `CombatMapGenerator`·`CombatMapData`·`CombatMapTileSpawner.CurrentWorldEnemyRoute`·`CombatMapRevealController`는 §1·§2 어디에도 없다(Docs 전체 grep: WatchList·MonsterMovement.md에만 등장). MonsterSpawn·MouseManager·TowerPlacer(`TowerPlacer.cs:102` 신맵 질의)가 이미 소비 중이라 자동/수동 리뷰가 접점을 못 짚는다(WL-003 잔여) | §1에 `CombatSpace/Map(절차적 전투맵 생성, SUNJIN)` 행 신설 + §2에 위 4개 공개 API 등재
- **WL-092** | PARTIAL | 미정 | `EditorBuildSettings.asset:28`의 App UI config object 한 줄(guid `99f9c949…`)이 PR#218 재등록 이후 미원복 상태다. 단 `com.unity.dt.app-ui` 2.0.0이 `com.unity.behavior` 1.0.16의 전이 의존으로 해석되므로(`manifest.json:6`, `packages-lock.json:48`) 참조는 PackageCache에서 풀리고 dangling 실패 모드 자체는 닫혔다. 잔여: 미원복 한 줄 + 에디터가 자동 기록하는 전역 ProjectSettings 필드를 커밋 전에 걸러내는 규율 부재(WL-084 churn 축) | 한 줄 원복하거나 '정상 참조'로 문서화해 종결
- **WL-094** | PARTIAL | n0wst4ndup(그래프)·muchan(SO 필드) | 신규 그래프는 수치를 Blackboard로 승격했으나(`TankBossBehavior.asset` 변수 57개) 중간보스 `Assets/Scripts/CombatSystem/Enemy/MiniBoss/MidBossBehavior.asset`은 노드 인라인 authoring 그대로다(블랙보드에 Self(GameObject) 계열만, 수치는 노드 입력 `BlackboardVariable<Single>` — `:426`,`:521`). 밸런싱 수치가 CSV/SO 밖 그래프 에셋에 남는 축(WL-015/027) 유지. 또 사인오프 앵커였던 GDD §9가 없다 — 현 GDD 최상위 절은 §8까지(`GDD.md:268`) | MidBoss 그래프 수치 Blackboard 승격 + 보스·스테이지 구성 절을 GDD에 재확립
- **WL-095** | PARTIAL | n0wst4ndup | 데이터 주도 보스 BT 주입(`Enemy.cs:89-97`: `AddComponent<BehaviorGraphAgent>` → `Graph` 대입)의 Play 실증이 `Tank` 쪽에서만 확보됐다 — `Tank.prefab`은 에이전트 미배선(`:335` Enemy·`:367` EnemyAgent만)이라 이 경로가 유일 주입인데 SystemMap §1(`:25`)이 '패턴 4종 Play 동작 확인'을 기록 → 무증상 무동작 리스크는 닫힘. 잔여: MidBoss(`MidBossBehavior.asset`) 경로 Play 미확인 + 플레이 종료 후 그래프 에셋 non-dirty 확인이 기록되지 않음 | MidBoss 웨이브 Play 1회 + 에셋 non-dirty 확인을 PR 테스트 절에 기록
- **WL-096** | PARTIAL | SUNJIN | 웨이브 SO 이관은 끝났다(7본+`BossTest`가 `Assets/Resources/ScriptableObjects/Wave/`, 개인 폴더 소멸 — WL-045/065 축 1건 감소). **경과(#294)**: 진행 순서가 `MonsterWaveAsset.waveNumber` 매칭에서 `MonsterSpawnWaveProvider.waves` 리스트 등록 순서로 바뀌어, 최종 웨이브가 "리스트 마지막 항목"으로 자동 결정된다(`MonsterSpawnWaveProvider.cs:94` `FinalWaveNumber = orderedWaves.Count`) — **코드 축은 닫혔고 남은 건 순수 데이터 편성 결정**이다. 잔여: 중간보스 `Candy_King_01`이 아직 리스트 마지막 항목에 `count:1`로 편성돼(`MonsterWave 7.asset:30-31`) `WaveCompletionCoordinator.cs:60`이 보상 3택1·`EndNight`를 건너뛰고 `TriggerVictory`를 부른다 — 중간보스 처치 보상이 구조적으로 붙을 자리가 없다. 최종 보스 `Tank`는 `BossTest.asset`(테스트 씬 `GameScene_501`만 등록)뿐이고 정본 웨이브 미편성 | 중간보스 웨이브를 리스트 중간으로 옮기거나 최종 보스 웨이브를 리스트 끝에 추가 — SUNJIN 사인오프
- **WL-098** | OPEN(진전, PR#257) | PR#217 | (기존 내용 유지) **경과(PR#257)**: `SettingUI`가 `GameSpeedController.Instance`의 **첫 소비자**가 되어 (a)의 "`Instance` 소비자 0건"이 해소되고 SerializeField 소비처가 2→1(`Reward/WaveRewardSelectionUI.cs:38`)로 줄었다 — `Instance` 경유 규약 확정의 실증 선례. 잔여: `OnSpeedChanged`/"입력 차단 여부" 질의 프로퍼티, `WaveRewardSelectionUI`·`CameraController2.cs:50`(SettingUI 직접 참조) 전환, SystemMap §1/§2 등재
- **WL-100** | OPEN | muchan(경영) | 배속(x1/x2/x4) 적용 범위 결정이 GDD에 없다 — `Docs/GDD.md` 전체에 '배속'·`timeScale`·#190 언급 0건. 배속은 전역 `Time.timeScale`(`GameSpeedController.cs:176`)이라 낮(경영) 연출까지 스케일된다. **남은 위험은 하나로 좁혀졌다**: GDD §4 경영 페이즈에 시간 기반 생산이 붙는 순간 배속이 곧 생산 배율이 된다(현재 생산은 밤→낮 정산이라 프레임 시간과 무관하다). **연출 축은 #138에서 판단 기준이 정리됐다 — "월드의 일부인가"로 가른다**: 월드 배경(열기구 비행 `BalloonFlight`, 그 불꽃 파티클, `TerritoryEdgeShip`, `VortexVisual`, `CombatMapTileSpawner`)은 scaled로 두어 배속·일시정지를 함께 따르고, 플레이어에게 주는 안내·피드백(건물 업그레이드 연출·줌 힌트 파티클 #138, `TowerSpawnEffect`·`TowerDissolveEffect` #264/#265)은 unscaled로 둔다 — 후자는 배속에서 소실되거나 일시정지 중 얼어붙으면 "느려진다"가 아니라 **정보가 전달되지 않는다**가 된다. 기준은 `SystemMap.md` §6에 등재했고, 팀 규칙으로의 승격(및 경영 생산 타이밍 규약)만 남았다 | 이슈 #190 결론을 GDD §4에 명시 + 위 기준을 팀 규칙으로 승격(WL-119와 세트) + **시간 기반 생산은 프레임 시간이 아닌 페이즈 이벤트/틱 카운트로 규약화** — 경영 담당 사인오프
- **WL-103** | OPEN(유지 결정) | n0wst4ndup | `Assets/Scripts/Editor/OutlineSmoothMeshBaker.cs:159`가 벤더 네임스페이스 `FlatKit.MeshSmoother.SmoothNormals`를 컴파일 타임 직접 참조 — `.gitignore`가 `Assets/Imported/`를 제외하므로 벤더 트리 미동기 환경에서 Assembly-CSharp-Editor 컴파일 실패 → Unity가 Play 진입을 차단한다(산출물이 커밋돼 있어도 도달 불가). 유지 근거는 `InteractionOutline.md:112-119` §2.5. 해소 경로(`Assets/Imported/@NorthLand` 이동)는 1급 툴을 비추적 저장소로 내보내 WL-045/065/082 축을 새로 만든다 ⚠ **전제가 사라졌다**(2026-08-22 줄참조 감사) — `Assets/Scripts/Editor/OutlineSmoothMeshBaker.cs`가 삭제되고 `Assets/Scripts` 전체에 `MeshSmoother` 참조가 **0건**이라, 벤더 트리 미동기 환경에서 컴파일이 실패하는 경로가 지금은 없다. **해소 후보** — 상태 변경은 팀 판정. | 이동까지 리플렉션 덕타이핑(`TerritoryNodeStateVisual.cs:200-207` 선례)으로 임시 차단, #148에서 종결
- **WL-105** | OPEN | n0wst4ndup | `TowerPlacer.keepPlacing`(`:69`)이 일반 배치와 합성 결과 배치가 공유하는 단일 인스펙터 필드로 남아 있다 — `:275`가 그대로 `KeepPlacingAfterConfirm`에 실어 보내고 `BeginTowerPlacement`(`:207`)에 연속 배치 인자가 없다(합성도 이 오버로드 사용, `TowerFusionController.cs:117`). true면 `_onConfirmed`가 확정 시 1회 소비되므로(`:420-421`) 재료는 한 번만 소모되고 이후 클릭은 `ExtraCost`만으로 결과 타워를 복제한다. #263 커맨드의 상태 기계는 이 값과 무관하게 안전하다(확정 후 `Committed` 가드가 뒤이은 `Undo`를 무시하므로 재료는 한 번만 소모·파괴된다). #281이 얹은 두 번째 축(되돌리기 소유권 `_historyOwner`가 세션 안에 남아 복제분이 히스토리에 안 오르는 문제)은 **해소됐다** — `Push` 판정 직후 `Placer`로 강등한다(`:434`, `EndPlacement`(`:564`)에도 대칭 리셋). 정본 `GameScene.unity:12908`을 포함해 모든 씬이 `keepPlacing: 0`이라 현재는 잠복 | `BeginTowerPlacement`에 연속 배치 인자를 받아 합성 경로는 항상 false 고정(한 줄)
- **WL-106** | OPEN(관찰, 유지 결정) | n0wst4ndup | `MouseManager.OnPrimarySelect` 선언 주석은 평클릭·빈 곳 클릭(사용자 입력)을 규정하나 `ClearSelection()`이 모드·페이즈 전환까지 같은 채널에 `null`로 태운다. 구독자가 `TowerMergeCoordinator` 1곳이라 무해 — 분리 판단 시점은 3번째 구독자 합류(WL-085 패턴). 호출자 수와 위치는 코드 변경 시 다시 확인해야 한다 | 지금 쪼개지 말 것 — 3번째 구독자 시점에 `OnSelectionCleared` 분리 결정
- **WL-109** | OPEN | KIM-SUNGSOO | `Assets/Personal/SUNGSOO/Prefabs/ResultUI.prefab`의 authored `m_LocalScale: 0` — 지금은 씬의 scale=1 오버라이드 1건만이 결과 UI 가시성을 지탱한다. Revert하면 게임오버 화면이 접혀 사라지고 씬 diff에는 정상값으로 보여 리뷰로도 안 잡힌다 | 프리팹 authored 스케일 1 정정 + 루트 Canvas/CanvasScaler/GraphicRaycaster 제거해 순수 패널화(WL-065 축과 동반)
- **WL-111** | OPEN | muchan918 | 주민 증가 게이트웨이의 index 도메인 불일치 — `TryIncreaseVillagers(BuildingAsset)`는 건물을 인자로 받지만 진행도는 전역 단일 `_bonusVillagers`(`ManagementController.cs:73`), 같은 컨트롤러의 업그레이드 트랙은 `UpgradeIndexOf`(`:281`)로 per-building 상태를 유지한다. 주민 증가 데이터를 가진 두 번째 건물이 생기면 조용히 어긋난다 | 파라미터 제거(전역 명시) 또는 asset→index 매핑 도입 중 택1
- **WL-112** | OPEN | muchan918 | 주민 상한 페이싱이 영토 시스템에 하드 종속 — `castle.asset` 6~8회차 비용이 금/루비/다이아 전용(기본 자원 0)이라 주민 6명 이상이 해당 영지 확보에 전적으로 의존하고, 확장은 하루 1회 + 프론티어 인접성이라 진입 시점이 영토 RNG에 좌우된다. GDD §5.1에 "영토 확장 사실상 필수"로 명문화됨 | 후반 회차 기본 자원 혼합 여부 + §5.3 페이싱을 muchan·n0wst4ndup 합의로 확정
- **WL-113** | PARTIAL | sunjin1222 | 프리팹 자기배선으로 정본 이관 시 수동 오버라이드가 GameScene 3건·TitleScene 2건으로 축소됨. 잔여: 프리팹이 `Assets/Personal/SUNJIN/Prefabs/` 상주(WL-045/065/096과 같은 축) | 공용 경로로 이관
- **WL-114** | OPEN | N0WST4NDUP | 설정창 키보드 토글 부재 — 이슈 #221 완료기준①과 PR 본문은 ESC 표기인데 미구현이고 `Closes #221`로 자동 종료 예정. `Docs/Core/UIZOrder.md`가 "키보드 토글은 중앙 입력 Cancel 우선순위 확정 후"로 보류 근거만 남김 | Cancel 액션 우선순위 확정 후 토글 구현 또는 #221 완료기준에서 분리
- **WL-115** | OPEN | N0WST4NDUP | `GameSceneManager` 위임 래퍼가 6중 복제(`SettingUI.cs:77-115`, `ResultUIManager.cs:64-83,95-104`) — 씬 전환 API가 여러 UI에 산재해 변경 시 동시 수정 강제 | 씬 전환 호출을 GameSceneManager 직접 호출로 단일화하거나 공용 헬퍼로 추출
- **WL-117** | OPEN | N0WST4NDUP | 로컬라이제이션 키 중복·오타 실측 — `game.setting.close`/`.replay`/`.title`이 기존 `game.btn.close`/`.retry`/`.title`과 중복(title은 문구까지 갈림)이고 `game.setting.Laguage`는 오타이자 유일한 대문자 시작 키. String Table 4로케일+Shared Data는 팀 전역 공유 자원 | 설정창 버튼을 기존 `game.btn.*`에 연결하고 중복 3개 제거, `game.setting.language`로 정정
- **WL-119** | OPEN | KIM-SUNGSOO | 보스 BT 노드 타이밍이 전부 스케일드 타임이라 `GameSpeedController` x4 배속에서 P3 예고 `Duration`(배포값 0.5초)이 0.125초가 되어 인지 불가능해진다. `TankGraphSpec.md`가 예고 원 드리프트 대응을 이 짧은 Duration에 의존시켜 "배속을 못 쓰거나 예고가 안 보이거나" 택일이 된다. BT 내장 `Wait`도 같은 성질이라 노드만 고쳐선 안 됨(WL-099와 같은 배속 정책 축) | 배속 정책 확정 — 예고·준비 모션만 unscaled로 분리할지 GameSpeedController 소유자 사인오프
- **WL-120** | OPEN(관찰) | KIM-SUNGSOO | 보스 BT 리프 노드 14종 + 보조 타입 4종이 전역 네임스페이스 점유(`EnemyAgent`/`EnemyNodeQuery`/`EnemyPatternMemory` 등). 같은 폴더 정본은 `NorthLand.Combat`, 기존 MiniBoss 노드 4종은 `NorthLand.Combat.Boss`라 자발적 규약 위반이고, asmdef 없이 전부 Assembly-CSharp이라 이름 충돌이 타 브랜치 머지 시점에 터진다 | 보스 2호 착수 전 `NorthLand.Combat.AI` 이관 여부 결정(노드 수가 늘수록 비용 증가)
- **WL-121** | OPEN | KIM-SUNGSOO | `IDamageable.HitPosition`(`IDamageable.cs:7`)이 non-null을 명문화하지 않은 채 Combat의 대상 좌표 질의 단일 창구로 승격됐고, `DummyDamageable.cs:14`가 아직 `throw new NotImplementedException()`이다. 이 더미는 Awake에서 자기 레이어를 Enemy로 지정해 Chain의 `FindNearestUnhit` 사냥감이 되며, `lightning_tower.asset`이 TowerType=Chain이라 값 기입만 남은 예약 경로다 | 스텁을 `=> transform`으로 교체 + IDamageable.cs:7에 "null 반환 금지" 계약 주석 + SystemMap §2 IDamageable 행에 HitPosition 등재
- **WL-122** | OPEN | KIM-SUNGSOO/muchan918 | 범위 판정 원점이 피벗→몸통(`HitPosition`)으로 바뀌면서 밸런싱 결과값이 **버전 관리 밖(Assets/Imported) 프리팹의 자식 Transform 로컬 Y**에 좌우된다 — `Projectile.cs:137`이 조준점을 그대로 `ApplyArea` OverlapSphere 중심으로 넘기고 몬스터는 6f 부양(WL-063), hitPosition 로컬 Y는 프리팹마다 다르다. WL-015/027(수치 소유권)이 프리팹 계층까지 번진 표면 | 착탄점과 범위 판정 원점 분리 또는 몸통 높이를 EnemyAsset authored 값으로 승격 중 택1 — SplashRadius/ChainRadius 값 기입 전에 합의
- **WL-123** | OPEN(미검증) | KIM-SUNGSOO | P1 돌진의 "경로 끝 파괴 회피"가 코드 계약이 아니라 수치 부등식(`P1_ArriveDistance 5 > AttackRange 3`)에만 의존한다. 돌진 중엔 `MovementOwnedByBehavior`가 Enemy.Update의 정지를 건너뛰므로 마지막 웨이포인트를 소비하면 `RouteCompleted`→`Destroy`로 **보스가 피해도 없이 소멸**한다. AttackRange 튜닝·성문 콜라이더 변경·x4 배속 어느 쪽으로도 부등식이 깨진다 | ① Play로 통과 여부 확인 ② 깨지면 `Enemy.HandleRouteCompleted`에 BT 소유권/보스 예외(마지막 웨이포인트 클램프) — Enemy 공유 파일이라 사인오프 필요
- **WL-125** | OPEN | sunjin1222 | 스폰 구성 검증 실패가 "웨이브 조용한 자동 성공"으로 귀결 — `MonsterSpawn.cs:349-377`의 세 검증이 모두 `Instantiate` 이후 `Destroy`라 한 웨이브가 전부 걸리면 `childCount == 0`으로 `WaveCleared`가 정상 발화한다. 판정 축이 컴포넌트 누락에서 `EnemyAsset.MovementMode` 값으로 넓어져 실수 확률이 올랐고, 지연 파괴 동안 AliveMonsterCount에 유령으로 잡힌다 | 검증을 Instantiate 전 프리팹 에셋 단계로 이관 + 스폰 시도 수 ≠ 투입 수일 때 클리어를 막는 카운터(WL-037 축)
- **WL-126** | OPEN | muchan918 | `MovementMode.Ground = 0`(`EnemyData.cs:7-11`, 경고 주석 없음)이 하중 값이 됐다 — 기존 SO 7종이 MovementMode 키를 직렬화하지 않아 "전부 지상"이 열거형 0번 슬롯에만 의존하는데, PR#241이 이 값을 스폰 게이트(`MonsterSpawn.cs:361`)로 승격시켰다. 모드를 앞에 삽입·재배열하면 7종이 Flying으로 읽혀 전 몬스터가 스폰 즉시 파괴되고 전 웨이브가 즉시 성공한다(컴파일 통과 + SO diff 0줄) | 열거형에 "명시 값 변경 금지·신규는 뒤에만 추가" 주석 + 기존 SO 필드 실체화 또는 TableImporter가 기본값 명시 기록
- **WL-127** | PARTIAL | KIM-SUNGSOO | 감속 강도에 원장 축이 없다 — `TowerStat`은 AttackDamage/AttackRange/AttackSpeed 3축뿐이라(`TowerStats.cs:6-11`) 순수 감속 타워(`choco_tower`)는 타일 버프에서 사거리만 이득을 보고 DoT 타워는 3축 전부를 받는다. 감속 중첩은 곱산(`MoveSpeedComposer.cs:68`)인데 타워 버프는 합산(`TowerStats.cs:17`)인 비대칭도 미확정. 데이터·환산 공유(`AuraModifiers`)·소스키 인스턴스화·로컬라이제이션 키 정리는 완료 | "CC 강도" 축 신설(GDD §7 CC 사인오프, WL-026 동반) + 곱산/합산 확정 + 플레이 모드 감속 실측
- **WL-132** | OPEN | N0WST4NDUP | `EndDayConfirmPopup.prefab`이 gitignore된 `Assets/Imported/@NorthLand/Prefabs/UI/`에만 있어(`.gitignore:162`, `git ls-files Assets/Imported`=0건) 메인 저장소만 받은 환경에서 `GameScene.unity:4355`의 GUID `d53307a6…`가 Missing Prefab이 되고 뷰의 `_endDayConfirmPopup`이 null이 된다. `Assets/Prefabs/UI/`에는 GameSpeed만 있다(EnemyStateUI는 #447로 `Assets/Resources/UI/`로 이관) | 팝업 프리팹을 `Assets/Prefabs/UI/`로 이전해 `.meta`와 함께 커밋(벤더 에셋 아님) + 정본 씬에서 참조 해결 확인
- **WL-133** | OPEN | muchan918 | 코드는 배치 유지(`ManagementController.cs:858` "초기화하지 않고 전날 배치를 그대로 유지")인데 문서는 매일 초기화를 계약으로 유지 — `GDD.md:115`·`:152`, `SystemMap.md:286`·`:323`(팀 계약 #5), `Docs/Core/DayNightManager.md:53·106·117`. `SystemMap.md:121`은 개명된 `RequestAdvancePhase()`를 계속 등재한다(Resources.md만 갱신됨) | GDD 조항 + 팀 계약 #5·§2 API + DayNightManager.md 개정, §9에 "주민 배분 전략의 새 축" 결정 기록
- **WL-134** | OPEN | N0WST4NDUP | 확인 다이얼로그가 도메인 클래스에 갇혀 있다 — 범용 ConfirmPopup 없음, ManagementEndDayConfirmPopup 183줄 중 특화는 BuildWarnings(:122)뿐이고 그 안의 조건도 #410으로 HasIdleVillagers 하나만 남았다(#337에서 영토, #410에서 타워 분기 삭제). 같은 이유로 NorthLand.Combat 참조가 사라져 추출 시 딸려오는 의존이 0이다. 진입점 Request가 인스턴스 메서드(:95)라 폴백이 캡슐화되지 않아 NightActionPanelView.cs:89·ManagementPanelView.cs:97이 호출 시점 null 가드 없이 역참조하고(가드는 Start의 LogError뿐 — :34/:61), ManagementController.EndDay()는 여전히 public(:938)이라 확인 우회 경로가 열려 있다 | 범용 ConfirmPopup.Show(message,onProceed,onCancel) + 정적 Request(controller) 폴백 캡슐화. WL-078·WL-002와 묶어 결정
- **WL-135** | OPEN | N0WST4NDUP·muchan918 | 로케일 변경 시 UI 갱신 경로 미확립 — `SystemMap.md:52`는 `LocalizeStringEvent`/`StringChanged`를 지시하나 코드 구독(`SelectedLocaleChanged`)이 세 번째 경로로 열려 병존한다. 수동 구독 사본은 `BuildingInfoUI`·`WaveRewardSelectionUI`·PR#489의 `TutorialController`까지 **3개**로 늘었다. `TutorialController`는 단계 SO가 가진 동적 키를 표시하므로 정적 `LocalizeStringEvent`로 대체할 대상이 없고, 현재 팝업·말풍선만 재조회하는 별도 형태다. 갱신 없이 남은 잔존은 **7곳**: StorePanelUI·CastlePanelUI·EndDayConfirmPopup·TerritoryNodeView·TowerInfoUI·TowerMergePanelView·`TowerUndoButtonView`. 뷰마다 `+=`/`-=`를 복제하면 정적 이벤트 해제 누락이 남는다(WL-033). **#445로 노출 면적이 넓어졌다** — `TowerMergePanelView.BuildCandidates`가 `Awake` 1회에 후보 버튼 이름을 확정해 언어 변경 후 이전 언어로 남지만, 툴팁과 정보 패널 후보 칸은 매 표시마다 재조회해 이미 강하다 | 갱신 트리거 소유 확정(`Assets/Scripts/Localization` 공용 진입점 권장) → SystemMap §2 명문화 + 수동 구독 3곳 및 잔존 7곳 일괄 적용. 공용 진입점은 정적 문자열 재바인딩과 튜토리얼의 동적 키 재조회를 모두 수용해야 하며, `TowerMergePanelView`에는 라벨만 다시 채우는 진입점이 필요하다
- **WL-136** | OPEN | muchan918 | `ExchangeUpgradeLevel`이 `UpgradeStep`을 계속 상속해(`BuildingAsset.cs:356`) '도달 비용·게이트' 의미를 물려받지만 실제 의미는 '본진 몇 레벨부터 이 배율'이다. 그래서 예외 3곳이 유지된다 — `UpgradeSteps` 4순위 후보(:38, `_upgradeBuildings` 오배선 시 `Cost:[]` 무료 레벨업), `ValidateUpgradeCosts`의 `label=="Exchange"` 경고 차단(:101), `ExchangeGainAmount`의 '마지막 만족 행' 규칙(`ManagementController.cs:532-536`). `FromCastleLevel`은 0건 | 상속 분리 + 자기 필드(`FromCastleLevel`) 도입으로 예외 3곳 동시 제거
- **WL-137** | OPEN | muchan918 | 본진 요구 레벨 범위 초과 가드가 교환 건물을 못 본다 — `WarnUnreachableCastleRequirements`(`ManagementController.cs:688-701`)가 `_upgradeBuildingRefs`·`_lineBuildings`만 순회하고 `WarnIfUnreachable` 호출부도 이 2곳뿐이라, 어느 배열에도 없는 `alchemist_house`의 배율 행은 본진 최대 레벨을 넘겨도 에디터·Play 모두 침묵한다(`ExchangeGainAmount`는 :534에서 조용히 continue, SO 측 `OnValidate`는 본진 테이블을 모름 — :686 주석) | `StorePanelUI.Show`/`ExchangeGainAmount` 첫 진입에서 건물별 1회 경고, 또는 `Resources.LoadAll<BuildingAsset>` 전수 검사
- **WL-138** | PARTIAL | N0WST4NDUP/muchan918 | 타워 카탈로그가 여전히 씬 인스펙터 리스트 단일 경로다(`TowerSelectPanelView.cs:23` `[SerializeField] List<TowerAsset>`, `Resources.LoadAll<TowerAsset>` 0건) — 씬 병합에서 "통째 채택"이 일어나면 등록 행 소실이 diff에 드러나지 않고 재발한다. 현재 `GameScene.unity:12847-12855`는 8행(soda·choco 포함) 정상이나, SO 9종 중 `lightning_tower`는 미등재 상태다 | 병합 절차에 `git diff origin/main...HEAD -- Assets/Scenes/GameScene.unity` 확인 고정 + 카탈로그를 카탈로그 SO/`Resources.LoadAll`로 이전
- **WL-141** | PARTIAL | KIM-SUNGSOO | 상태효과 소스키 도메인 분기는 유지 — 오라는 인스턴스별(`DebuffAuraAction.cs:83` `GetInstanceID()`)인데 명중 스턴은 `HitEffect.cs:169`의 static `SharedEffectId`("onhit.stun" 해시) 공유라 대상당 스턴 슬롯이 하나뿐이다. 감속·DoT는 중첩되는 비대칭. 다만 **"2기째 CC 기여 0" 증상은 #441로 해소됐다** — 면역 창 0.4 · 간격 2.2에서 1기 가동률 `0.7/2.2`≈31.8%가 천장 `0.7/(0.7+0.4)`≈63.6%보다 낮아 2기째가 실제로 CC를 늘린다(3기째부터 0). 인스턴스화 자체가 안전하다는 것도 확인됐다 — `CanStunNow()`(StatusEffectHandler.cs:170) 게이트가 `slows` 딕셔너리 조회보다 앞에 있고 `stunActive`·`stunImmuneUntil` 둘 다 **대상(핸들러) 인스턴스 필드**라, 소스키를 타워별로 채번해도 대상당 스턴은 하나이고 천장도 유지된다 | 스턴 소스키 인스턴스화 여부 결정 — 구조적 장애물은 없고 밸런스 변화(1기 가동률이 대수에 비례해 오르다 천장에서 멈추는 형태)만 남았다
- **WL-142** | OPEN | N0WST4NDUP | 결과 확정 후 미니맵 카메라 이동이 차단되지 않는다 — 결과 가드가 `CameraController2.Update()`(`:123`)에만 있고 미니맵 `SmoothDamp`는 `LateUpdate()`(`:87-95`, `Time.unscaledDeltaTime`)에서 계속 돌아 `timeScale = 0`과 무관하다. 공개 진입점 `MoveViewCenterTo`(`:250`)도 결과와 무관하게 호출 가능. 이슈 #253 기대 결과 미충족인데 `Closes #253`으로 닫혔다. 부수: `:123`의 `GameManager.Instance` 무가드 역참조, 미사용 `[SerializeField] GameResult gameResult`(`:53`) 씬 dead 직렬화 | 결과 가드를 리워드/설정 분기와 통합(`_isDragging=false` + 미니맵 이동 취소) + 진입부 차단, 근본적으로 차단 사유 3갈래를 `GameSpeedController` 단일 질의로 통합(WL-098과 세트)
- **WL-145** | OPEN → **#371로 이관** | n0wst4ndup | MouseManager 소비처 코디네이터 보일러플레이트 — **세 번째 소비처가 붙어 해소 조건의 시점을 지났다**(#341 `ResidentDragCoordinator`). 자가 부팅 + 참조 백오프(`:145-153`) + `IsDay` 게이팅 + 씬 로드 정리 + 재구독 플래그가 세 벌이 됐다. 세 번째는 구독 이벤트가 다르지만(사각형이 아니라 유닛 끌기) **부팅·참조·페이즈 축은 문장 단위로 동일**하다. **divergence가 두 번째로 발생했다** — `ResidentDragCoordinator.TrySubscribe`에 형제의 진입부 `if (_subscribed) return;` 가드가 없었고 `OnDestroy` 주석이 `DayNightManager`를 `DontDestroyOnLoad`로 잘못 적었다(실제로는 씬 싱글톤). 둘 다 #341 리뷰에서 교정해 **현재 세 코디네이터의 동작은 일치한다.** 아래는 두 번째 소비처(#302) 때의 기록이다 — 드래그 선택(#261) 소비처 보일러플레이트 — **두 번째 소비처가 실제로 붙었다**(#302 `ResidentSelectionCoordinator`). 한 소비처가 구독 5종 + 기준 집합 스냅샷(`_dragBase`) + 유예 플래그(`_boxDragging`) + `IsDay` 게이팅 + 밤 전환 정리 + 마커→도메인 캐스팅을 전부 인라인으로 갖춰야 하고, 그 6종이 `TowerMergeCoordinator`(`:144-157`)와 `ResidentSelectionCoordinator`(`:151-155`)에 두 벌로 있다. **예고한 "미묘하게 다르게 구현하면 재발한다"가 실제로 발생했다** — 주민 쪽이 처음에 `OnPrimarySelect`가 아니라 deduped `OnSelectionChanged`를 구독해 빈 곳 클릭 해제가 조기 반환에 삼켜졌고(드래그가 `Select(null)`을 먼저 하므로 `_selected == null`), `OnDayToNight` 정리가 빠져 선택이 밤을 넘어 아침에 초록을 달고 등장했다. 둘 다 #302에서 선례와 동일하게 교정해 **현재 두 코디네이터의 동작은 일치한다** — 남은 것은 재발 방지용 추출뿐이다. **세 번째 사례는 성격이 다르다**: 재리뷰에서 "밤 전환 정리가 단일 클릭 초록을 놓친다"가 나왔는데(주민은 밤에 비활성돼 아침에 유령 초록으로 발현) 이건 divergence가 아니라 **양쪽에 다 없는 공통 공백이고, 타워 쪽에서는 그게 정답이다** — 타워 단일 선택은 밤에도 유지되는 것이 설계(사거리 원·정보 패널 정합)라 `TowerMergeCoordinator`는 그룹만 리셋하는 게 맞다. 그래서 추출할 "밤 정리"는 공통 코드로 뭉칠 수 없고 **단일 선택 해제를 소비처별 정책 훅으로 남겨야** 한다(공통에 넣으면 타워의 의도된 동작이 깨지고, 빼면 주민 버그가 그대로다). 주민 쪽은 `_lastSingle != null`일 때만 `MouseManager.ClearSelection()`을 부르는 형태로 닫았다. 추출 자체는 `TowerMergeCoordinator` 재배선(합성 게이팅·패널 스왑·핑크 프리뷰·WL-087 우회를 소유)이라 테스트가 0인 상태에서 회귀 위험이 커 #302·#341 어느 쪽에도 넣지 않았다 — **기능 PR에 순수 리팩터를 섞지 않는다**는 판단이다 | **#371에서 처리한다**(이슈 등재 완료). 부팅·참조 백오프·씬 정리·페이즈 게이팅을 순수 C# 헬퍼로 추출해 세 코디네이터가 공유하되, **밤 정리의 단일 선택 해제는 소비처별 정책 훅으로 남긴다** — 공통에 넣으면 "타워는 밤에도 단일 선택 유지"라는 의도된 동작이 깨지고, 빼면 주민 유령 초록이 그대로다. 세 코디네이터가 아직 같은 소유자인 동안 처리
- **WL-147** | OPEN | muchan918/n0wst4ndup | 순수 자원 지급 창구가 열렸다 — `ManagementController.Grant(IReadOnlyList<ResourceCost>)`(`:213`)가 팀 계약 #3(`SystemMap.md:863`, "`Add`를 public으로 열지 말고 차감+지급이 한 트랜잭션인 패턴을 따를 것")이 금지한 형태를 도입하고, #284가 같은 커밋에서 계약 문서를 갱신했다. 되돌리기(#281)의 역연산으로서 필요성은 인정된다 — 지불과 환원이 다른 시점·다른 조작에서 일어나 `TryExchange` 형태에 담기지 않는다. 문제는 "인자는 실지불 비용이어야 한다"는 가드가 **주석뿐**이라는 점이다. #444가 호출 지점을 `ReversibleCommandBase.Undo`(`Assets/Scripts/Command/ReversibleCommandBase.cs:71`) **한 곳으로 모아** 감사 범위는 오히려 좁아졌지만(커맨드 셋이 전부 그 한 줄을 지난다), 시그니처는 그대로라 위험의 성질은 안 바뀌었다. WL-043·WL-056(웨이브 클리어 마나석 지급)과 §5.6 보상 컨트롤러가 착수되면 임의 수량 지급에 이 API를 쓰는 것이 가장 싼 길이 되고, 그 시점에 계약 #3은 사실상 사라진다(자원 유입 지점을 한 자리에서 감사할 수 없게 된다). 이중 환원도 시그니처 차원에서는 열려 있다 — 오늘 막는 것은 커맨드의 상태 기계뿐이다 | Resource 소유자(n0wst4ndup) 사인오프 + 영수증 패턴(`TrySpend`가 `out ResourcePayment`(실지불 내역)를 돌려주고 `Refund(in ResourcePayment)`만 public)으로 구조적 차단 중 택1. 커맨드가 이미 `IReadOnlyList<ResourceCost>`를 들고 있고 환원 호출이 한 줄로 모여 있어 이관 비용은 작다
- **WL-148** | OPEN | muchan918 | 되돌리기 LIFO 불변식이 런타임 검사 없이 주석으로만 지켜지고, **#444로 의존 범위가 넓어졌다** — 종전의 "타일 점유 회수 순서"에 더해 "본진 레벨 ≥ 하위 건물 요구 레벨"이라는 수치 제약까지 순서에 기댄다(`ResourceSpendCommand.cs:23-26`). 게다가 `CommandHistory.Undo`가 `command.Undo()` **전에** 스택에서 빼고(`CommandHistory.cs:97-99`) `ReversibleCommandBase.Undo`도 `OnUndo` 전에 `Undone`으로 전이해(`ReversibleCommandBase.cs:66-67`), **복원이 false를 내면 그 항목은 재시도 없이 소비된다** — 다음 Ctrl+Z가 하위 계층을 먼저 되돌릴 수 있다. 지금 도달 가능한 false는 `TryRevert*`의 index 대조 실패(중복 SO authoring)뿐이고 그 경로는 LogError로 드러내므로 조용하지는 않다. 타워 축은 `Tower.cs`에 `Destroy` 경로·HP·`IDamageable` 구현이 없어 히스토리 밖에서 죽는 경로가 여전히 없다. 철거가 들어오면 `A 배치 → A+B 합성 → 합성 결과 철거` 뒤 되돌리기가 `TowerFootprint.Reoccupy` 규칙 ②(`:53-59`)를 밟아 **타일 없는 타워**를 만든다(임의 철거는 `Docs/Core/Tower.md` §6 열린 항목 #1) | `IReversibleCommand.Undo()`를 `bool`로 바꿔 실패 시 상태 전이를 되돌리고 `CommandHistory`가 pop을 취소하게 — 구현체 3종·호출부 4곳(`CommandHistory.cs:99`, `TowerFusionController.cs:151·161`, `TowerMergeCommand.cs:129`)뿐인 지금이 가장 싸다. 인터페이스 소유자(muchan918) 합의 필요. 철거 이슈의 선행 조건으로 묶을 것
- **WL-149** | OPEN | muchan918/SUNJIN | 전투맵 표면 y에 단일 출처가 없다 — 지면은 `tileRoot.y`(씬 -2)+`CombatMapGenerationSettings.TileHeight`(0)이 결정하고, 몬스터 발은 거기에 `monsterWaypointYOffset`(씬 3.2)을 더한 값인데, 스킬 시전면은 이 셋 중 무엇도 참조하지 않는 별도 상수 `SkillButtonView._castHeight`에 손으로 복제돼 있다. #465에서 GameScene·스크립트 기본값·UI 프리팹 값을 5로 맞추고 사용하지 않는 월드 X/Z 오프셋은 제거했지만, 전투맵 표면과 자동 연동되지 않는 근본 문제는 남아 있다. 지면·몬스터·시전면 중 하나만 움직이면 스킬 적중 판정만 조용히 어긋나며 컴파일·경고 어느 쪽에도 걸리지 않는다 — 인디케이터는 정상적으로 보이고 쿨다운도 돈다 | `CombatMapTileSpawner`에 표면 y 질의 API 공개(또는 `CurrentWorldEnemyRoute[0].y` 사용) 후 `SkillButtonView`가 pull — SUNJIN 사인오프
- **WL-150** | OPEN | SUNJIN | 런 길이 출처가 둘이다 — #294로 웨이브 수가 `MonsterSpawnWaveProvider.waves` 등록 개수(현재 7)에서 파생되는데, 맵 공개·스폰 지점은 `CombatMapRevealController.totalRounds`(씬 30)를 분모로 독립 계산된다(`:15,153-160`). 라운드 번호는 같은 `DayNightManager.CurrentWave`가 양쪽에 흘러간다(`CombatMapMonsterConnector.cs:126,128,136`). 최종 웨이브 7에서도 `progress = (7+2)/30 = 0.3`이라 경로의 30%만 공개된 채 게임이 끝나고, 앞으로 웨이브를 리스트에 append하면 승리 조건은 자동으로 따라오지만 공개 페이스는 따라오지 않아 두 값이 조용히 어긋난다 | `totalRounds`를 `MonsterSpawnWaveProvider.FinalWaveNumber`에서 pull하거나 최소한 시작 시 불일치 경고 — `GDD.md:302`(전체 웨이브 수·구성 확정)와 묶어 처리
- **WL-151** | OPEN | n0wst4ndup | 주민 BT 밸런싱 수치가 그래프 Blackboard가 아니라 **에디터 어셈블리 상수**에 있다 — `ResidentBehaviorGraphBuilder.cs`의 `const` 40여 개(`DanceSoloRadius=30`·`EncounterChance=0.08`·`GoHomeSpeedFactor=5` 등)가 `ResidentBehavior.asset`을 산출하고 **손 편집은 재빌드에 소실된다**. `SystemMap.md` BossAI 행이 확립한 "수치는 그래프 Blackboard로 authoring"(WL-094, 같은 소유자·같은 BT 계열)의 역방향이다. 실제로 이번 이슈에서 플레이 튜닝값이 재빌드로 날아갈 뻔해 회수용 Dump 메뉴(`NorthLand/Resident/Dump Behavior Graph Values`)를 만들었는데, 그 도구의 존재 자체가 워크플로가 어긋나 있다는 증거다. **#305에서 드리프트가 두 번째로, 이번엔 실제로 발생했다** — 대화 빈도를 플레이로 튜닝한 궤적(`EncounterChance` 0.05 → 0.1 → 0.08, `ConversationJoinChance` 0.10 → 0.3 → 0.12)의 중간 단계가 그래프 에셋에만 반영되고 상수는 옛 값에 멈춰 있었다. 커밋 `6c80f3d`가 손으로 맞출 때까지 빌더 diff는 **마지막에 내린 값을 올린 값으로 보이게 했고, 자동 리뷰가 실제로 "PR 본문과 코드가 반대 방향"이라고 오판했다.** Dump 메뉴는 값을 **회수만 할 뿐 불일치를 탐지하지 못한다** — 드리프트가 있어도 아무도 알려주지 않는다. 클립 이름 4종은 이미 Blackboard로 올라갔다(에디터가 로컬 `List<string>`을 표시하지 못해 "연결 안 됨"으로 오진된 사고 이후). 코드 저작 + 자기검사 자체는 가치가 실증됐으므로(조용한 실패 12건을 잡는다) 폐기 대상이 아니다 | 개체차 없는 전역 튜닝 수치만 Blackboard 공유 변수로 승격하고 빌더를 "없으면 상수로 초기화, 있으면 보존"으로 전환 — 또는 "이 그래프는 코드 소유, Blackboard 튜닝 금지"를 SystemMap에 명문화해 종결
- **WL-152** | OPEN | n0wst4ndup | 경영 공간 신규 타입이 전역 네임스페이스를 점유했다 — #276이 `Resident`·`ResidentConversation`·`ResidentSpawner`·레지스트리 3종을 포함해 18종을 전역에 추가했다. BT 리프 노드·`ResidentAgent`는 패키지 규약상 전역이 강제되지만(`BossNodeReference.md` 「작성 규약」) 나머지는 그 제약을 받지 않는데 세트를 맞춰 함께 전역이다. **WL-029(같은 폴더 `ManagementSpace/Territory` 16개 파일 전역, 같은 소유자)와 동일 축**이고, asmdef이 없어 전부 `Assembly-CSharp`이라 WL-146(`BurnEffect` 이중 정의)처럼 **다른 브랜치 머지 시점에** 터진다. **#341이 5종을 더 추가했다** — `BuildingInstanceRegistry`·`IDragHandle`·`ResidentDragCoordinator`·`ResidentVoice`·`ResidentVoiceAudibility`. 이 중 앞의 둘은 주민 전용이 아니라 **건물·입력 축의 전역 이름**이라 충돌면이 그만큼 넓어졌다. 현재 전수 grep상 충돌 0건. "주민"은 muchan의 `MaxVillagers` 작업과 §8 드래그가 동시에 건드릴 개념이라 충돌 확률이 낮지 않다 | 지금 쪼개지 말고 WL-029에 이 세트를 묶어 경영 공간 네임스페이스 격리를 **한 번에** 수행(BT 노드·파사드는 제외 대상으로 명시)
- **WL-153** | OPEN | n0wst4ndup | 주민 군중의 밀도 조건이 정본 경영 씬에서 사문화될 수 있다 — R5 춤은 `DanceSoloRadius=30` 안에 이웃 0명일 때만 시작하고 `DanceInterruptRadius=30`이 그 조건이 깨지면 즉시 끊는다. 이 값은 **200×200 테스트 씬에 24명**(주민 1명당 약 1,667㎡)을 놓고 실측한 것인데, 정본 `GameScene`의 경영 지면은 그보다 훨씬 좁아 같은 인원이면 반경 30 안에 늘 누군가 있다. 그러면 R5가 **로그 한 줄 없이 화면에서 사라진다** — 노드는 정상적으로 Failure를 반환하므로 에러도 경고도 없다. 같은 축으로 조우 반경 6.4·대화 거리 4·문 배분(문 20개 가정)도 전부 테스트 씬 밀도 기준이다 **#302에서 메인 씬 이식이 끝났으나 이 재실측은 하지 않았다** — 정본 지면의 최대 NavMesh 리전은 전체의 81%이고 주민 30명이 그 안에서 돈다. | 메인 씬 이식이 끝났으므로 **즉시 밀도 의존 수치 5종을 재실측**(춤 반경·이웃 수·조우 반경·대화 거리·문 개수). `ResidentDebugView`의 분홍 원이 한 번도 안 뜨면 이 항목이 발현한 것이다
- **WL-154** | OPEN | KIM-SUNGSOO | 유도 없는 고정 방향 비행의 접촉 판정 이산 샘플링 터널링. 산탄(StraightFlight)은 SphereCast 스윕 전환으로 해결됐으나(ProjectileFlight.cs:217), 부메랑(BoomerangFlight.Step, ProjectileFlight.cs:303)은 여전히 매 프레임 점 CheckSphere만 쓴다 — boomerang_tower.asset이 Speed 60·HitRadius 0.5로 프레임당 이동(1)=판정 지름(1.0) 경계값이라 델타타임 변동·프레임 드랍 시 재발 여지가 남는다 | BoomerangFlight.Step도 SphereCast 스윕으로 통일
- **WL-155** | OPEN(피해자 교체 · #315) | KIM-SUNGSOO | `BeamAction`이 `Projectile.DamageDealt` 통지 축 밖에서 직접 `TakeDamage`를 부른다(`BeamAction.cs:126`) — 히트스캔은 투사체가 없으니 발행하지 않는 것이 설계 의도지만(`TowerAddGuide.md` §6), 그 결과 **이 이벤트에 붙은 기능은 빔 타워에서만 조용히 빠진다.** 원래 피해자였던 버프 화상은 #315로 사라졌으나 **구조는 그대로고 새 피해자가 이미 들어왔다** — #300의 성장형 타워가 `Trigger=Hit`일 때 `Projectile.DamageDealt`를 구독하므로(`RampAction.cs:181`) "빔 타워 + 명중 램프"는 아무리 때려도 스택이 0에 머문다. 현재 완화는 **저작 시점 경고 하나뿐**(`TowerAsset.cs:238`, `hasRamp && Trigger==Hit && !hasAttack`이면 LogWarning) — 구조 수정이 아니라 조합 금지 안내이고, `AttackAction`과 `BeamAction`을 **둘 다** 가진 타워는 이 경고를 통과하면서도 빔 데미지분은 여전히 집계되지 않는다 | 향후 투사체 없는 공격 액션이 하나 더 추가되는 시점에 통지 축을 `IAttacker`/`Tower`로 통합
- **WL-156** | OPEN | KIM-SUNGSOO·muchan918 | 직접배치 로스터 **구성 변경이 계속 팀 사인오프 없이 진행된다** — 11→7종 축소(개틀링·스나이퍼·화염아처·소이캐논 제거)에 이어 #441이 `soda_tower`를 합성 전용으로 올리고 `killstack_tower`(사냥꾼)를 직접배치 1차(Cost 나무 40 · UnlockWave 7)로 내렸다. 비용 저작 규칙 자체는 전수 감사에서 다시 성립 확인 — 타워 SO 19종 중 직접배치(Rarity 0) 7종 전부 Cost 채움, 합성 전용(Rarity 1·2) 12종 전부 `Cost: []`, 예외 0건. 단 #441 중간 커밋에서 `soda_tower`가 Rarity 1인데 Cost 40을 남겨 **합성 후보 툴팁이 차감되지 않는 비용을 표시하는** 실제 버그가 났다(합성은 `recipe.ExtraCost`만 차감하는데 툴팁은 `Result.Cost`를 읽는다) — 규약이 SO 한 줄로 깨질 수 있고 `TowerAsset.OnValidate`는 자기 SO만 봐서 "이 타워가 어떤 레시피의 결과인가"를 알 수 없다. GDD §5.8은 족보를 여전히 TBD로 남긴 상태 | 로스터 구성 사인오프 + 합성 전용 Cost/UnlockWave 규칙의 기계 검사 수단 결정(`TowerRecipeCatalog`를 훑는 에디터 검사 — WL-171의 ②·③ 툴과 같은 자리)
- **WL-157** | PARTIAL | muchan918 | 범위 판정 버퍼가 포화 시 초과분을 조용히 버리는 문제 — **스킬 3종은 #398에서 닫혔다**: `SkillHitScan`이 `Collider[64]`로 시작해 **포화하면 2배로 키워 다시 친다**(`SkillHitScan.cs:43-50`, 경고 대신 무손실 재질의). `IDamageable` 단위 중복 제거까지 그 안에 있어 콜라이더 2개짜리 몬스터의 이중 피격도 함께 막혔다. **잔여: `Projectile` 스플래시**(`ApplyArea`/`FindNearestUnhit`, WL-025 잔여 축과 같은 코드) — 여기는 아직 고정 버퍼이고, 무증상 누락이라 발현해도 원인 추적이 어렵다 | `Projectile`도 `SkillHitScan`과 같은 확장형(또는 공유 버퍼 유틸)으로 전환 — WL-025와 함께 닫을 것
- **WL-158** | OPEN | n0wst4ndup·muchan918 | 주민 선택 상한이 "표시를 막는 것"으로 구현돼 있어 상태와 화면이 어긋날 수 있다 — 유휴 인원이 0이면 `ResidentSelectable.AllowsOutline(OutlineKind.Selected)`가 거짓을 돌려 `OutlineInteractionDriver`가 초록을 그리지 않는데(`IOutlineKindFilter`), **`MouseManager._selected`에는 그 주민이 실제로 들어간다**(`OnSelected()` 호출·`OnPrimarySelect` 발행 모두 정상 진행). 주민은 `OnSelected()`가 비어 있고 정보 패널도 없어서 지금은 관측상 동일하지만, §8 주민 패널이 붙으면 **"막았다"고 한 선택에서 패널이 열린다**. 초기 구현은 상한 0일 때 주민을 `Selectable` 레이어에서 내려 레이캐스트 자체를 끊었는데, 그 방식은 호버·툴팁까지 함께 죽여 "고를 수 없다"와 "대상이 아니다"가 구분되지 않고 거절 피드백(흔들림·토스트)을 걸 자리도 없어 폐기했다. **같은 뿌리에서 두 번째 얼굴이 나왔다** — 밤 전환 정리가 단일 클릭 초록을 놓쳐 아침에 유령 초록이 남던 문제(재리뷰 지적, WL-145 세 번째 사례)도 원인이 같다: 선택 상태를 **전역 `MouseManager._selected`와 도메인 집합이 나눠 갖는다.** 그쪽은 `_lastSingle != null`일 때만 `ClearSelection()`을 부르는 것으로 닫았지만, 그것도 "도메인이 전역 상태를 대신 정리한다"는 같은 우회다 — 소유권을 한쪽으로 모으지 않는 한 세 번째 얼굴이 또 나온다 | 상한을 "선택 가능 여부"가 아니라 **"동시 선택 개수"** 로만 쓰도록 의미를 정리하고(단일 클릭은 항상 허용, 그룹 크기만 제한) 거절 피드백 형태를 muchan918과 합의 — 주민 정보 패널(§8) 착수 전에 결정
- **WL-159** | OPEN | n0wst4ndup | 대화 세션이 밤 일괄 수거에서 해산 절차를 밟지 못한다 — 세션은 티커가 없고 진행이 참가자의 행동 종료에만 붙어 있어(`ResidentConversation`), **참가자 전원이 한 프레임에 비활성되면**(`ResidentSpawner`의 밤 수거) 어느 노드도 `Disband(cooldown)`에 도달하지 못한다. 세션 객체 자체는 `Resident.OnDisable`이 `Conversation = null`로 참조를 놓아 GC되므로 누수는 아니다(자기정리가 필요하던 부속물 `ResidentConversationObstacle`은 폐기돼 이 축에서 빠졌다). 남는 실질 잔재는 **해산 쿨다운 미적용** 하나다 — `Encounters.Mark`가 안 걸려 다음 아침에 같은 쌍이 즉시 다시 대화할 수 있다(§7.1 재진입 방지가 그 창에서만 무효). 세션 부속물이 다시 늘어나면 폐기된 회피물이 그랬듯 개별 자기정리를 또 복제해야 한다 | 세션 소유자(레지스트리 또는 스포너)가 밤 전환 시 활성 세션을 일괄 `Disband`하도록 승격 — 부속물 자기정리를 한 곳으로 모은다
- **WL-160** | OPEN | n0wst4ndup | 경영 공간 NavMesh 배선이 **두 저장소로 쪼개져 있다** — `NavMeshSurface`와 그 `m_NavMeshData` 참조, 그리고 다리 프록시 오브젝트는 `Assets/Imported/@NorthLand/Prefabs/Management/CandyLand.prefab`(별도 private repo)에 있고, 베이크 데이터(`Assets/Scenes/GameScene/NavMesh-NavMesh.asset`, 758KB)와 프록시 메시·머티리얼(`Assets/Meshes/Nav/`)은 본 저장소에 있다. **본 저장소 어디에서도 새 베이크 에셋 GUID(`01dbedcce8abdb54f901f82155b243ff`)를 참조하지 않아**(Grep 0건) "다리를 건넌다"는 결과가 이 저장소의 diff만으로는 성립하지도, 검토되지도 않는다. 두 저장소의 머지 순서가 어긋나면 조용히 깨진다 — 본 저장소만 먼저 들어가면 Surface가 옛 에셋을 가리켜 다리가 안 고쳐지고, Imported만 먼저 들어가면 프록시 없는 상태로 `NavMeshLink`가 사라져 다리가 완전히 끊긴다. 어느 쪽도 콘솔 경고가 없고 플레이해야만 드러난다. **프록시가 씬 루트 절대좌표로 떠 있던 축은 해소됐다** — #305에서 다리 오브젝트의 자식으로 재부모화해 아트와 함께 움직인다(부작용: 재베이크에 Imported 체크아웃이 필수가 됐다, `SceneWorkflow.md` §7). **#332로 범위가 한 단계 더 넓어졌다** — 정지 금지 존 6개(`ResidentNoStopZone`)가 `CandyLand/ResidentNoStopZones/` 아래, 즉 Imported 저장소로 들어갔다(`db58569`). 스크립트·문서는 본 저장소에 있고 **배선만 저쪽에 있어**, "다리·계단에서 대화가 안 열린다"는 결과를 본 저장소 diff만으로는 검토할 수 없다 — 실제로 #333 자동 리뷰가 본 저장소만 grep하고 "존이 0개라 기능이 통째로 빠졌다"고 🔴로 오판했다. 머지 순서가 어긋나면 본 저장소만 들어갔을 때 **존 0개로 아무것도 막히지 않으면서 컴파일·콘솔 신호가 없다**(초안은 씬 루트에 두어 이 축을 피했으나 `GameScene.unity`가 #327·#329와 병합 충돌해 프리팹으로 옮겼다 — 씬 병합 비용과 맞바꾼 결정). **#341로 축이 하나 더 늘었다** — 주민 목소리 클립 8본과 `Marshie_01~03`의 `ResidentVoice` 배선, 소멸 파티클(`ResidentSpawner.despawnEffectPrefab`)이 Imported로 들어갔다(짝 커밋 `63f277b8`·`e0d39395`). 스크립트·문서는 본 저장소인데 배선만 저쪽이라 "소리가 난다"를 본 저장소 diff로 검토할 수 없고, 본 저장소만 먼저 들어가면 `ResidentVoice`가 어느 프리팹에도 붙어 있지 않아 **컴파일·콘솔 신호 없이 소리만 안 난다**. `despawnEffectPrefab`은 미배선 시 경고조차 없다(`[Tooltip]`만). **#466로 네 번째 축이 붙었다** — 들기 자세 클립(`@NorthLand/Animations/Resident/Marshies_Sitting.fbx`)과 `Resident.controller`의 `Sitting` 스테이트가 Imported에 있다. 이쪽은 **개별 처방을 적용했다**: `ResidentAgent.PlayState`가 `Animator.HasState`로 먼저 확인하고 없으면 상태 이름당 1회 경고 + `false`를 반환한다(종전에는 `CrossFadeInFixedTime`을 그냥 불러 조용히 무시됐고, 전이 없는 고립 상태라 화면에도 안 드러났다 — R3 인사·R4 수다·R7 놀람 등 기존 임의 클립 경로도 함께 덮인다) | `NavMeshSurface`를 아트 프리팹 밖(씬의 `=== ManagementArea ==========` 루트)으로 올려 **경영 공간 내비게이션 소유권을 본 저장소로** 모은다 — 지금은 아트 저장소 프리팹이 게임 저장소 에셋 GUID를 참조하는 역방향 의존이다. 주민 앵커(웨이포인트·문 지점·정지 금지 존)를 같은 축으로 볼지 함께 결정할 것
- **WL-161** | OPEN | n0wst4ndup·muchan918 | 경영 공간 NavMesh가 **완전 정적 베이크**인데 GDD §5.3 영토 확장은 런타임에 섬 프리팹을 `Instantiate`한다(`TerritoryNodeStateVisual.cs:177-180`, `TerritoryDefinition`이 "자원 종류 + 섬 프리팹 + 일일 수급"). `Assets/Scripts` 전체에 `NavMeshSurface`/`BuildNavMesh`/`NavMeshData` 호출이 **0건**이라 런타임에 NavMesh를 갱신하는 경로가 없다 — 확장 영지 위를 주민이 걸어야 하는 순간 매 확장마다 에디터 재베이크 외에 답이 없다. #305에서 마지막 동적 요소(`NavMeshLink`)까지 베이크 프록시로 대체돼 정적 쪽 결합이 한 걸음 더 강해졌으므로 방향을 지금 정하는 편이 싸다 ⚠ **전제 절반이 사라졌다**(2026-08-22 줄참조 감사) — `TerritoryNodeStateVisual`·`TerritoryDefinition`이 #337로 삭제돼 런타임에 섬 프리팹을 Instantiate하는 경로가 지금은 없다. NavMesh 런타임 갱신 호출 0건은 그대로이므로, 확장 시스템이 다시 들어올 때의 **선행 조건으로 재범위화**가 필요하다 — 상태 변경은 팀 판정. | 확장 영지의 주민 통행 여부를 먼저 결정 — 통행한다면 섬 프리팹마다 사전 베이크된 `NavMeshData`를 붙여 `NavMesh.AddNavMeshData`로 합치는 방식(프록시 메시 방식과 잘 맞는다)을, 통행하지 않는다면 "확장 영지는 주민 통행 대상 아님"을 `Resident.md` §11.11에 명시
- **WL-162** | OPEN | n0wst4ndup | **#101에서 분리된 축이다(팀 결정 2026-08-07)** — #101 원문의 잠금 명세는 "라이트 전환만 UniTask Lerp로 만든다"는 전제 위에 쓰였는데 구현이 셀 와이프 + 전역 블렌드 구조로 가면서 전제가 바뀌었다. 잠금 대상·시점을 새 구조 기준으로 재정의해야 해서 원문을 그대로 소화하지 않고 이 항목으로 옮겼고, #101은 전환 연출로 닫았다. 낮/밤 전환이 0.8초 연출로 바뀌었는데(`DayNightTransition`) **전환 중 게이팅 소비처가 0**이다. `DayNightManager.EndDay()`/`EndNight()`는 여전히 `CurrentPhase`를 동기로 바꾸고 이벤트를 그 자리에서 발행하므로, 화면이 채 바뀌기 전에 후속 동작이 전부 실행된다 — `StageBuilder.GenerateNextStage`가 `OnDayToNight`에서 동기로 `MonsterSpawn.StartRound` 호출(낮 화면에 몬스터 등장), `ManagementController.RequestAdvancePhase`·`NightActionPanelView` 버튼 재클릭으로 페이즈 중복 전환, `TerritoryController.HasExpandedToday`가 `OnDayStart`에서 즉시 리셋돼 `TryClaim` 가능, `ManagementController.IsDay`가 즉시 true라 주민 배치 변경 가능. `DayNightTransition.IsTransitioning`·`OnTransitionComplete`는 뚫려 있으나 **아무도 읽지 않는다**(전체 grep 소비처 0). 타워 배치 게이팅은 WL-019·#71 소관이나 붙으면 같은 이유로 막아야 한다 | 4개 진입점이 `IsTransitioning`을 보거나 `OnTransitionComplete`를 기다리도록 재배선(#101 완료기준)
- **WL-163** | OPEN | n0wst4ndup·muchan918 | 아트 저장소 FBX 임포트가 **albedo를 emissive 슬롯에 배선한 머티리얼을 만들어 낸다** — `FK_Color`(`Textures_4`)·`FK_Color_2`/`FK_Color_2_Glossy`(`Textures_3`)가 `_BaseMap`과 `_EmissionMap`에 같은 텍스처를 물고 발광 픽셀 비율 97~99%였다. 라이트를 0으로 내려도 이 머티리얼을 쓰는 오브젝트(`Platform_*`·`PlatformGround_*`·`Candy_07`·`IceCream_01`·`ChocolateBridge_01`·`Cake_03`·`Tower_01` 등 캔디랜드 지형·건물 전반)만 대낮 밝기로 남아 밤 라이팅이 아무리 어두워도 화면 절반이 낮처럼 보였다. 이 3개는 껐지만(`0207c41e`, 별도 private repo) **새 FBX를 임포트할 때 같은 상태로 다시 들어오는 것을 막는 장치가 없다** — 임포트 프리셋도, 변환 툴(`FlatKitMaterialConverter`)의 검사도 없다. `FK_GEM_LP`(7.3%)·`FK_blinn14SG`(58%)는 정상 마스크라 유지 대상이므로 "이미시브 전부 끄기"로는 닫히지 않는다 | `FlatKitMaterialConverter`에 "emissionMap == baseMap이면 경고/해제" 검사 추가, 또는 아트 임포트 절차에 체크 항목 등재
- **WL-164** | OPEN | sunjin1222·muchan918 | 테스트 전용 디버그 패널이 정본 GameScene.unity에 실사용 배선된 채 출하 대기 중이다(PR#310 본문에서 "의도적 복원, 출시 전 제거/제한 여부는 별도 결정"으로 명시). **#350에서 권능이 확대됐다** — 자원 4종 지급 + 웨이브 강제클리어 + 낮 스킵에 **전 타워 무료 배치 18종**(합성 전용 포함, `DebugTowerSection.cs`) · **스킬 효과 레벨 임의 조작**(`DebugSkillEffectSection.cs`) · **본진 무적**이 더해졌고, 무적은 `Assets/Scripts/Test` 밖 프로덕션 클래스의 `PlayerBase.DebugInvincible`(public setter, 가드 없음)을 통과한다. 행 프리팹 `Assets/Prefabs/UI/DebugRowButtonPrefab.prefab`도 프로덕션 프리팹 폴더에 있다. 레거시 `Input`은 Input System으로 교체됐고 `Keyboard.current`·`Panel` null 가드도 추가됐다(#350). `Assets/Scripts/Test`·`Editor`에 `#if` 가드 선례는 0건 | 팀이 출시 전 제거/`#if UNITY_EDITOR \|\| DEVELOPMENT_BUILD` 가드 여부를 확정하고, `PlayerBase.DebugInvincible`과 `DebugRowButtonPrefab`도 같은 결정에 포함한다
- **WL-165** | OPEN | sunjin1222 | `Assets/Prefabs/UI/UICanvas.prefab`(guid 158159f491590e440ad520dd4b8d47cf)이 **참조 0건 유령 사본**으로 남아 있다. 씬 UI를 정본으로 옮기려던 일회성 이관 수단이었고 Unpack 이후 역할이 끝나, 현재 어떤 씬·프리팹도 이 프리팹을 인스턴스화하지 않는다(전수 grep 0건 — 정본 GameScene.unity의 UICanvas는 언팩된 순수 GameObject다. 종전 서술 "씬 인스턴스가 RectTransform 오버라이드 64건"은 현재 정본 씬과 맞지 않아 대체했다). 파일 안에는 `BuffSkillButton`(`:6061`)+`BuffSkillButtonView`(`:6180`) 등 옛 UI 배선이 그대로 살아 있고, `Assets/Prefabs/UI/`의 유일한 `UICanvas`라는 이름 때문에 UI 정본으로 오인해 씬에 끌어다 놓기 쉽다 — 그러면 #315로 제거한 버프 버튼까지 함께 복귀한다(WL-166과 같은 축, 다만 수동 경로) | 파일 삭제, 또는 `_Deprecated_` 접두 리네임으로 정본이 아님을 이름으로 드러내기
- **WL-166** | PARTIAL | muchan918 | 씬에서 **삭제한** 배선이 덮어쓰기 병합으로 조용히 되살아난다 — `SceneWorkflow.md` §4의 정본 확정은 개인 스냅샷을 정본 파일에 그대로 덮어쓰는 방식이라 삭제가 병합으로 보존되지 않는다. #315가 `GameScene.unity`에서 뺀 버프 배선 3개(`BuffSkillManager` 루트·`SkillEffectManager`의 `BurnBuff`·`UICanvas/SkillPanel/BuffSkillButton`)가 스크립트 보존 결정 때문에 부활해도 **missing script 경고가 0건**이라 무증상이며, 보상 풀은 3종이라 "버튼은 눌리는데 화상은 없는" 반쪽 상태로 돌아온다. WL-059(개인 씬 사본 미소진)·WL-028(정본 이원화)과 같은 통로이나 방향이 반대다(추가가 아니라 삭제의 유실). #321에서 `BuffSkillManager.Awake`·`BuffSkillButtonView.Awake`(`WarnRetired()` 가드로 `Instance` 미설정 + `enabled=false`)·`BurnBuff.Awake`에 LogError 트립와이어를 넣어 **재배선이 콘솔에 즉시 드러나게** 했으나, 배선이 살아 있는 사본 자체는 그대로다 — `BuffSkillManager` guid `4d1911a9…` 기준 21개 파일: `Assets/Scenes/Branches/GameScene_ori·_5·_10·_12~_21·_501`(14), `Personal/SUNJIN/Scenes/GameScene13·14·GameSceneTest`(3), `Personal/muchan/Scene/Skill·Skill_v2·SkillEffectTest`(3), `Assets/_Recovery/0 (1).unity`(1) | 진행 중 개인 씬을 정본에서 재복사하거나 버프 3개 오브젝트를 삭제하고, `Branches/`는 `SceneWorkflow.md` §5의 주간 정리로 비운다
- **WL-167** | OPEN | sunjin1222·muchan918 | 은퇴한 `WaveRewardType`이 **구 세이브를 파괴하고 파일까지 지운다** — `RunSaveManager.RewardEffects.cs:110-123`은 `SkillEffectManager.HasEffect`가 false인 타입의 레벨이 1 이상이면 배선 오류로 보고 `LogError` 후 `return false`를 한다. #315 이전 세이브는 `BurnBuff`가 씬에 배선돼 있었으므로 캡처 루프(`:28-47`)가 `BuffBurn` 레벨을 실제로 저장해 왔고, `SaveFormat.cs`가 `OldestSupportedVersion = CurrentVersion = 1`이라 버전 게이트에도 걸리지 않고 그대로 로드된다. 실패는 영토·경영·전투맵·타워·본진 복원이 **모두 끝난 뒤** 발생하며(`RunSaveManager.Orchestration.cs`), `RunSaveManager.Persistence.cs:91-107`이 "타이틀로 돌아갑니다" 후 **복원 불가 세이브를 삭제한다.** 빌드 4 플레이테스트에서 버프 화상을 한 번이라도 고른 팀원·QA가 대상이다(출하 세이브 없음, 팀 내부 한정). "정의는 보존하고 배선만 끊는다"가 이 팀의 표준 제거 방식이 된 이상 은퇴 타입은 앞으로도 계속 생긴다 | 은퇴 타입(풀·컴포넌트 양쪽에 없는 타입)은 레벨 무관 `LogWarning`+스킵으로 강등하거나, `CurrentVersion`을 올려 **복원 시작 전에** 거절 — 반쪽 복원 후 삭제만은 피한다. 당장은 팀에 `run-save.json` 삭제 공지
- **WL-168** | PARTIAL | SUNJIN·muchan918 | 보상 **4종 = 카드 3**이라 3택1이 아직 성립하지 않는다 — `WaveRewardPool.asset` 4종 × `SkillEffect.cs:13` `maxLevel = 3` = 소비 총량 12인데, 런 길이는 `MonsterSpawnWaveProvider`가 씬에 등록된 wave 에셋 순서로 정하며 `GameScene.unity:798-805`에 **7개**가 배선돼 있어 선택 기회는 7회다(`MonsterSpawnTable.csv`의 Round 1~10은 `Assets/Scripts` 전체 소비처 0건인 죽은 데이터다 — 런 길이 근거로 쓰지 말 것). **경과(#316)**: 전기장 추가로 3종 → 4종이 되어 **"첫 웨이브부터 늘 같은 3장"은 해소됐다** — 이제 4장 중 3장이 뽑혀 런 간 분기가 최소한으로 생긴다. 잔여: `WaveRewardPool.cs:62`의 `Mathf.Min(count, remaining.Count)`는 그대로라 **한 효과가 Lv3에 닿는 순간 남은 3종이 매번 전부 제시**되고, 두 효과를 밀면 다시 2장으로 줄어든다. 후보 0은 `WaveRewardController`가 정상 경로로 스킵하므로 크래시는 없다. GDD §5.6이 스스로 "종류가 (상한 + 3)보다 적으면 3택1이 무너진다"를 조항으로 갖고 있고 4종은 그 조건(6종)에 여전히 미달한다 | 보상 종류를 6종으로 확장(**잔여 2종**, 별도 이슈, 시드 파생 WL-008 동반) — 확장 전 임시 완화는 효과 상한 3→2(SO/인스펙터 수치, 코드 변경 없음). ⚠ 착수 전에 WL-080(종료 취소 훅)을 먼저 닫을 것
- **WL-169** | OPEN | muchan918 | 스킬 보상 효과의 밸런스 수치와 디버그 플래그가 `GameScene.unity`의 컴포넌트 인스펙터에만 존재해, 씬 diff가 사실상 읽히지 않는 리뷰에서 조용히 통과한다 — #316이 실사례다: PR 본문·이슈 §초기수치·코드 기본값(`FieldEffect.cs:12·19`)이 전부 틱뎀 3·반경 3.6인데 씬(`GameScene.unity:4309·4312`)에는 5·18이 들어갔고, `debugLog: 1`(`:4316`)까지 켜진 채 올라왔다(같은 씬의 다른 3효과는 전부 0). 수치가 문서와 갈리면 **밸런스 사인오프의 근거 숫자와 출하 숫자가 달라져** 리뷰 승인이 무의미해지고, 나아가 그 값에 기대 도입한 판정 방식의 정당성까지 흔들린다 — "반경 3.6은 부양 높이 6에 못 닿는다"를 근거로 수직 캡슐(`SkillField.cs`)을 넣었는데 실제 출하 반경 18에서는 구체로도 닿아 그 근거가 성립하지 않는다. WL-015(수치가 CSV 밖 SO/인스펙터에 authoring)가 **씬 파일**로 번진 표면이며, 씬은 SO와 달리 diff 판독조차 어려워 감지가 더 늦다 | 효과 수치를 SO로 승격(WL-015와 함께 결정) 또는 PR 체크리스트에 "씬 인스펙터 authoring 값 = 본문 수치 / `debugLog` = 0" 확인 항목 추가
- **WL-170** | OPEN | KIM-SUNGSOO·muchan918 | 범위 판정 버퍼 상한이 시스템마다 제각각이고 대부분 초과 감지가 없다 — TowerPlacer.cs:113 8, SkillManager.cs:48·SkillBomb.cs:16·Soldier.cs:18·Enemy.cs:52·AttackAction.cs:61 16, BeamAction.cs:68·DebuffAuraAction.cs:47 32, EnemyNodeQuery.cs:19 64. Physics.Overlap*NonAlloc은 버퍼가 차면 나머지를 말없이 버린다. 경과([Feature] 타워 에셋 적용 #336): SkillField.cs:30에 이어 Tower.acquireBuffer(16, Tower.cs:233)와 GroundZone.hitBuffer(32, GroundZone.cs:192)에도 count == 버퍼길이 시 LogWarning이 추가돼 포화 감지 지점이 셋으로 늘었다 — 나머지 6곳은 그대로다 | 버퍼 크기 산정 근거 합의(반경 × 최대 동시 스폰 밀도) + 포화 경고를 공통 헬퍼로 추출해 전 소비처 적용
- **WL-171** | PARTIAL | KIM-SUNGSOO | 밸런싱 규약 3개 중 **①만 코드가 강제한다** — 규약 ①(`간격 ≤ 사거리[타일] ÷ 3 + 스턴지속`)은 #326에서 `TowerAsset.OnValidate`가 잡도록 추가됐고, #441이 **CC 항과 경계 2종을 더해 강제 범위를 넓혔다** — 상한 `간격 ≤ 사거리/18 + 스턴지속`, 하한 `간격 > 스턴지속`(어기면 대상이 전진하지 못해 밤이 끝나지 않는다), 창 `면역창 ≤ 간격 − 스턴지속`(어기면 피해는 그대로인데 CC만 절반 이하로 떨어진다). CC 타워를 "규약의 예외"로 빼지 않고 항을 세워 검사 안에 넣었으므로 새 CC 타워도 같은 식으로 검사된다. 타워 SO 19종·레시피 12종 전수 실행 경고 0 확인(#326 이전에는 `cannon`·`incendiary_cannon`이 간격 2.0/2.2로 발사 1.4/1.3발이었다). 잔여 둘: **②(합성 결과 킬 수 > 재료 킬 수의 합)와 ③(티어 밴드 눈금)은 여전히 문서에만 있다** — 재료 타워들의 킬 수 총합을 알아야 해서 단일 SO `OnValidate`로는 검사 불가이고, 레시피 SO를 타고 재료를 로드하는 별도 에디터 툴 규모다(#441의 `rampup`이 재료 `killstack`+`haste`를 사거리 25/20 · 간격 0.8/1.1 · 발당 피해 20/14 **세 축**에서 상회하는 새 사례 — 성장 축은 `rampup`이 `AttackSpeed`, `killstack`이 `AttackDamage`라 스탯이 달라 직접 비교가 아니다). **CC 항이 명중 스턴과 오라·구역 스턴을 구분하지 않는다** — `LongestStun()`이 `Effects`를 무차별로 훑는데 그 리스트는 공격·디버프 오라·착탄 구역이 공유하므로, 오라형 CC 타워가 생기면 명중은 등속 통과 그대로인데 `AttackInterval` 상한만 완화된다(현재 스턴원이 `soda_tower` 하나뿐이라 전제가 성립하며, 그 사실이 코드 주석에 명시돼 있다). 절차 쪽은 `TowerAddGuide.md` §3.5와 `SystemMap.md` §6으로 막아 뒀다 | ②·③의 기계 검사 수단 결정(레시피 순회 에디터 툴 — WL-156의 Cost 검사와 같은 자리) + CC 항의 효과 축 분리(`Effects`를 명중용·오라용으로 가르는 작업이라 별도 이슈 규모)
- **WL-172** | OPEN | KIM-SUNGSOO·몬스터 담당 | 커밋된 타워 15종 수치가 **아직 적용되지 않은 몬스터 값을 전제한다** — `CombatBalance.md` §6.2 표는 `Yellow_Grummy` **속도 6 · HP 60**을 전제로 계산됐는데(`archer` 무변경 12피해 × 체류 4.98초 = 59.8 ≈ 킬 1.0으로 검산) 실제 `yellow_grummy.asset`은 속도 10 · HP 40이다. **속도만 적용되면 전 타워가 설계값의 1.5배로 동작하며 컴파일·콘솔 어느 쪽에도 신호가 없다** — 증상이 "밸런싱했는데 오히려 쉬워졌다"로 나타나 원인에서 멀다. #326에서 `MonsterBalanceRequest.md` §0의 HP 항목을 ⬜선택 → **✅필수로 승격**하고 "하나만 적용하면 어떻게 되는가" 표를 넣어 **분기 자체는 제거**했으나, 실제 SO 적용은 몬스터 담당 권한이라 미완이다 | `EnemyAsset` 6종에 이동속도 ×0.6 + HP ×1.5 **동시** 적용 후 `CombatBalance.md` §9.1의 HP-40 대안표 제거
- **WL-173** | PARTIAL | sunjin1222·muchan918 | 세이브 스키마 버전 고정 문제는 #328에서 해소됐다 — `SaveFormat.CurrentVersion = 2`로 올리고 `SaveMigrationChain`에 v1→v2 마이그레이션을 등록해 구버전 빌드가 v2 세이브를 v1로 오인하지 않는다. 다만 은퇴한 `WaveRewardType.BuffBurn`은 enum에 남아 있고, 해당 효과가 없는 현재 구성에서 기존 세이브의 레벨이 1 이상이면 `RunSaveManager.RewardEffects`가 LogError 후 복원을 실패시킨다. 이 실패는 전체 복원 실패 및 세이브 삭제로 이어질 수 있다 | v1→v2 마이그레이션에서 은퇴한 BuffBurn 데이터를 제거하거나, 복원 시 LogWarning 후 안전하게 건너뛴다
- **WL-174** | OPEN | SUNJIN·muchan918 | 보상 **3종 = 카드 3**이라 3택1이 성립하지 않고 런 간 빌드 분기가 사라졌다 — `WaveRewardPool.asset` 3종 × `SkillEffect.cs:13` `maxLevel = 3` = 소비 총량 9인데, 런 길이는 `MonsterSpawnWaveProvider`가 씬에 등록된 wave 에셋 순서로 정하며 `GameScene.unity:798-805`에 **7개**가 배선돼 있어 선택 기회는 7회다(`MonsterSpawnTable.csv`의 Round 1~10은 `Assets/Scripts` 전체 소비처 0건인 죽은 데이터다 — 런 길이 근거로 쓰지 말 것). `WaveRewardPool.cs:62`가 `Mathf.Min(count, remaining.Count)`라 **첫 웨이브부터 늘 같은 3장**이 뜨고, 한 효과를 3연속 찍으면 4웨이브부터 2장, 두 효과를 밀면 마지막엔 1장이 된다. 후보 0은 `WaveRewardController`가 정상 경로로 스킵하므로 크래시는 없다. GDD §5.6이 스스로 "종류가 (상한 + 3)보다 적으면 3택1이 무너진다"를 조항으로 갖고 있고 현재 그 조건을 크게 미달한다 | 보상 종류를 6종으로 확장(별도 이슈, 시드 파생 WL-008 동반) — 확장 전 임시 완화는 효과 상한 3→2(SO/인스펙터 수치, 코드 변경 없음)
- **WL-175** | PARTIAL | sunjin1222·N0WST4NDUP | 스타트맵 영역 판정은 #328에서 `StartMapTileRegistry.TryGetTileId` 역방향 조회로 통일됐고, 저장과 복원이 동일한 레지스트리를 사용한다. `TowerMapArea`도 공통 이름인 `MapArea`로 개명됐다. 남은 문제는 건설 가능 지형 조건의 중복이다 — `StartMapTileRegistry`는 `TileKind.Grass`를 등록 조건으로 사용하고, `TowerPlacer.IsBuildable`은 `TileKind.Grass && !Occupied`를 별도로 검사한다. 현재는 양쪽에 동기화 주석만 있어 건설 가능 지형이 확장되면 배치는 가능하지만 스타트맵 레지스트리에 등록되지 않는 타일이 생길 수 있다 | `BattleTile`에 Occupied와 무관한 공통 건설 가능 지형 프로퍼티를 추가하고, 레지스트리와 TowerPlacer가 해당 판정을 함께 사용한다
- **WL-176** | OPEN | n0wst4ndup·muchan918 | 스킬 조준 모드가 시전 1회로 강제 종료돼 충전(탄약) 연발 흐름과 어긋난다 — `MouseManager.cs:548`이 확정 직후 무조건 `CancelSkillTargeting()`을 부르고, 주석도 "연속 시전 불필요"(쿨다운 1발이던 #103 전제)로 남아 있다. #319로 감전이 쿨다운 대신 충전을 쓰게 되면서 최대 4발(Lv3) 연발이 설계 의도인데, 실제로는 매 발마다 스킬 버튼을 다시 눌러 조준 모드에 재진입해야 한다 — 이슈가 지키려던 건 "매번 새로 조준"이지 "매번 버튼 재클릭"이 아니다. 배치 쪽에는 같은 문제의 해법이 이미 있다(`PlacementRequest.KeepPlacingAfterConfirm`, `PlacementRequest.cs:18` / 소비 `MouseManager.cs:510` / `TowerPlacer.cs:253`)는데 `SkillTargetRequest.cs:6-12`에는 대응 필드가 없다. 조준 진입 시 `ClearSelection()` 미호출(WL-086 잔여)과 겹쳐 재진입 횟수만큼 UI 잔재도 반복된다 | `SkillTargetRequest.KeepTargetingAfterConfirm` 추가 + `!SkillManager.CanCast()`이면 자동 종료, `SkillButtonView`가 opt-in — MouseManager 소유자(n0wst4ndup) 사인오프 필요
- **WL-177** | OPEN | N0WST4NDUP | **Unity Behavior 그래프의 블랙보드 값은 에셋 안에 네 벌로 직렬화되며 자동 동기화되지 않는다** — 컴파일된 노드(`BehaviorGraph`, 런타임 실행 대상) / `BehaviorAuthoringGraph`의 노드 FieldModel이 가리키는 `LinkedVariable` 대상(에디터 재빌드 소스) / `RuntimeBlackboardAsset` / `BehaviorBlackboardAuthoringAsset` 네 곳이다. 블랙보드만 고치면 게임 동작이 안 바뀌고(런타임 바인딩이 블랙보드를 다시 읽지 않는다 — `BehaviorGraphAgent.Init` 후 실측), 컴파일된 노드만 고치면 에디터에서 그래프를 열어 저장할 때 authoring 사본에서 되읽어 되돌아간다. 실제로 `TankBossBehavior.asset`의 `P1_MinSpeed`가 WL-122의 15 → 25 변경에서 네 벌 중 두 벌만 갱신돼 오래 어긋나 있었고(PR#351 전수 조사로 발견, 네 벌 모두 3.75/10으로 정렬 완료), 같은 계통으로 `P1_DamagePerSpeedUnit`도 어긋나 있었다 | 블랙보드 값은 **Behavior 에디터에서만** 변경하고 스크립트/텍스트로 건드렸다면 네 벌을 전수 확인한다. 그래프 에셋을 스크립트로 편집하는 관행이 반복되면 같은 사본 불일치가 재발하므로, 편집 후 `TypedVariableModel` 이름별 값 전수 검사를 습관화할 것
- **WL-178** | OPEN | KIM-SUNGSOO | "이 타워가 겨누는 대상"의 단일 출처가 공격 액션을 가진 타워에만 성립한다 — AcquireTarget(Tower.cs:250)이 사거리를 AttackRange = Get<AttackAction>()?.Range ?? 0f(Tower.cs:574)에 고정해, Attack 전 필드가 0인 타워(히트스캔·오라 전용)는 항상 null이고 TowerTurretAim을 붙여도 포탑이 돌지 않는다. #387로 조준이 저작 가능한 정책이 되면서 표면이 넓어졌다 — TowerAsset.Targeting은 모든 타워 SO에 뜨는데 BeamAction.MaintainLocks(BeamAction.cs:143-155)는 정책을 보지 않고 OverlapSphereNonAlloc 반환 순서 그대로 잠근다(최근접조차 아니다). 빔 타워에 정책을 저작하면 경고도 로그도 없이 무시된다. 인게임 전환 행은 Has<AttackAction>()(Tower.cs:511)로 가렸지만 SO 필드는 가릴 수단이 없다 | AcquireTarget(float range) 오버로드를 두고 무인자 버전은 DisplayRange(Tower.cs:536)를 쓰게 한 뒤 BeamAction이 자기 Range로 호출해 빈 슬롯을 정책 순으로 채운다(프레임 캐시는 (frame, range) 쌍으로 무효화) — 임시 완화로 Targeting 저작 + AttackAction 부재 조합에 LogWarning
- **WL-179** | OPEN | N0WST4NDUP·muchan918 | **AudioMixer를 쓰지 않는 대가로 매니저를 거치지 않는 재생은 볼륨 제어를 전혀 받지 않는다** — 연결 고리가 `AudioManager.GetEffectiveVolume`(AudioManager.cs:210)을 호출부가 직접 곱하는 규약뿐이라 컴파일러도 리뷰도 누락을 잡지 못한다(믹서였다면 `outputAudioMixerGroup` 미배선이 인스펙터에 보인다). 현재 `SkillManager.cs:282`의 `AudioSource.PlayClipAtPoint`(스킬 착탄음)가 여전히 밖에 있다. **다만 안쪽 소비처는 크게 늘었다** — 공용 효과음 5본(버튼 클릭·패널 오픈·타워 설치·거절·주민 증가)이 `Sfx`/`SfxBank`를 거쳐 전부 `PlaySfx`/`PlaySfxExclusive`로 들어왔다. 즉 "밖에 남은 것"이 스킬 착탄음 하나로 좁혀졌고, 그만큼 **누락이 눈에 덜 띄게 됐다**(주변이 다 제어를 받으므로 착탄음만 안 먹는 것이 개별 버그처럼 보인다). #346 슬라이더가 먼저 붙으면 "SFX를 0으로 내려도 착탄음이 난다" = 설정이 안 먹는 버그로 보인다. 순서상 SFX 풀 이슈를 #346보다 먼저 태우는 편이 안전 | SFX 풀 경로 도입 + `PlayClipAtPoint` 이관 완료, 이후 "신규 재생 경로가 `AudioManager`를 거치는가"를 PR 체크 항목화
- **WL-181** | OPEN | 팀 | **정본 씬 편집이 `SceneWorkflow.md` §4의 스냅샷 절차를 실제로는 거치지 않는다** — §4 1항은 "`Branches/`에 번호 붙인 스냅샷 추가 → 정본 확정" 2단계를 요구하는데, #361은 `GameScene`/`TitleScene`에 `SoundCue` 계층(오브젝트 2~3개)을 직접 편집·저장하고 스냅샷을 남기지 않았다(직전 #357은 남겼다 — 현재 최고 번호 `Branches/GameScene_600`). 규칙대로면 컴포넌트 1개짜리 변경도 758KB급 씬 파일 사본을 매번 커밋해야 해서 실무와 괴리가 있고, 반대로 스냅샷 없이 정본이 갱신되면 그 이전 정본에서 뜬 개인 복사본이 §4 2항("정본에 그대로 덮어쓴다")을 실행할 때 **새 오브젝트가 에러 없이 사라진다**(스크립트는 남아 컴파일도 통과, 증상은 "언젠가부터 소리가 안 난다"뿐) | 팀이 §4를 "오브젝트 N개 이하 변경은 직접 편집 허용" 등으로 완화하고 문서에 명시하거나, 스냅샷을 실제로 매 PR에서 내도록 합의
- **WL-182** | OPEN | N0WST4NDUP·sunjin1222 | **씬 오브젝트의 `Start` 스냅샷과 세이브 복원의 순서가 보장되지 않는다** — `InGameCue.Start`(InGameCue.cs:63)가 `DayNightManager.CurrentPhase`를 읽어 초기 BGM 트랙을 고르는데, 그 값을 복원하는 `RunSaveManager`도 `Start`에서 돈다(RunSaveManager.Persistence.cs:52·82). 지금은 세이브 v1이 `Phase != Day` 복원을 아예 거부해(RunSaveManager.Progress.cs:79-84) 드러나지 않지만, 밤 세이브를 여는 순간 "밤에서 이어했는데 낮 BGM"이 확률적으로 난다. `DayNightManager`가 1일차 `OnDayStart`를 한 프레임 지연 발행하는 것(DayNightManager.cs:46-56)과 같은 축의 문제이고, 페이즈 스냅샷을 `Start`에서 읽는 다른 시스템이 생기면 함께 재현된다 | 밤 페이즈 복원을 여는 PR에서 복원 완료 통지(또는 `OnDayStart` 구독)로 초기화 순서를 닫고, 페이즈 스냅샷 소비자 목록을 SystemMap RunSave 접점에 명시
- **WL-183** | OPEN | muchan918 | **비용 행 프리팹이 둘로 갈렸고 구본을 지우면 참조가 조용히 끊긴다** — 실사용은 `Assets/Prefabs/UI/CostRow.prefab`(`BuildingInfoUI._costRowPrefab`·`CastlePanelUI._costRowPrefab` 둘 다 여기를 가리킨다)인데, 구본 `Assets/Imported/@NorthLand/Prefabs/UI/BuildingCostRow.prefab`(GUID `0a7a27080d36b3d4e84023ee59fc265d`)이 남아 `CastlePanel.prefab`·`Assets/Prefabs/UI/UICanvas.prefab`·`Scenes/Branches/*`·팀원 개인 씬 등 10곳 이상에서 아직 참조된다. 구본은 아이콘화 이전 구조(`Label`/`Amount` 텍스트 2개)라 그 경로로 인스턴스화되면 아이콘이 없는 옛 행이 나오고, 정리한다고 삭제하면 그 10곳이 Missing Prefab이 된다. 발단은 `Assets/Imported/`가 `.gitignore:162`에 걸린 것을 '팀에 전달 안 됨'으로 오판해 메인 저장소에 프리팹을 새로 만든 것 — 실제로는 중첩 저장소로 공유되는 경로다 | 구본 참조 10여 곳을 `CostRow.prefab`으로 이전 후 구본 삭제, 또는 반대로 구본을 아이콘 구조로 갱신하고 `CostRow.prefab`을 폐기(둘 중 하나로 단일화)
- **WL-185** | OPEN | n0wst4ndup·muchan918 | **자원 표기가 아이콘 단독이 되면서 이름 폴백이 사라졌고, 정본 명세는 아직 이름 열을 규정한다** — #345가 `ProductionLineView`(`:19-22,52,103,116`)·`BuildingCostRow`(`:30-38`)·`StoreOfferRow`(`:53-63`)·`StorePanelUI`(`:231`)에서 자원명 텍스트를 걷어내고 `ResourceAsset.Icon`만 그린다. 아이콘 미할당이면 `Image.enabled = false`라 **수량 숫자만 남아 어느 자원인지 알 수 없다**. ⚠️ **`game.resources.*`의 표시 소비처는 정확히 1개다**: `TowerTooltipView.BuildResourceLines`(`k_DefaultTable` + `ResourceData.NameKey`)가 유일한 표시 경로이고 — #445에서 `BuildCost(TowerAsset)`가 `BuildCost(TowerAsset, TowerRecipe)`로 갈리면서 자원 줄 생성이 이 함수로 분리됐고 **호출 지점이 둘로 늘었다**(배치 코스트 `TowerAsset.Cost` · 합성 추가 비용 `TowerRecipe.ExtraCost` — 후자는 현재 레시피 12종 전부 비어 실사용 0이지만 채우는 순간 같은 키를 탄다),, 후보로 보이는 `ManagementController.LineDisplayName`(`:303`)은 호출부가 `Debug.Log` 4곳(`:915-916`, `:923`, `:929`, `:974`)뿐이라 UI가 아니다. #376이 그 값을 **과자 테마 임시 이름**(비스켓·초코·설탕·별사탕)으로 바꿨으므로 현재 타워 툴팁 비용에 그 이름이 노출된다 — 정본 매핑은 `Docs/ManagementArea/Resources.md` §5.6이고 **확정안이 아니다**. 반면 정본 명세 `Docs/ManagementArea/Resources.md:145,154`는 여전히 "ProdRow 열 구성: **이름** · 지갑 · 주민수+버튼 · +n"이고 SystemMap §2에만 "어디서든 아이콘" 규약이 등재돼 둘이 어긋난다. 함께 걸린 축: `ResourceIconTable`(#345 신설 SO)을 존치할지, `ManagementController`가 이미 소유한 `_lineAssets`(`:67`, `LineDisplayName`/`LineKind`와 같은 계보)에 `ResourceIcon(ResourceKind)`로 흡수할지 미결 — 후자면 SO 하나와 Imported 프리팹 배선 하나가 사라진다. ko/en/ja 3로케일을 유지하면서 표기 소스를 텍스트→스프라이트로 옮긴 결정이라 Management(Resource)·Localization 소유자 판단이 필요하다 | n0wst4ndup 사인오프 3건: ① 아이콘 null 시 이름 폴백(또는 호버 툴팁) 도입 여부 ② `Resources.md` §5.5 열 구성 갱신 ③ `game.resources.*` 키 유지/폐기 — **폐기 시 `TowerTooltipView.BuildResourceLines`의 대체 표기(아이콘 또는 다른 키)를 반드시 동반할 것**(위 실측대로 소비처가 0이 아니므로 그냥 지우면 툴팁 비용 표기가 조용히 깨진다) + `ResourceIconTable` 존치 여부 결정
- **WL-186** | OPEN | muchan918 | **본진 업그레이드 효과 문구가 다른 건물 SO의 수치를 손으로 옮겨 적은 프로즈라 데이터와 링크가 없다** — `CastlePanelUI`가 `castle.effect.lv{n}` 키를 레벨로 조립하고 문구는 스트링 테이블에 있다. 그 문구가 주장하는 "나무꾼의 집·광산·농장 최대 레벨 +2 / 마법 연구소 +1"은 `woodcutter_house.asset`·`magic_lab.asset`의 `RequiredCastleLevel`에서 사람이 읽어 옮긴 값이라, 밸런싱으로 그 값을 조정하면 **문구가 조용히 거짓말을 한다**(두 값 사이에 컴파일러도 테스트도 없다). 미등록 키 노출은 `CastlePanelUI.UpgradeEffect`가 테이블 엔트리 부재 시 빈 문자열을 돌려주도록 해 막았으나(#365 리뷰 대응), 문구와 데이터가 어긋나는 축은 그대로 열려 있다 | 효과 키를 `BuildingAsset.Castle.UpgradeLevels[i]`에 `EffectKey` 필드로 붙여 레벨을 정의하는 데이터 옆에 두거나(`SkillVisualSet.FromLevel` 희소 매핑 선례), `OnValidate`에서 하위 건물 `RequiredCastleLevel` ↔ 문구 대조 경고
- **WL-187** | OPEN | n0wst4ndup | **픽셀레이션 미채택 후 `PixelationZoomBinder`가 씬에 잔재로 남아 설정 에셋 diff churn만 만든다** — #374가 `PC_Renderer.asset`의 `Flat Kit Pixelation`을 `m_Active: 0`으로 내려 미채택으로 종결했으나(문서 반영 완료: `VisualLookPipeline.md` §2 결정 8·§3.7·§8, `SystemMap.md` VisualLook 행), 정본 `GameScene`의 `PixelationZoomBinder` 컴포넌트는 그대로 남아 있다. `[ExecuteAlways]`(`PixelationZoomBinder.cs:25`) + `LateUpdate`(`:59-61`)라 **에디터에서 씬을 열어두기만 해도** 꺼진 피처가 참조하는 `Assets/Settings/FlatKit/PixelationSettings_PC.asset`에 `_PixelSize`를 계속 쓴다 — 스크립트 주석(`:23-24`)이 스스로 경고한 git diff churn이 이제 **아무 렌더 효과도 없이** 남는다. 컴파일도 콘솔도 조용하고, 증상은 "건드리지도 않은 에셋이 매번 modified로 뜬다"뿐이라 원인 추적이 어렵다 | 컴포넌트 비활성화(또는 제거) + `PixelationSettings_PC.asset` 존치 여부 판단(재활성 대비로 남길지). 재활성한다면 미니맵 카메라 제외 축이 **선행 조건**이다(`VisualLookPipeline.md` §3.6)
- **WL-188** | OPEN | sunjin1222·muchan918 | **카메라 이동 튜닝값이 정본 씬 직렬화에만 있고 이동 속도가 줌에 비례하지 않는다** — `CameraController2.cs:20-22` 코드 기본값(`moveSpeed 15` / `xBounds ±40` / `zBounds ±40`)과 정본 `GameScene` 실제값(`250` / `x -1450~1200` / `z -1050~550`)이 10~16배 어긋난다. #374가 감도를 50→250으로 올렸는데 그 값이 씬에만 있으므로, `SceneWorkflow.md` 절차대로 개인 씬 사본에서 작업하거나 새 씬에 컴포넌트를 붙이면 **코드 기본값이 적용돼 카메라가 사실상 안 움직인다**(#28 카메라 구도 확정 때 다시 만난다). 더해서 `MoveKeyboard`(`:185`)가 `moveSpeed * unscaledDeltaTime`을 그대로 쓰고 오쏘 사이즈를 보지 않아, 줌 범위 30~150(화면 높이 60~300 유닛, 5배 차)에서 **한 값으로 전 구간 감도를 맞출 수 없다** — 최대 축소 기준 250은 최대 확대에서 초당 4화면이 되어 과민하고, 확대 기준으로 낮추면 축소에서 답답해진다 | 이동 속도를 현재 오쏘 사이즈에 비례시키고(`MinZoomSize`/`MaxZoomSize`를 이미 공개하므로 추가 배선 불필요) 코드 기본값 3개를 정본 씬 값과 정합 — WL-023·WL-024(카메라 값이 씬·코드·룩에 흩어짐)와 같은 축이라 #28과 세트로 처리
- **WL-189** | OPEN | muchan918 | **정본 씬의 `BuildingInfoPanel`이 프리팹에서 unpack돼 있어 프리팹 수정이 게임에 반영되지 않는다** — `GameScene.unity`의 `BuildingInfoPanel`은 `isPrefabInstance = False`(대응 소스 없음)인 독립 GameObject인데, `Assets/Imported/@NorthLand/Prefabs/UI/BuildingInfoPanel.prefab`(GUID `53cbe448978caf141aede2796eb224fb`)은 폐기된 에셋이 아니라 `Assets/Prefabs/UI/UICanvas.prefab`·`Scenes/Branches/*` 15곳·팀원 개인 씬이 참조하는 현역이다. 즉 **프리팹을 고쳐도 정본 씬에는 안 내려가고, 씬을 고쳐도 프리팹·브랜치 씬에는 안 올라간다.** 구조는 이미 갈라졌다 — 이름 텍스트가 프리팹은 `Body/BuildingName`, 씬은 `Title/Txt_BuildingName`이고, 프리팹 쪽 `BuildingInfoUI`는 `_descriptionText`·`_costContent`·`_upgradeButton` 등 대부분이 **null 배선**이라 그 경로로 인스턴스화되면 패널이 반쯤 죽은 채로 뜬다. 컴파일도 콘솔도 조용하고 증상이 "고쳤는데 화면에 안 나온다"뿐이라 원인 추적이 어렵다(#375에서 프리팹만 고쳤다가 플레이에 안 나와 씬 작업을 다시 했다). WL-183(비용 행 프리팹이 `Assets/Imported` 구본과 `Assets/Prefabs` 신본으로 갈림)과 **같은 축** — 둘 다 `Assets/Imported` 프리팹과 메인 저장소 사본의 이원화다 | 정본을 한쪽으로 정하고 단일화: 씬 오브젝트를 프리팹으로 되돌리거나(프리팹을 씬 구조에 맞춰 갱신 후 재연결), 반대로 프리팹 참조 15여 곳을 정리. WL-183과 세트로 `Assets/Imported` UI 프리팹 소유권을 한 번에 결정하는 편이 낫다
- **WL-190** | OPEN | KIM-SUNGSOO·기획 | 타워 조준 정책 축(#387)이 GDD에 미등재이고 우선순위 사슬이 미고정이다 — 플레이어 조작이 하나 늘었는데(타워별 조준 순환) GDD §5에 조항이 없고, `Tower.Targeting`(`Tower.cs:220`)의 사슬(인게임 전환 > SO 저작값 > 기본)이 문서로 고정되지 않았다. 보상 특수효과(GDD §5.6)·합성(#195)이 나중에 조준을 건드리면 세 번째 소스의 자리가 없어 규칙을 다시 정해야 한다. ※ 공개 계약 4종과 NaN 규약은 #387에서 SystemMap §1·§2에 등재했고 `RouteDistanceTracker`의 0 반환도 같은 PR에서 해소했다 — **이 항목의 범위가 아니다** | GDD §5에 조준 축과 우선순위 사슬 한 문단 + 저작이 실제로 들어갈 때 `TowerRoster.md`에 타워별 조준 열 추가
- **WL-191** | PARTIAL | sunjin1222·muchan918 | 웨이브 미리보기가 페이즈를 모른다 — `NextWavePreviewHover.OnDisable`의 패널 정리와 `UIZOrder.md` §4의 낮 전용 자식 계약은 반영됐다. 잔여: 정본 `GameScene`에서 `PreviewHoverButton`·`NextWavePreviewPanel`의 부모가 아직 `UICanvas`라 밤에도 노출되며, 이때 `DayNightManager.CurrentWave`는 진행 중인 웨이브라 "다음 웨이브"라는 명명과 어긋난다. 같은 값을 쓰는 `ManagementPanelView.cs`는 이미 낮/밤 표기를 분기한다 | 정본 씬의 두 오브젝트를 `PhasePanelSwitcher._dayPanel`인 `TowerPanel` 자식으로 이동하고 낮→밤→낮 호버 잔류를 플레이 모드에서 확인
- **WL-192** | OPEN | KIM-SUNGSOO·기획 | 낮 종료 안내 층이 튜토리얼보다 먼저 비워졌다 — #337(영토 미확장)에 이어 #410(타워 미배치)이 상시 경고를 제거해 남은 것은 유휴 주민 하나뿐인데, 역할을 넘겨받을 튜토리얼(#271)은 OPEN·미배정이고 `Assets/Scripts`에 관련 코드 0건이다. 빌드 3→4→5로 두 번 이월된 이력이 있다(`Build4 빌드 노트.md:21`, `Build5 빌드 노트.md:23`) | **판단: 경고 복구가 아니라 #271로 해결한다** — 첫 배치 안내는 튜토리얼이 담당하고, 상시 경고로 되돌리지 않는다(#410 취지: 전략적 미배치를 방해하지 않는다). #410을 #271의 선행 종속으로 링크 + 빌드 6 범위 확정 시 #271 착수 확인. 재이월이 확정되면 그때 무방비(타워 0개) 한정 경고를 **전투 공간이 공개하는 상태값** 경유로만 복구(경영 UI가 `Tower`를 직접 보지 않는다)
- **WL-193** | PARTIAL | KIM-SUNGSOO·N0WST4NDUP | 루프 `Fire` 클립을 쓰는 모델 팩에서 발사 모션의 정지가 **코드 계약이 아니라 연출 필드 3개의 우연한 조합**에 걸려 있었다 — FattyPoly Part4(`MachineGun`/`Minigun`)는 `Fire` 클립이 `m_LoopTime: 1`이고 `Fire`에서 나가는 전이가 전부 조건부(`Idle`/`Reload`/`Remove`)라 무조건 탈출이 없는데(Part2는 exitTime으로 복귀 — **팩마다 저작이 정반대**), 유일한 정지 신호인 `TowerTurretAim.TargetLost`는 `turret` 미할당 시 `LateUpdate` 조기 반환으로 **영영 발행되지 않고**(`TowerTurretAim.cs`) 소비 측 게이트도 연출 필드다(`TowerAnimationVisual`). 어긋나면 경고도 예외도 없이 밤새 발사 모션이 반복된다. 모델 팩 교체가 정확히 `turret`을 놓치기 쉬운 작업이라 재발 가능성이 높다 | **대응 반영됨**: ⓐ `TowerTurretAim.PublishesTargetLost`(읽기) 노출 ⓑ `TowerAnimationVisual`에 명시적 정지 경로 `idleTrigger` 추가(두 컨트롤러에 `Idle` Trigger + `Fire → Idle` 전이가 이미 저작돼 있었으나 코드에서 닿을 수 없었다. 장전과 함께 켜지 않는 이유는 `HandleTargetLost` 주석) ⓒ `Awake`가 「`turret` 미할당」과 「루프 발사 클립 + 정지 경로 없음」을 저작 시점에 경고(`WarnOnLoopingFireWithoutStop`) ⓓ `SystemMap.md` §2에 `TowerAnimationVisual` 등재 + `TowerTurretAim` 계약에 `TargetLost` 절 추가 ⓔ `TowerAddGuide.md` §3.3 프리팹 표·함정 ④ + §5 증상 3행. **잔여**: 기존 타워 21종에서 오탐 경고가 나지 않는지 에디터 콘솔 1회 확인(이 환경에 unity-cli 없음) + 컴포넌트 소유자 KIM-SUNGSOO 사인오프. 기존 프리팹은 의도적으로 재배선하지 않았다 — 램프업·개틀링은 `Reload` 마무리 연출이 정지까지 겸하고 실제로 종료된다(`Minigun_Reload`는 `m_LoopTime: 0` + 무조건 exitTime 전이)
- **WL-194** | PARTIAL | n0wst4ndup | FlatKit 변환 원장과 사본 머티리얼이 서로 다른 저장소에 있어 낡은 체크아웃이 원장을 조용히 훼손했다 — 원장은 이 저장소 `Assets/Settings/FlatKit/FlatKitConversion.json`인데 사본은 `Assets/Imported/@NorthLand/Materials/FlatKit`(중첩 repo)에 생성된다. 구 `GetOrCreateConverted`는 기록된 GUID가 로드되지 않으면 **경고 없이 항목을 삭제하고** 새 GUID로 사본을 다시 만들었다. **미래 리스크가 아니라 이미 발생한 손상이다**: 사본 132개 vs 원장 120건 → **미등재 사본 12개**, 그중 **5개는 프리팹이 참조 중**(`FK_Maine_Material`은 보스 프리팹 3종 + 벤더 프리팹 5종 = 8곳인데 원장에 `Maine` 문자열이 0건). `Revert`는 원장으로 역매핑을 만들어 미등재 사본을 `continue`로 건너뛰므로 **이 8곳은 원본 복원이 불가능하다.** `FK_FK_Color_1`·`FK_FK_Color_2_1`(이중 접두사 = 사본을 다시 변환한 흔적)이 직접 증거다 | **대응 반영됨**: ⓐ `GetOrCreateConverted`가 사본 로드 실패 시 삭제 대신 **경고 후 `null` 반환**(슬롯 미변경 → 프리팹 참조 보존) ⓑ 같은 삭제를 메뉴 「5. 끊긴 기록 정리」로 옮기고 "Imported 최신 확인" 대화상자로 게이트 ⓒ 메뉴 「4. 원장 점검」 추가 — 끊긴 기록·미등재 사본을 **참조 개수까지** 진단만 하고 바꾸지 않는다 ⓓ `SystemMap.md`에 Part4 모델 동기화 계약 등재. **청소 완료(2026-08-18)**: ⓔ 참조 중이던 미등재 사본 4종의 원본 매핑을 원장에 되메움 — 이름 추정이 아니라 **①`FK_` 접두사+`Sanitize` 규칙 ②알베도 텍스처 GUID ③소비 프리팹** 3중 일치로 확정했다(`Color 1`→GrassTile 1 / `Color 2`→GrassTile 2 / `Color 3`→GrassTile 3 / `Maine_Material`→보스 3종+벤더 5종). 이것으로 복원 불가 참조가 11곳→1곳이 됐다 ⓕ `FK_Palette256 1`(MissileTower 5슬롯)은 원본이 이미 `FK_Palette256 2`로 매핑돼 있던 **중복 사본**이라 프리팹을 원장 등재 사본으로 재배선한 뒤 삭제 — 남긴 쪽이 템플릿과 정확히 일치하고(`disabledShaderPasses`·`_OutlineColor`) 형제 타워 `TwinMissileTower`가 이미 그것을 쓰고 있어 방향이 정본과 같다 ⓖ 참조 0인 잔재 사본 7종 삭제(`FK_Eff_Range00`·`FK_Eff_Range01`·`FK_FK_Color_1`·`FK_FK_Color_2_1`·`FK_Lit 1`·`FK_Palette256`·`FK_Palette256 3`). **결과: 디스크 사본 125 = 원장 125, 끊긴 기록 0 · 미등재 사본 0 · source 중복 0 — "원본 1개당 사본 1개" 불변식이 처음으로 성립하고, 삭제한 8개 GUID의 잔존 참조도 0건이다.** | **잔여 2건**: ① **PR 브랜치에 교정을 되돌리는 커밋이 들어 있다(이번에 실제로 발생)** — `feat/rampup-gatling-tower-assets`의 `5347885`(Chore: FlatKit Palette256 머티리얼 변환 등록)는 **내용 전체가 이 1줄을 `4b52853f…` → `f3ad0778…`(`FK_Palette256 3`, 삭제된 사본)로 바꾸는 것**이다. 그래서 정상 상태 위로 그 브랜치를 rebase·merge하면 그 커밋이 replay되며 매번 되돌아간다 — 실측: `b514afa`(정합 0/0, `4b52853f…`) 위로 rebase하자 브랜치 팁이 `f3ad0778…`로 회귀했고, 그 뒤 교정 커밋을 다시 얹어야 했다. 줄끝이 LF↔CRLF로 흔들리는 것도 이 브랜치 blob의 성질이다. **근본 해결은 `5347885`의 그 줄을 `4b52853f…`로 고치거나 그 커밋을 드롭하는 것**이고, 그러지 않으면 rebase마다 교정 커밋이 필요하다 ② 에디터 refresh 후 메뉴 「4. 원장 점검」 1회 실행해 「어긋난 지점 없음」 확인(이 환경에 unity-cli 없어 미실행) — 사본 8개를 에디터 밖에서 지웠으므로 Unity의 AssetDatabase가 낡아 있다. MissileTower 외형 1회 눈확인도 함께
- **WL-196** | OPEN | KIM-SUNGSOO | 저장소 간 참조 방향 계약이 원장에 없이 코드 주석에만 있다 — 연출 수치를 프리팹 컴포넌트에 두면 Imported 프리팹이 본 저장소의 MonoBehaviour를 참조해 머지 순서가 모델·에셋과 **반대**(본 저장소 선행)가 되고, Imported만 먼저 받으면 Missing Script 상태에서 재직렬화되며 배선이 조용히 사라진다. #426은 빔 단계 연출을 프리팹 컴포넌트에서 `TowerAsset.BeamStage`(SO)로 이관해 회피했고(`a5ed972`), `BeamAction.cs:38-40`과 `SystemMap.md`가 이 결정을 "새 연출 값은 이 선례를 따를 것(WL-196)"이라며 선례로 인용한다. **그런데 그 번호가 원장에 없었다** — 본 항목이 그 인용 대상이다 | 본 항목 등재로 인용 해소. 이후 리뷰에서 감시할 것: 연출 수치(`[SerializeField]` 굵기·색·오프셋 등)가 SO가 아니라 프리팹 MonoBehaviour에 추가되는 변경
- **WL-197** | PARTIAL | KIM-SUNGSOO·기획 | `soda_tower`가 밸런스 표와 어긋난 채 잠정 상태다 — 2티어 승격은 #441에서 실행됐다(SO **27 / 2.2 / 30** · Rarity 1 · `Recipe_SodaTower` = 초코×1 + 캐논×1 신설). 그런데 `CombatBalance.md` §6.2·§3.2 두 행은 여전히 이전 값(**16 / 0.85 / 12**)이라 **소다는 계속 검산 대상에서 제외해야 한다** — WL-172(몬스터 `EnemyAsset` 6종 속도 ×0.6 · HP ×1.5)가 §6.2 표를 검산 기준으로 대기 중인데, 소다 행만 어긋나는 것을 몬스터 값 탓으로 오인하면 원인이 갈리지 않는다. 잔여 문제 둘: ① `incendiary_cannon`(27 / 1.5)과 사거리·간격이 완전히 동일했던 문제는 간격이 갈리면서 해소됐으나 **사거리 4.5타일이 2차 최장인 것은 그대로**이고 `rampup`이 25(4.17타일)로 올라 같은 축이 하나 늘었다 ② **CC 커버리지가 §6.2 표에 반영되지 않는다** — 스턴은 대상을 멈추므로 소다 사거리 안의 **모든 타워**의 체류가 함께 늘어나는데(가동률 u에서 체류 배수 `1/(1−u)`, 소다 1기 31.8% → 1.47× · 2기 63.6% → 2.75×), §6.2가 "1회 통과 킬 수"를 화력 단위로 쓰므로 소다를 겹쳐 깐 구간에서 표의 입력이 최대 2.75배로 벌어진다. 소다가 판에 주는 실제 기여는 DPS가 아니라 이 배수인데 표에는 자리가 없다 | 명중 방식(`HomingFlight` 유지 vs `BallisticFlight`) 확정 → §6.1 환산으로 눈금 재배정 → §6.2·§3.2 두 행과 각주 갱신. **CC 커버리지 계수를 §6.2에 한 줄로 세울 것** — `체류 = 사거리 × 1/(1−u)`, `u = min(지속/간격 × 대수, 지속/(지속+창))`을 명시하고 소다를 검산에서 빼는 대신 다른 타워의 검산에 이 계수를 곱할 조건을 적는다
- **WL-198** | OPEN | n0wst4ndup | 되돌리기 버튼 클래스 이름이 대상과 어긋난다 — `Assets/Scripts/UI/TowerPanel/TowerUndoButtonView.cs`가 #444 이후 타워뿐 아니라 건물 업그레이드·주민 증축까지 되돌리는데 이름·폴더가 `TowerPanel/Tower*`로 남았다(클래스 주석에 잔재임을 명시). 이름을 바꾸려면 파일·클래스를 함께 옮겨야 하고, 그러면 정본 씬·프리팹 10여 곳(`UICanvas.prefab` 외 `GameScene_*` 5종, 개인 씬 4종)의 `m_EditorClassIdentifier`가 어긋난 채 남는다 — GUID로 해석되므로 동작은 하지만 재직렬화 전까지 diff가 지저분하고, 그 사이 스크립트를 찾는 사람이 폴더를 보고 "전투 UI 전용"으로 오해한다 | 에디터를 띄운 상태에서 `UndoButtonView`(위치 `Assets/Scripts/UI/`)로 rename → `unity-cli reserialize`로 참조 에셋 재직렬화 → 같은 PR에 에셋 diff 포함. 씬 병합 충돌을 피하려면 주간 정리(`SceneWorkflow.md`) 직후가 싸다
- **WL-199** | OPEN | n0wst4ndup | `KeyboardManager`의 바인딩 목록이 플레이 세션 사이에 리셋되지 않는다 — `KeyboardManager.cs:53`의 `s_bindings`가 static이고 `[RuntimeInitializeOnLoadMethod]` 리셋을 **의도적으로 두지 않았다**(등록 쪽도 같은 훅이라 초기화 순서가 보장되지 않아, 리셋이 그 프레임의 등록을 지울 수 있다 — 근거는 같은 파일 주석). 중복 등록은 `Bind`의 동등성 검사가 막지만, **인스턴스 메서드를 `Bind`하면 파괴된 오브젝트를 붙든 채 남는다**(`Unbind` 누락 시). 오늘은 등록이 `UndoRequest.Submit`(static, 앱 수명) 하나뿐이라 무증상이고, 단축키가 늘면서 MonoBehaviour가 자기 메서드를 등록하는 순간 조용히 새기 시작한다 — 증상은 "씬을 다시 로드한 뒤 단축키가 두 번 실행된다" 또는 파괴된 대상 접근 예외다 | `Bind`가 `MonoBehaviour` 소유자를 함께 받아 파괴를 감지해 자동 해제(`Instance != null` 검사)하거나, 씬 스코프 바인딩 전용 진입점을 따로 열 것. 단축키 2번째 사례가 들어올 때 함께 닫는다
- **WL-200** | OPEN | n0wst4ndup·muchan918 | 이 원장의 **코드 근거가 하드코딩된 줄 번호**라 커밋마다 조용히 썩는다 — 2026-08-22 전수 감사(255개 참조) 결과 정확 87건, 나머지는 드리프트·오참조였다. 표본 실측: `keepPlacing`은 48→**69**(+21), `:228`→**275**(+47), `BuildingInfo.cs:24`→**31**(+7), `StatusEffectHandler.cs:62`→**67**(+5), `SkillButtonView.cs:83·92`→**123**(+31~40). 삭제·개명된 파일을 가리키는 참조도 10종 있었다(`Territory*` 6종은 #337 삭제, `DebuffAuraBehaviour`→`DebuffAuraAction`·`AttackBehaviour`→`AttackAction`은 #164 개명, `OutlineSmoothMesh*` 2종 삭제). 리뷰 봇이 이 원장을 매 리뷰의 입력으로 읽으므로, 틀린 줄 번호는 **없는 문제를 있다고 하거나 있는 문제를 놓치게** 만든다 — 감사 없이는 어긋난 것과 멀쩡한 것을 구분할 수 없다는 게 실제 비용이다 | 근거 표기를 **심볼 인용**(`ManagementController.TrySpend`, `TowerPlacer.KeepPlacingAfterConfirm`)으로 전환 — grep으로 항상 찾아지고 개명은 컴파일이 잡는다. 줄 번호가 꼭 필요한 곳(에셋 YAML 등)만 남기고, 남긴 것은 PR 리뷰 워크플로에 존재 검사 스크립트를 붙일 것. 감사 도구 초안은 이번 감사에 쓴 것이 있다(재사용 가능)
- **WL-201** | OPEN | n0wst4ndup | `KeyboardManager`의 보조키 판정이 **전역 정확 일치** 단일 정책이다(`KeyboardManager.cs:141` `binding.Modifiers != held`). Ctrl+Z와 Ctrl+Shift+Z를 가르는 데는 옳지만, `KeyModifier.None`으로 등록한 바인딩은 Ctrl·Shift·Alt 중 하나라도 눌려 있으면 발화하지 않는다. 이 프로젝트에서 Shift는 그룹 선택 토글로 쥔 채 조작하는 키라(`MouseManager.cs:353-354`) 무보조키 단축키가 "가끔 안 먹는" 증상이 되고, 원인이 이 한 줄이라는 것이 증상에서 멀다. 지금은 등록이 Ctrl+Z 하나뿐이라 무증상이다 | `Bind`에 바인딩별 정확 일치 여부 인자를 더해 무보조키 단축키는 "요구 보조키 포함, 나머지 무시"를 고를 수 있게 — **두 번째 바인딩이 붙을 때** 함께 정한다(지금 넣으면 소비처 없는 코드 경로가 생긴다). 이관 후보인 ESC 2건(`SettingUI.cs:53`, `MainMenuUI.cs:128`)이 그 시점의 첫 대상이다
- **WL-202** | OPEN | n0wst4ndup | **아이콘 칸이 프리팹·컴포넌트 두 벌로 갈렸고 원본 레이아웃 변경이 한쪽에만 따라온다** — 배치 팔레트 칸(`TowerButton.prefab` + `TowerButtonView`)과 정보 패널 합성 후보 칸(`TowerTargetSlot.prefab` + `TowerMergeTargetSlot`)이 **복제 관계**다: 후자는 전자를 복제해 `Button`·`TowerButtonView`·`TowerLockOverlay`·배너 서브트리를 떼고 만든 것이고, 두 컴포넌트의 `Set`은 아이콘 채우기 본문이 사실상 같다(미할당이면 슬롯을 끄는 `enabled = icon != null` 규약까지). 복제라서 팔레트 칸의 테두리·배경·크기를 바꾸면 후보 칸은 **따라오지 않고 컴파일러도 리뷰도 그것을 잡지 못한다**. 컴포넌트를 나눈 것 자체는 의도다 — `SetLocked`·해제 연출이 팔레트의 **해금** 개념과 한 몸이라 잠금이 없는 정보 패널로 끌고 오지 않았다(`TowerMerge.md` §8.5). 지금 관측되는 비용은 0이고(#445 시점에 두 칸이 같은 모양으로 출하됐다) 다음 레이아웃 변경이 첫 청구서다. 같은 복제 함정이 이미 한 번 오독을 만들었다 — 후보 칸의 배너 제거가 **의도**인데 자동 리뷰가 "복제 과정에서 빠진 배선 누락"으로 읽었다(#445 리뷰, 계약을 §8.5·SystemMap §2·컴포넌트 주석 3곳에 명문화해 닫음) | 아이콘+테두리만 갖는 공용 슬롯 뷰를 두고 `TowerButtonView`가 그것을 참조해 `SetLocked`만 더하는 컴포지션으로 전환 + 후보 칸 프리팹을 `TowerButton.prefab`의 **Variant**로 전환(원본 레이아웃 변경이 자동으로 따라오고 배선 누락이 구조적으로 불가능해진다). **팔레트 칸 레이아웃을 다시 손대는 작업과 같은 PR에서** 하는 것이 가장 싸다 — 그때가 아니면 순수 리팩터라 씬·프리팹 diff만 남는다
- **WL-203** | OPEN | KIM-SUNGSOO·기획 | **성장형 타워의 획득 경로가 GDD와 갈렸다** — `Docs/GDD.md` §5.8.1이 "성장형 3종은 전부 합성 전용이다"를 조항으로 두는데, #441이 `killstack_tower`(사냥꾼)를 직접배치 1차로 내렸다(Rarity 0 · Cost 나무 40 · UnlockWave 5, `Recipe_KillStackTower` 폐기). §5.8.1은 자가 성장을 합성과 **독립적으로 스택되는 두 번째 축**으로 세운 절이고 "전부 합성 전용"이 그 축의 진입 비용을 정의하는 조항이라, 1차 타워가 성장 축을 갖는 순간 초반 구간(웨이브 5부터 나무 40)에서 두 축이 독립이라는 전제가 성립하지 않는다. `TowerRoster.md`는 갱신됐고 GDD는 그대로여서 **두 문서가 반대를 말한다** — 나중에 GDD를 근거로 밸런싱을 잡는 사람이 §5.8.1을 그대로 믿으면 어긋난다. 방향 자체는 설득력이 있다(램프업 재료를 "성장 축을 공급하는 재료"로 바꾼 논리는 §5.8 "재료가 결과를 설명한다"는 족보 원칙에 오히려 더 맞는다 — 개틀링+헤이스트에는 성장이라는 축 자체가 없었다). 문제는 결정의 등재 위치다 | §5.8.1 "획득 경로" 불릿을 새 방향("성장형은 합성 전용을 기본으로 하되, 성장 메커니즘을 재료로 공급하는 1차 타워를 둘 수 있다")으로 개정하거나 GDD §8 미확정 항목으로 승격 후 사인오프(WL-012·WL-156 연동)
- **WL-204** | OPEN | KIM-SUNGSOO·기획 | **램프업 둘째 성장 축의 수치가 사인오프 없이 들어갔다** — 원장형 램프가 한 스택으로 두 스탯을 올릴 수 있게 확장되고(`Ramp.SecondaryStat`/`SecondaryPerStack`, 원장이 소스당 modifier 묶음을 들고 있어 채번 확장 없이 됨), `rampup_tower`가 공속 +8%/스택에 **피해 +5%/스택**(상한 공유 10 → ×1.50)을 얻었다. 기계 구조는 안전하다 — 미저작 기본값이 0이라 나머지 타워 20종은 거동 불변이고, 같은 축 중복·주 축 미저작 두 경우를 `OnValidate`가 잡는다. 문제는 수치다: **+5%는 구현자 추정**이고 만렙 DPS가 45.0 → 67.5로 올라 사냥꾼 만렙(20.9)의 **3.23배**가 된다(종전 2.16배). 사냥꾼은 재료인 동시에 직접배치 1차이고 그 정체성이 피해 성장 하나뿐이라, 램프업이 같은 축을 얻은 것 자체가 티어 관계를 다시 흔든다(피해 상한을 ×1.50으로 사냥꾼의 ×1.64보다 낮게 둔 것이 그 방어인데 **여유가 8.5%뿐**이라 두 값은 함께 움직여야 한다). 규약 ②(합성 결과 킬 수 ≤ 재료 킬 합 × 1.3)는 코드가 잡지 못해(WL-171) 손 대입이 필요하다. `CombatBalance.md` §6.2의 `killstack`·`rampup` 두 행은 옛 배율(×2.2 / ×1.8)로 계산돼 있어 검산 대상에서 제외해 뒀다 | 램프업 둘째 축 수치 사인오프 → 규약 ② 손 대입 기록 → §6.2 두 행 재계산 후 제외 각주 제거. ⚠ 둘째 축은 주 축과 스택을 공유하므로 §11의 "통과 한 번에 10스택 미완주"가 두 축에 함께 걸린다 — 실전 배율을 도달 스택 기준으로 먼저 확정할 것
- **WL-205** | OPEN | KIM-SUNGSOO | **자폭 저작(#453)에 짝 검사가 없어 두 조합이 무증상으로 실패한다** — ① `SelfDestruct.Enabled`가 켜졌는데 `Damage`가 0이면 자폭병이 본진까지 달려가 **0 피해**를 주고 사라진다(예외·경고 없음, 증상은 "자폭병이 있는데 본진이 안 깎인다"뿐이라 원인에서 멀다) ② 자폭병의 `Melee.Stat.AttackDamage`는 `Enemy.cs:514`의 자폭 분기가 평타 경로를 가로채 **영구히 읽히지 않는 값**이라, 나중에 누가 "0인 게 이상하다"며 채워도 아무 일도 일어나지 않는다. `EnemyAsset`(`EnemyAsset.cs:62` `SelfDestructFields`)에는 `OnValidate`가 아예 없고, 같은 유형의 무증상 조합을 `TowerAsset.cs:145`의 `OnValidate`가 이미 잡고 있다 — 선례가 있는데 안 쓴 자리다 | `EnemyAsset.OnValidate` 신설 + 위 두 조합 경고, SO 저장 시 경고 0 확인
- **WL-206** | OPEN | KIM-SUNGSOO·기획 | **「낮 시작 = 본진 회복」이 문서에만 있고 구현이 없으며, 밸런스 규약이 그 반대를 전제한다** — `Docs/GDD.md:125`(§4.1 표)와 `SystemMap.md` §4 팀 계약 5번은 낮 시작 시 본진 자동 회복을 명문화했으나 `PlayerBase`에 회복 API 자체가 없다(`TryRestoreCurrentHp`는 세이브 복원 전용이고 0 이하·최대 초과를 거부한다). 반대로 `CombatBalance.md:604`는 **"본진 HP는 회복 경로가 없다"**를 전제로 규약 ④(웨이브당 자폭 총량 ≤ 본진 HP×0.5)를 세웠다. #453으로 자폭병이 실제 스폰되면서 이 미결정이 처음으로 수치가 됐다 — W7·W11·W15 총 11마리 × 20 = **220 = 본진 HP 200의 110%**라, 규약 ④의 "웨이브당"이 **밤마다 리셋되는 단위**인지 **런 전체 누적 예산**인지에 따라 자폭 마릿수의 정답이 갈린다(W11·W15는 현재 4마리 — 경계값 5에서 한 칸 아래로 내려둔 상태이고, 상한대로 5마리면 260 = 130%가 된다) | 회복 도입 여부 팀 결정 → GDD/CombatBalance 중 한쪽 정정 + 규약 ④ 재산정(`CombatBalance.md`의 「본진 회복 경로」 미해결 항목과 세트). 결정 전까지 W11·W15를 4마리로 두는 것이 되돌리기 싸다
- **WL-207** | OPEN | KIM-SUNGSOO·muchan918 | **`EnemyType`이 세 가지 역할을 겸직한다** — ① 스탯 블록 선택(`Enemy.Stat`의 `switch`) ② 근접/원거리 공격 경로 선택(`Enemy.TryAttack`) ③ 보스 술어의 유일한 근거(`Enemy.IsBoss` = `EnemyType == Boss`). 그래서 **원거리 보스가 들어오면 `EnemyType.Ranged`가 되어 `IsBoss`가 false로 조용히 뒤집히고**(보스는 웨이브 HP 배율 제외 대상이라 `MonsterSpawn.SpawnPrefab`이 배율을 이중으로 곱한다), 새 몬스터 축을 추가할 때마다 값을 늘리면 스탯 블록·`EnemyAssetEditor`·`TableImporter`·CSV 파싱 네 곳이 함께 갈라진다. #453의 자폭 축을 `EnemyType`이 아니라 직교 필드(`EnemyAsset.SelfDestruct`)로 뺀 근거가 이것이다 | 보스 여부를 authored 플래그로 분리 + 스탯 블록/공격 경로 선택을 `EnemyType`에서 떼어낼지 합의(#26 오너). **주의**: 이 축을 WL-176(스킬 조준 모드)으로 오참조한 자리가 `SystemMap.md:208`에 오래 있었고 #453이 2곳을 복제했다 — 이 번호로 정정 완료
- **WL-208** | OPEN | N0WST4NDUP | **팩 클립을 `.meta`째 복사해 GUID가 뒤바뀌었다** — `@NorthLand/Sound/Feedback/SFX_Castle_ResidentIncrease.wav`만 `.wav`가 아니라 `.meta`까지 함께 복사돼 GUID가 중복됐고, Unity가 충돌을 풀며 **팩 원본**(`CartoonSFX/UpdateOne/Miscellaneous/SFX_MusicalFanfare_Happy_01.wav`)에 새 GUID를 발급했다. 결과적으로 팩 데모 씬이 참조하던 팬파레가 우리 복사본을 가리킨다. **바이트가 같은 파일이라 청감으로는 드러나지 않아** 팩을 갱신하거나 우리 복사본을 옮길 때 비로소 끊긴다(WL-040과 같은 축 — Imported는 별도 저장소라 본 저장소 diff에 안 보인다). 나머지 6본은 `.wav`만 복사돼 깨끗하다. 절차는 `Docs/Core/AudioManager.md` §4.5.1 | 복사본 `.meta` 삭제 → 새 GUID 발급 → 팩 원본 `.meta` 되돌림 → `SfxBank.asset` 재배선(GUID가 바뀌면 참조가 끊긴다). 재발 방지는 "팩에서 클립을 가져올 때 `.wav`만 복사"를 에셋 반입 규약에 명문화
- **WL-210** | OPEN | n0wst4ndup | **「커서 밑 경영 공간 지면은 어디인가」의 유일한 구현이 연출 클래스 private에 갇혔다** — `ResidentCarryVisual.ResolveGroundPoint`/`TryMarchToNavMesh`/`CaptureMarchRange`가 이 프로젝트 고유의 두 제약을 푼다: **① 경영 공간 지면에는 콜라이더가 없어**(씬 콜라이더는 전투 타일 90·건물 6·Ground 12뿐) 물리 레이캐스트로 못 짚고, **② NavMesh가 y −18.6~+28.0에 여러 층**이라 높이를 하나로 가정하면 45° 카메라에서 높이차 1당 수평 1씩 어긋나 다른 층에 놓는 것이 아예 불가능해진다(#466에서 실제로 27유닛 밖을 짚어 반송됐다). **탐색 반경 확대는 해결이 아니다** — 반경 16이면 겨눈 곳에서 30유닛 떨어진 다른 층을 집어 온다(실측). 소비처가 이미 셋 예정이다: §8.3 거절 피드백(놓은 자리 표시) · 들린 주민 발밑 표시 · 건물 배치(#27, WL-048). 사본이 생기면 위 함정이 다른 자리에서 그대로 재현되고, WL-034가 좌표 변환식 사본에서 겪은 재발 패턴과 같다. ⚠ **`MouseManager`로 올리는 것은 답이 아니다** — 그 클래스는 "생산 건물인가는 매니저가 답할 수 있는 질문이 아니다"를 원칙으로 명시하고 도메인을 모르는 것을 계약으로 삼는데, NavMesh는 경영 도메인 지식이다. 지금은 소비처가 하나뿐이라 추출하지 않았다 | **두 번째 소비처가 붙을 때** `ManagementGroundProbe`(경영 공간 소유의 정적 서비스)로 승격하고 훑기 범위 캐시도 그쪽이 소유한다. 카메라는 `MouseManager.ActiveCamera`, 커서는 `PointerPosition`을 계속 경유한다(입력 단일 창구 계약 유지)
- **WL-211** | OPEN | muchan918·N0WST4NDUP | **새 String Table 컬렉션의 Addressables 등록 누락을 자동으로 잡는 장치가 없다** — PR#489의 `NorthLand_Tutorial` Shared Data·ko-KR·en-US·ja-JP 4에셋이 처음에는 각 Localization 그룹에 미등록인 채 올라와, 다른 체크아웃과 플레이어 빌드에서 테이블 전체를 로드하지 못할 상태였다. PR 후속 수정으로 그룹 4곳에 등록했지만, 에디터 로컬의 그룹 변경을 커밋에서 빠뜨려도 코드·테이블 키 전수 검사·컴파일이 모두 통과하는 재발 경로는 남아 있다 | 새 테이블 추가 체크리스트에 Addressables 그룹 4곳 동반 커밋을 명시 + 디스크의 Shared/locale 테이블 수와 각 그룹 엔트리를 대조하는 EditMode 검증 추가
- **WL-213** | OPEN | n0wst4ndup·미정(전역) | **아웃라인 머티리얼 값 2개를 위해 이미 저장소에 있는 42MB 폰트 에셋을 통째로 복제했다** — `Assets/TextMesh Pro/Fonts/Pretendard-Outline SDF.asset`(Static, 4096², 글리프 2484, 43.7MB)은 기존 `Pretendard-Regular SDF.asset`(43.7MB, 2024-08-17부터 커밋됨)의 **재-bake 사본**이다. 실측: 두 파일의 `m_PointSize`(87)·`m_AtlasRenderMode`(4169)·`m_AtlasPadding`(5)·`m_AtlasWidth/Height`(4096)·글리프 수(2484)·아틀라스 텍스처 페이로드가 동일하고, 텍스트 라인 차이는 `m_MarkToBaseAdjustmentRecords` 구간 한 곳뿐이며 **내부 fileID까지 같다**(텍스처 `748470775162137925`, 머티리얼 `3648609920881002622`). 결정적으로 사본에 딸린 머티리얼의 이름이 그대로 **`Pretendard-Regular SDF Material`**이고, 실질 차이는 `_OutlineWidth: 0.4` + `_OutlineColor: 검정` 둘뿐이다(TMP 아웃라인은 아틀라스에 굽는 값이 아니라 SDF에서 런타임 렌더되는 **머티리얼 속성**이므로, 패딩이 같은 Regular SDF로 동일 폭 아웃라인이 그대로 나온다). 소비처는 `EnemyStateUI.prefab`의 겹 수 라벨 TMP 3개뿐(`:613-614`·`:1079-1080`·`:1216-1217`). WL-084(동적 아틀라스 churn)·WL-041(고아 폰트 사본)과 실패 모드는 다르지만 같은 축(폰트 SDF 자산 거버넌스)의 세 번째 사례다 | **머티리얼 프리셋 전환**(에디터 필요, ~5분): `Pretendard-Regular SDF` 우클릭 → Create > Material Preset → `_OutlineWidth 0.4`/`_OutlineColor 검정` → 프리팹 TMP 3개의 `m_fontAsset`을 `abd3dbb38214e2542946d1e6ecbad192`(Regular)로, `m_sharedMaterial`을 새 프리셋으로 재바인딩 → `Pretendard-Outline SDF.asset` 삭제(선례: `Assets/TextMesh Pro/Resources/Fonts & Materials/LiberationSans SDF - Outline.mat`). 병행: WL-084 정책 문구에 "기존 폰트 에셋의 머티리얼 변형은 프리셋 `.mat`으로, 재-bake 금지" 기준 추가
- **WL-214** | OPEN | sunjin1222·n0wst4ndup·muchan918 | 합성 재료 표시의 집계 경로가 둘로 갈려 `TowerMerge.md` §6·§8.2의 단일 출처 규약을 벗어난다 — 툴팁은 `TowerFusionMatcher.BuildRequired(recipe)`로 TowerID별 요구량을 집계한 뒤 `타워명 x수량` 텍스트를 내지만(`TowerTooltipView.cs:318-345`), 도감은 `recipe.Materials`를 직접 순회하고 `Count`만큼 아이콘을 반복 생성한다(`FusionTowerCodexUI.cs:277-310`). 현재 `BuildRequired`는 `Tower == null`·`Count <= 0`만 제외하고 TowerID가 빈 문자열이거나 중복돼도 걸러내지 않는다. 서로 다른 재료 타워가 같은 빈 ID를 가지면 딕셔너리의 한 키로 합산돼 툴팁의 이름·수량이 섞일 수 있지만, 도감은 Tower 오브젝트 참조별로 아이콘을 그려 이 충돌에는 덜 취약하다. 동일 TowerID가 여러 엔트리로 나뉜 정상 집계에서도 툴팁은 한 줄로 합치지만 도감은 엔트리 구조를 그대로 그린다. `Icon == null` 재료 누락은 #511 피드백에서 이미 해소돼 현재 도감도 빈 아이콘 슬롯을 생성하며, 이 항목의 잔여는 **집계 규칙 이원화와 아이콘/텍스트 표현 정책 미확정**이다 | `TowerFusionMatcher.BuildRequiredAssets(TowerRecipe)`처럼 집계된 TowerAsset+수량을 반환하는 공용 경로를 추가해 기존 `BuildRequired`와 규칙을 공유하고 툴팁·도감을 함께 이관한다. 이후 재료 정본 표현을 아이콘 반복/아이콘+수량/텍스트 중 하나로 팀 합의해 `TowerMerge.md`에 명시한다
