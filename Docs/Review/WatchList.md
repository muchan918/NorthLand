# WatchList — 누적 아키텍처 이슈 원장 (PR 리뷰 필수 입력)

> **목적**: PR 리뷰 간 '기억'. 리뷰 봇은 매 리뷰에서 이 목록과 대조하고 해결/악화/신설을 보고한다.
> **갱신 규칙**: 봇이 리뷰 코멘트의 "📒 WatchList 갱신 제안" 절에 항목을 제안하면, 팀이 검토 후
> 이 파일에 커밋한다. 봇이 직접 커밋하지 않는다.
> **RESOLVED 항목은 삭제하지 않고 상태만 바꾼다** (재발 감지용). 해결 PR 번호를 상태에 남긴다.
> **형식**: WL-번호 | 상태(OPEN/RESOLVED) | 등록 PR | 내용 | 해소 조건

- **WL-001** | 부분 착수(PR#80) | PR#29/53/59 | Combat `Tower.cs`/`Enemy.cs`가 `TowerAsset`/`EnemyAsset` 소비로 이관 완료(archer_tower/goblin_warrior 등으로 확인). 옛 Combat 자체 SO(`TowerData`/`EnemyData`)는 PR#80에서 삭제되어 동명 타입 혼동은 해소됨. 잔여: 나머지 타워/적 종류의 값 채움 + Soldier(`SoldierData`, 여전히 Combat 자체 SO — 데이터 파이프라인 미이관) 이관 미착수. Combat이 이제 전역(네임스페이스 없음) `TowerAsset`/`EnemyAsset` 폴리모픽 구조에 하드결합돼, muchan이 필드 그룹을 바꾸면 Combat이 깨질 수 있음 | 전 종류 값 채움 + Soldier 스탯 원본 이관 + 공격 계약 정본(`TowerAsset.*.Attack`) 합의
- **WL-002** | OPEN | PR#31,PR#218 | 매니저 수명주기 3종 난립(static/DontDestroyOnLoad/씬 싱글톤) + MouseManager.\_camera 씬 참조가 씬 전환 시 끊어짐. PR#115에서 `SkillManager`/`BuffSkillManager` 씬 싱글톤 2종이 추가로 늘어남, 매니저 수명주기 4번째 패턴: `OutlineInteractionDriver`가 `RuntimeInitializeOnLoadMethod(AfterSceneLoad)` 자체 부팅 + `DontDestroyOnLoad`(씬 파일 병합 회피, SceneWorkflow 정합). 부트스트랩 규약 확정 시 이관 대상 — 대가로 폭·색 튜닝값이 전부 코드 상수 | 부트스트랩/매니저 규약 문서화 + TooltipUI(#38) 임시 싱글톤 추가 — UIManager 흡수 대상 3종째, 스킬 2종 포함하면 5종째
- **WL-003** | RESOLVED(PR#83) | PR#32/83 | StageBuilder.MonsterRoute(IReadOnlyList<Vector3>) 읽기 전용 공개 프로퍼티 신설로 순서 경로가 MapBuilder 정본으로 확정됨. 병사(§6.4)·Combat 소비 창구 확보 | (종료 — 스폰 지점·최종 목표 좌표 공개 API는 아직 없음, 필요 시 별도 항목화)
- **WL-004** | RESOLVED(PR#81) | PR#31/32 | 배치 검증 공백: CanPlaceAt 항상 true, Snap 항등, 타일 종류(도로/용암/잔디) 질의 API 없음 → 도로·용암 위 타워 설치 가능 | **해소**: MapBuilder 질의 API 대신 배치 측이 `BattleTile`(TileKind Grass/Road/Lava + Occupied) 마커 + `TowerPlacer`로 스냅·풋프린트 검증·점유 확인을 구현(도로/용암/점유/맵밖 셀 차단). `Assets/Scripts/GameManager/MouseManager/TowerPlacement/`에 위치. tileSize 상수 동기화 잔여는 WL-034로 별도 추적
- **WL-005** | RESOLVED(PR#80) | PR#29/31/53 | 레이어 규약 부재: Enemy/Selectable/Ground에 더해 PR#53에서 PlayerUnit·PlayerBase·Enemy(targetLayerMask) 전투 탐지 3종이 각자 SerializeField로 추가됨 — 미설정 시 무증상(탐지 0) | **해소**: 전투 레이어 3종(Enemy=7 / Soldier=8 / PlayerBase=9)을 `ProjectSettings/TagManager.asset`에 등재(PR#80, 커밋 0efacec). 프리팹 레이어·마스크 비트(768=Soldier+PlayerBase, 128=Enemy)와 정합 확인. 잔여: LayerMask vs Tag 방식 최종 확정은 각 스크립트에 TODO(TBD) 주석으로 남아있음(Tower/Soldier/Enemy) — 팀 컨벤션 회의 필요
- **WL-006** | OPEN | PR#22 | 에셋 로딩 이원화: Resources.Load(DataTable) vs Addressables(Localization) | 로딩 전략 단일화 결정
- **WL-007** | RESOLVED(PR#83) | PR#58/83 | SpawnFinalCenterObject 인라인 grid→world 복제 제거, StageMonsterRouteTracker.ToWorldPosition(GetWorldPosition)로 단일화 | (종료 — StageRoadTracker 경로는 별도 용도로 잔존. PR#81에서 TowerPlacer가 별도 tileSize 변환을 재구현해 유사 패턴이 새로 생겼으나 이는 WL-034로 분리 추적)
- **WL-008** | OPEN | PR#32 | 로그라이크 시드 재현성: 전역 UnityEngine.Random 사용, 시드 주입 설계 없음. PR#75(영토 생성기)가 요구 해법(System.Random 주입 + 소비 순서 고정)을 선구현 — MapBuilder 적용 시 참조 패턴 | Run 시드 설계 후 MapRandom에 주입
- **WL-009** | OPEN | PR#32 | 용어 충돌: StageWaypoint(블록 연결점) vs GDD 웨이포인트(병사 배치 지점), '스테이지'(블록) vs GDD 스테이지(런 단위) | 병사 시스템 착수 전 리네임
- **WL-010** | RESOLVED(스크립트 통합 PR) | PR#29/53 | 폴더명 오타 `Scirpts` — PR#53에서 Base/·Unit/ 하위 신규 파일로 meta GUID 참조 추가 심화 | `Assets/Personal/*` → `Assets/Scripts/` 통합 이동 시 `Scirpts` 오타 폴더 자체가 사라지고 `Assets/Scripts/CombatSystem`으로 대체되며 해소됨
- **WL-011** | OPEN | PR#31 | 선택 통지 이중 경로: OnSelectionChanged 이벤트(구독자 0) vs SelectableTest→TowerInfoUI 직접 호출 | 정본 경로 결정
- **WL-012** | OPEN | - | GDD §9 미확정 항목 결합 주의: 병사/스킬 통합 여부, 몬스터 테마, 스테이지/보스 구성, 밸런싱 수치 → 관련 코드는 결합을 느슨하게 | GDD 확정 시 해제
- **WL-013** | RESOLVED(PR#126) | PR#20/22/126 | 표시 문자열 소유권: Building/Enemy/Resource/Tower CSV의 표시 문자열을 String Table 키로 이관 완료(`NorthLand_default`/`NorthLand_buildings`/`NorthLand_Enemies`/`NorthLand_Towers`, ko-KR/en-US/ja-JP). 이번 PR에 새로 들어온 `poison_tower` 행도 `towers.poison.name/role/desc` 키로 이관(ko/en/ja 3종 값 입력). 경영 공간 UI는 키 조회로 표시. **해소**: 이관 방침 확정·구현 완료 | 잔여(후속·별도 추적): 전투 공간(TowerInfoUI) 키 조회 표시 배선은 #102 후속
- **WL-014** | OPEN | PR#22/29/31 | Get/Instance 계열 null 무가드 역참조 반복 (DataTableManager.Get, MouseManager.Instance 등) | 호출부 null 가드 관행 정착
- **WL-015** | OPEN | PR#46/59 | 밸런싱 수치 소유권 이원화가 타워로 확대: 건물(주민당 생산량)에 이어 타워 공격 수치(damage/range/splash/chain/버프량)가 CSV가 아니라 TowerAsset SO 인스펙터 수기 입력(TowerAsset.\*.Attack). 폴리모픽 필드라 평면 CSV에 담기 어려운 구조적 이유는 있으나 계약 #2(수치=CSV)와 긴장. 값 미기입 시 #21 소비 불가. **연동(#139)**: 건물 업그레이드 레벨 테이블(비용·주민당량)도 CSV가 아니라 `BuildingAsset.Production.UpgradeLevels`(SO)에 authoring — 같은 축(수치 기입은 placeholder TBD). **연동(#205)**: 마법 연구소 레벨→스킬 배율 매핑도 CSV가 아니라 `BuildingAsset.Skill.UpgradeLevels`(SO, 도달 비용과 같은 리스트)에 authoring — 같은 축(수치는 placeholder TBD). 최초엔 씬 인스펙터 리스트였다가 WL-102로 SO 이관 완료 | 폴리모픽 스탯의 데이터 원본 합의 + 값 기입
- **WL-016** | RESOLVED(#139/PR#154) | PR#46/#139/#154 | BuildingAsset.Data 캐시가 건물 타입당 단일 SO — 인스턴스별 레벨/주민 상태(GDD §5.7 건물 업그레이드) 도입 시 공유 SO 덮어쓰기 위험. **#139(생산 건물 업그레이드=주민당량 증가)가 이 per-instance 레벨 상태를 도입.** **해소**: 레벨/주민당량을 공유 `BuildingAsset`이 아니라 `ManagementController` **라인별 런타임 배열**(`_level`·`_amountPerVillager`)로 소유. `BuildingAsset`은 읽기 전용 기준값(base 주민당량·레벨 테이블)만 제공 → 공유 SO 오염 없음(BuildingUpgrade.md 쟁점1) | (종료 — 정적 조회 데이터 vs per-instance 상태 경계 확정)
- **WL-017** | 진전(PR#116) | PR#48/#43/#116 | ResourceWallet 소유권 #43에서 `ManagementController`가 지갑을 소유·노출(씬 범위, `OnChanged`로 UI 갱신)하여 하네스 로컬 문제는 해소. 단 전역/씬 간 공유(다른 씬의 소비처 접근) 방식은 WL-002 수명주기와 함께 미확정, PR#116에서 소비 게이트웨이 `ManagementController.CanAfford/TrySpend(IReadOnlyList<ResourceCost>)`가 컨트롤러 계층에 신설돼 소비처(타워)가 지갑 직접 접근 없이 경유하는 경로가 실증됨(WL-017 방향 준수). 지갑 `OnChanged`→컨트롤러 `OnChanged` 버블도 추가. 잔여: SystemMap §2 API 등재 + 전역/씬 간 공유 방식은 WL-002와 함께 미확정 | 전역 매니저/부트스트랩 규약 확정 시 최종화
- **WL-018** | OPEN(진전, PR#179) | PR#49/#43/#66 | 밤→낮 전환(`DayNightManager.EndNight()`) 자동 트리거 부재 — 트리거 주체가 **경영 패널(`ManagementController.RequestAdvancePhase`)에서 밤 전용 임시 UI(`NightActionPanelView`의 "웨이브 성공" 버튼)로 이동함(#66)**. **#17에서 `MonsterSpawn`이 웨이브 클리어(스폰 완료 후 생존 0) 시 `EndNight()`를 자동 호출하도록 연결(단 처치가 아닌 본진 도달-디스폰 기준 — 처치 기반은 Enemy 병합 후 WL-005).** 정식으로는 밤을 끝내는 주체(Combat 웨이브 클리어/사망 처리 등)가 `EndNight()`를 직접 호출해야 하며, `NightActionPanelView`는 그때 제거해야 함. 같은 이유로 "웨이브 실패"(`GameManager.TriggerGameOver()`)·"보스 처치"(`GameManager.TriggerVictory()`) 버튼도 각각 Combat의 실제 웨이브 실패·보스 사망 판정 연동 전까지의 임시 대체물 | Combat 웨이브 클리어/실패/보스 처치 판정 확정 후 `EndNight()`/`TriggerGameOver()`/`TriggerVictory()`를 그쪽에서 직접 호출하도록 연결하고 `NightActionPanelView` 제거 **진전(PR#179)**: 최종 웨이브 클리어 시 `WaveCompletionCoordinator`가 `GameManager.TriggerVictory()`를 데이터 기반(`MonsterSpawnWaveProvider.IsFinalWave`, 웨이브 수 비하드코딩)으로 직접 호출 — "보스 처치" 임시 버튼의 정식 대체물 확보. `MonsterSpawn.StartRound`에 `GameManager.Result != Playing` 스폰 차단 방어선 추가. 잔여: `NightActionPanelView`의 임시 승리/실패 버튼 제거 + 웨이브 실패(성문 파괴)→`TriggerGameOver` 정식 연동 | (기존 해소 조건 유지)
- **WL-019** | 부분 해소(PR#104) | PR#50/#104 | Tower 페이즈 게이팅 미연결: Tower.Update()가 낮/밤 구분 없이 공격하던 문제 → PR#104에서 Tower.Update()에 DayNightManager 밤 게이팅 연결(CurrentPhase!=Night이면 미공격, DayNightManager.Instance null 가드 포함). GDD §5.2 밤 전용 동작 충족. 잔여: WL-018의 임시 밤 종료 버튼 제거·정식 웨이브 판정 연동 | Tower 밤 게이팅 완료 — WL-018과 세트로 종료
- **WL-020** | OPEN | 이슈#7 | `DayNightLightingController`가 `RenderSettings.ambientMode`(Skybox→Trilight 전환)·`RenderSettings.skybox`(프라이빗 인스턴스 교체)를 씬 전역으로 변경 — 경영/전투 공간이 한 씬에 공존(SystemMap §5)해 전투 공간 조명·스카이박스도 함께 바뀜. 팀 계약 #4(공간 분리) 관련 Combat/BattleMapBuilder 조명 전제와 충돌 가능 | Combat/BattleMapBuilder 팀과 조명 영향 범위 합의, 필요 시 공간별 분리 방안 검토
- **WL-021** | OPEN(부분 해소 #139/PR#154) | PR#52/#43/#139/#154 | 경영 생산 라인이 인스펙터 `ResourceAsset[]` + 단일 전역 `_baseAmountPerVillager`로 구성됨(ManagementController.BuildModel) — `ResourceProductionSource.TryCreate(BuildingAsset)` 경로 미사용. **부분 해소(#139)**: 라인 생성원을 `ResourceAsset[]` → **`BuildingAsset[]`(`_productionBuildings`)로 이관**, 전역 `_baseAmountPerVillager` **제거**하고 라인별 주민당량(`_amountPerVillager[]`)으로 분화(WL-016 세트, BuildingUpgrade.md 쟁점2). `TryCreate(BuildingAsset)`도 실사용 전환(주민당량 캡처·훈련장 필터는 제거 — 주민당량 인자화 + 병사 시스템 리젝). **잔여**: 건물 **배치**(#27) 인스턴스로 라인 생성원 연동, 확장 자원(영토 해금, GDD v0.3) 동적 라인 지원. **또한 #27에서 다인스턴스 지원 시 "업그레이드 소유 단위(타입 vs 인스턴스)" 결정 선행 필요** — 현재는 타입 단위 확정(BuildingUpgrade.md §2). | 건물 배치 연결 시 라인 생성원을 건물 인스턴스로 교체(업그레이드 소유 단위 결정 선행). **#166**: 확장 자원(미개척 영지)은 주민 배치 라인이 아니라 **매일 자동 수급 표시 행**으로 구현(패널 고정 행 + `ManagementController.SupplyDaily`) — "확장 자원 동적 라인" 요구는 이 형태로 충족. 잔여는 건물 배치(#27) 인스턴스 라인 연동
- **WL-022** | RESOLVED(PR#224) | PR#52/#43/#224 | "전원 배치라야 밤 전환"(CanEndDay) 하드 게이트 제거 완료 — 유휴 주민 판정이 밤 전환 차단이 아니라 확인 팝업 경고(`HasIdleVillagers`)로 강등됐다. GDD §6.1 훈련장 배치처·잉여 주민 정상 플레이와의 충돌 해소 | (종료 — 경고 상시화 문제는 WL-109로 분리)
- **WL-023** | OPEN(부분 완화, PR#104) | PR#54/#84/#104 | 카메라 제어가 Mouse/Keyboard.current 직접 폴링 — PR#54 CameraController에 이어 PR#84가 CameraController2에서 동일 패턴 재도입(직접 폴링 카메라 2개 병존, CameraController2 좌클릭 드래그가 MouseManager 선택과 충돌). PR#104에서 CameraController2에 EventSystem.IsPointerOverGameObject() 가드 추가로 UI 위 드래그/줌 간섭은 완화됨. 단 직접 폴링 카메라 2개 병존·입력 소유권 미정은 유지 | 카메라 입력 소유권 합의 + 두 카메라 정합, SystemMap §4.1 반영
- **WL-024** | OPEN(재발, PR#84) | PR#54/#84 | 줌이 Lens.OrthographicSize에 하드결합 — PR#84 CameraController2에서 재발. #28 구도(쿼터뷰=원근 가능성) 미확정 상태에서 투영 방식 변경 시 재작업 | #28 확정 후 줌 대상 격리(ApplyZoom) 또는 확정값 반영
- **WL-025** | OPEN(악화 방향) | PR#53/80 | 콜라이더→IDamageable 해석·타겟 선정: Tower/Soldier/Enemy의 FindTarget 자체는 `OverlapSphereNonAlloc`+최근접 선정으로 이미 통일돼 있음(등록 당시 지적된 분기는 현재 코드엔 없음). 다만 PR#80에서 추가된 `Projectile.ApplyArea`/`FindNearestUnhit`(스플래시·연쇄 타격 처리)이 할당형 `Physics.OverlapSphere`를 새로 사용해 NonAlloc 원칙이 다시 깨짐 | impact 경로도 NonAlloc 버퍼로 통일 + 콜라이더 배치 규약 확정(레이어/태그 방식 포함)
- **WL-026** | OPEN(심화) | PR#59/80 | GDD 미기재 타워 택소노미(Single/Area/Chain/Magic)가 데이터 설계에 이어 PR#80에서 전투 로직(`Projectile`의 Single/Area/Chain 분기, Magic은 no-op)까지 확대 적용됨 | GDD §6.2 반영 + 팀 합의(WL-012 연동)
- **WL-027** | OPEN | PR#62 | 밸런싱 수치 SO화가 Enemy로 확대(WL-015 연동): 몬스터 공통 스탯(MaxHp/MoveSpeed/AttackDamage/AttackRange/AttackInterval)이 CSV가 아니라 EnemyAsset.CombatFields(SO) 인스펙터 입력. 공통 스탯은 세 타입 전부 공유해 평면 CSV 가능하나 전부 SO로 감 — 이슈#26의 '스탯 CSV 정의 + 계약-우선 MonsterData' 미충족, 다운스트림 #14/#15/#16이 EnemyTable로 스탯 조회 불가 | 공통 스탯 CSV 승격 여부 + 타입 고유 필드만 SO 유지 합의(SUNGSOO·#26 오너)
- **WL-028** | RESOLVED(#66) | PR#61 | 경영 공간 씬 정본 이원화: 게임 부트 씬 ManageSpace-Sungsoo.unity가 muchan/Scene/ManageSpace.unity의 복사본으로 출발했으나 main 머지로 157줄 divergence 발생(muchan 정본엔 TowerDataTest·ManageObjects 등 존재, 부트 복사본엔 없음). GameSceneManager가 복사본을 부팅해 muchan 후속 경영 씬 작업이 게임에 반영 안 됨, 25779줄 씬 재동기화 불가 | **해소**: #66에서 `GameSceneManager`의 씬 이름 상수를 `SceneWorkflow.md` 정본 이름(`TitleScene`/`GameScene`)으로 교체, `EditorBuildSettings`에 두 씬만 활성화 등록(코드로 확인됨). muchan/n0wst4ndup/SUNGSOO 세 소스 씬을 `GameScene.unity` 하나로 병합 완료. 잔여: SUNGSOO/muchan/SUNJIN 3개 개인 사본 씬 파일은 아직 물리적으로 남아있어 "복사본 폐기"는 미완 — 재발 방지를 위해 개인 사본 폐기 및 부트 씬 지정 변경 시 리뷰 게이트 필요
- **WL-029** | OPEN | PR#75 | 경영 영토(#18) 모델 12종(TerritoryGraph/Node/State/Edge/Controller 등)이 네임스페이스 없이 전역 진입 — 이슈#18이 요구한 전투 영토 확장(#1)과의 데이터 구조 공유 검토 미결. #1 착수 시 일반명 전역 충돌 예약 | #1과 모델 공유/분리 합의 + Territory 네임스페이스 격리
- **WL-030** | RESOLVED(잔여: 주민 심·분배 정책) | PR#75 | 영토 확보 효과 지급 **연결 완료**: 효과 SO 계층(`TerritoryEffect` 추상 + `Grant/GainResident/ProductionMultiplier` + `TerritoryDefinition` + `TerritoryEffectContext`) + 중복 없는 정의 주입(`AssignDefinitions`) + **Apply 배선**(`ManagementController`가 `Graph.OnNodeClaimed` 구독 → `TerritoryDefinition.ApplyAll`)까지 구현. 즉시 자원/마나석은 `ResourceWallet.Add`로 지급(계약 #3 준수), 패시브 생산 +X%는 신설 `ProductionModifiers`(곱셈 누적)를 `ManagementController`가 정산·예상치에 반영(`ResourceProductionSource.Produce(count, multiplier)`). 편집모드 검증 완료. **수치는 효과 SO authored** — 영토 효과는 계약 #2(수치=CSV)의 예외로 팀 확정(TerritoryGraph.md §5). 잔여: (a) 주민 획득은 주민 시스템 부재로 여전히 placeholder 심, (b) 효과 분배 정책(풀 소진 시 재셔플 반복)은 잠정 — 팀 회의로 방향성 재검토 예정. **후속(#166/PR#170)**: 효과 SO 계층 자체가 삭제되고 매일 자동 수급으로 대체됨(WL-070) — 이 항목의 대상 코드(효과 Apply 배선)는 더 이상 존재하지 않으며, 주민 획득 효과(`GainResidentEffect`)도 제거됨. 분배 정책은 자원 종류 반복 배정으로 정리(정상) | (종료 — WL-070으로 대체됨)
- **WL-031** | RESOLVED(PR#77 문서 수정) | PR#77 | 씬 병합 규약(SceneWorkflow.md)의 "번호 누적 파일 + GameSceneManager 활성 씬 상수 수동 갱신"이 WL-028 실패 모드(부트 씬이 옛 버전 지목) 재생산 위험. GameSceneManager.cs:12-13이 활성 씬을 하드코딩 const로 보유 → 병합마다 소스 편집·재컴파일 강제 | `Docs/Core/SceneWorkflow.md` §4를 재작성 — 정본 파일 이름(`TitleScene`/`GameScene`)을 절대 바꾸지 않고, 병합은 번호 스냅샷 추가 후 "정본 확정" 단계에서 스냅샷 내용을 정본 파일명에 덮어쓰는 방식으로 변경. `GameSceneManager`/Build Settings가 항상 고정 이름만 참조하므로 병합마다 소스 편집·재컴파일이 필요 없어짐
- **WL-032** | OPEN | PR#64/#74/#66 | `GameManager`/`ResultUIManager`/`GameSceneManager`의 공개 API(`TriggerGameOver()`/`TriggerVictory()`/`ShowGameOver()`/`ShowVictory()`/`LoadMainMenu()`/`LoadManageSpace()` 등)가 `SystemMap.md` §1/§2에 전혀 등재돼 있지 않음 — #56/#65에서 도입됐으나 문서 갱신이 누락됐고(팀 계약 #7), #66의 `NightActionPanelView`가 `GameManager.Instance.TriggerGameOver()`/`TriggerVictory()`를 신규로 소비하기 시작하면서 문서 공백이 실사용 코드로 확대됨 | `SystemMap.md` §1에 GameManager(Core)/GameSceneManager 시스템 행 추가 + §2에 공개 API 등재
- **WL-033** | RESOLVED(PR#82) | PR#82 | `MouseManager`(`DontDestroyOnLoad` 싱글톤)가 씬 재로드 시 카메라(`_camera`)는 재바인딩하나 `_selected`/`_hovered`/`_ghost`/`_mode` 상태는 정리하지 않음 — #66의 "다시 시작"(`GameSceneManager.LoadManageSpace()`) 경로가 활성화되며 노출됨. 재로드 전 상태가 남아있으면 파괴된 옛 씬 오브젝트 참조가 되어 다음 클릭/배치 프레임에서 `MissingReferenceException` | **해소**: `HandleSceneLoaded`에서 `SetCamera` 뒤에 `_selected`/`_hovered`를 알림 호출 없이 직접 `null`로 리셋 + `CancelPlacement()` 호출로 정리 완료
- **WL-034** | OPEN | PR#81 | tileSize 상수 이중화(WL-007 계열): `TowerPlacer.tileSize`(=5f)가 `StageBuilder.TileSize`(=5f)와 독립 존재, 풋프린트 이웃 셀을 tileSize 간격으로 재구성 → 변환식 사본. 수동 동기화 의존(불일치 시 조용한 오배치, 현재 `TowerPlacer.Awake` 경고로만 방어) | WL-007 변환 유틸 단일화에 흡수하거나 `TowerPlacer`가 `StageBuilder.TileSize`를 단일 출처로 참조하도록 변경
- **WL-035** | RESOLVED(PR#224) | PR#84/#224 | 주민 배치 불리언 게이트(`CanAssignVillagers=HasExpandedToday`) 제거 + `CanEndDay` 결합 해제로 프론티어 소진 소프트락 경로 소멸(`CanEditLine`은 밤 게이팅만 유지, `ProductionLineView.editable=IsDay`) | (종료)
- **WL-036** | RESOLVED(PR#142) | PR#83/142 | `MonsterMove` 하드코딩(3f) 제거 → `IMovementAgent.SetMoveSpeed`로 `Enemy`가 `Stat.MoveSpeed`를 단일 소스(Enemy.data)에서 주입(프리팹 EnemyAsset 슬롯 1개로 정리). 미할당/0 시 fallbackMoveSpeed 폴백+경고(정지 없음, WL-049 소프트락 경로 차단). Awake 순서 비결정성은 hasInjectedMoveSpeed 플래그로 흡수 | (종료)
- **WL-037** | RESOLVED(PR#104) | PR#86/#104 | 타워 "선택 → 배치" 데이터 계약 공백: TowerSelectPanelView.OnTowerSelected가 TowerAsset을 발행하나 TowerPlacer 게이트웨이가 별도 필드를 요구하던 문제 → **해소**: PR#104에서 TowerSelectPanelView가 TowerAsset을 TowerPlacer.BeginTowerPlacement(TowerAsset)로 직접 주입하고, TowerPlacer가 SO(TowerPrefab/GhostPrefab/TowerType)에서 배치 정보를 해석하도록 연결. | (종료)
- **WL-038** | 해소 근접(PR#160), 노출면 확대(PR#238) | PR#87(#17)/PR#89/PR#160/PR#238 | (기존) + PR#160 최종: MonsterMove 경로끝 Destroy를 RouteCompleted 이벤트로 승격 → Enemy 단일 owner로 파괴 일원화(사망 Die + 경로끝 모두 Enemy 경유, isDying 가드로 이중 파괴 차단), MonsterSpawn이 스폰 시 Enemy+MonsterMove 필수 검증(누락 시 LogError+Destroy)으로 "구독자 없는 MonsterMove 잔존" 경로 차단. 잔여: destroyDelay(2s)가 childCount 웨이브 클리어(WL-049) 판정을 최대 2초 지연. **+PR#238**: `MonsterSpawn.AliveMonsterCount`(=`monsterParent.childCount`)가 보스 P4 소환 상한(`EnemySpawnMinionsAction.MaxAlive`) 판정의 입력으로 재사용되면서, 이 잔여 오차가 **밸런싱 수치에 직접 노출**됐다 — 집계에 보스 자신과 사망 연출 중인 몬스터가 포함돼 `MaxAlive` 실효값이 의도보다 빡빡해진다 | 병사/보스 프리팹 합류 시 destroyDelay↔웨이브 판정 정합 확인 + #235 Play 검증에서 `MaxAlive` 실측 보정
- **WL-039** | OPEN | PR#89 | Monster FSM 신규 타입(MonsterState/MonsterStateMachine/MonsterAnimation)이 네임스페이스 없이 전역 진입 — WL-029(Territory 전역)와 동일 축의 드리프트 | NorthLand.Monster 네임스페이스 격리 검토
- **WL-040** | OPEN(의존성 명시, PR#104) | PR#104 | 게임오버 딜리버리(성문 파괴→GameOver)가 `Assets/Imported`(중첩 git repo) 소재 BaseGate 프리팹에 의존 — 메인 repo 커밋엔 미포함. **팀 결정: 메인 repo로 이관하지 않고 Imported 유지**(BaseGate.prefab+.meta는 Imported repo에 커밋돼 있어 Imported 동기화 시 GUID 정합됨). 미동기화 시 성문 미생성→GameOver 미동작(팀 계약 #8). 완화: `MonsterSpawn.gatePrefab` 코드 주석 + PR#104 본문에 의존성·요건(PlayerBase 컴포넌트+레이어9) 명시 | Imported repo 동기화를 팀 온보딩/CI 규약에 보장(또는 정책 변경 시 메인 repo 이관 재검토). **재발 패턴(PR#114)**: 리뷰 봇이 Imported(중첩 repo)를 읽지 못해, 씬이 참조하는 Imported 상주 에셋(#92 건물 유료 프리팹 등)을 "프리팹 없음/이슈 미충족"으로 오탐함 — 유료 에셋을 팀 공용 Imported에만 두는 것은 정상 배치이며 오탐임(SystemMap §4 #8 리뷰어 주석 참고). **추가 의존(PR#170)**: 경영 패널 자원 행 프리팹 `@NorthLand/.../ProdRow.prefab`도 Imported 상주 — `ProductionLineView`의 5개 슬롯(신규 `_balanceText` 포함 · `_villager/expected/plus/minusButton`) 배선이 Imported 동기화에 의존. 미동기화 시 자원 보유량/수급 UI 실종(NRE는 없음, 전부 null 가드). 온보딩 규약에 ProdRow 포함 필요
- **WL-041** | OPEN | PR#104/#114 | 폰트 머지 충돌 마커 커밋 사고: `1917306 Merge branch 'main'`이 `TextMesh Pro/Fonts/Pretendard-Regular SDF.asset`·`Personal/SUNGSOO/Font/Pretendard-Regular SDF.asset`에 충돌 마커(`<<<<<<< / ======= / >>>>>>>`)를 해결 없이 커밋 → Unity 임포트 실패·한글 깨짐. 근본 원인: TMP 동적 아틀라스 폰트를 git에 커밋 + 같은 폰트 사본 3벌 파편화(GUID 상이)로 머지마다 충돌 재발. PR#104에서 부모 버전 복구로 마커 제거 + Regular를 Static 한글(2350자) 재베이크. **PR#114 후속 판단(2026-07-18, n0wst4ndup) — 재발 아님**: PR#114에서 `Personal/SUNGSOO/Font/Pretendard-Regular SDF`의 글리프/문자 테이블이 다시 비워졌으나 충돌 마커·임포트 실패는 없다. 팀은 폰트를 TMP 정본(`Assets/TextMesh Pro/Fonts/` + `Resources/Fonts & Materials/`)으로 이미 이관했고 `Personal/SUNGSOO/Font/`는 **더 이상 참조되지 않는 죽은 사본**이다(현재 Pretendard 사본이 3곳 병존 확인). 리뷰 봇은 이 죽은 사본을 몰라 그 경로 churn을 매번 WL-041 재발로 오탐하므로, 사본 삭제 전까지 무시. | 폰트 정본 1벌로 통합(dedup) — **잔여 실행: 죽은 사본 `Assets/Personal/SUNGSOO/Font/` 삭제**(삭제 시 3벌 파편화 근본 원인·리뷰 봇 오탐 동시 해소) + TMP 정본 Static 아틀라스 유지 + 일본어 폴백 별도
- **WL-042** | RESOLVED(#211, 교환 경로 한정) | PR#108 | build2 계획의 "마나석 교환 건물(마나석→기본자원 전환)"이 팀 계약 #3·GDD §3.2와 충돌하는 제2 획득 경로 신설. 함께 "본진 업그레이드 마나석 소모"도 GDD §3.2(건물 업그레이드=기본자원)와 소폭 어긋남. **부분 결정(#97, n0wst4ndup)**: 기본 자원의 **영토 확장 보상** 경로는 허용으로 확정 → 계약 #3의 "기본자원=생산에서만" 조항을 "생산 또는 영토 보상"으로 개정(GDD §3.2·Resources.md·TerritoryGraph.md §6 반영 완료). 잔여: 교환 건물·본진 업그레이드 마나석 소모 등 **다른** 우회 경로는 여전히 미결. **추가 결정(GDD v0.3, #139) → 갱신(2026-07-22)**: 미개척 영지 자원의 획득 방식이 '식량 소모 생산'에서 **'영토 해금 시 매일 자동 수급'으로 변경**(주민·식량 모두 무관, GDD §3.2·Resources.md §5.5 (A)). 따라서 '식량 → 확장 자원' 변환 경로 자체가 사라졌고, 확장 자원은 영토 확장 보상의 일종으로 정리됨. **해소(#211, 2026-07-28)**: 마나석→자원 교환을 **연금술사의 집(Store 타입) 단방향 교환소**로 확정(근거: Build2 계획 §2-2 팀 승인 방향). 낮 전용, 마나석 제외 7종(나무·철·식량·금·루비·사파이어·다이아), 역교환(자원→마나석) 없음, 교환비는 SO authoring(밸런싱 TBD). **계약 위반이 아닌 근거**: 지갑 획득 API(`ResourceWallet.Add`)는 계속 비공개이고, 소비처에 열린 것은 **차감+지급이 한 트랜잭션인 `ManagementController.TryExchange` 단일 진입점**뿐이다 — 마나석 없이는 자원이 생기지 않으므로 교환은 '제2 획득 경로'가 아니라 **마나석 소비처**다. GDD §3.2·계약 #3 문구 갱신 완료. **잔여(미결)**: 본진 업그레이드 마나석 소모는 본성 업그레이드 설계가 TBD라 함께 보류 | (교환 경로 종료 — 본진 업그레이드 마나석 소모는 본성 업그레이드 이슈에서 별도 판단)
- **WL-043** | OPEN(부분 완화, PR#152) | PR#108/#152 | build2 계획의 "웨이브 클리어 시 마나석 고정 지급"을 PR#152가 `ManagementController.HandleNightToDay`(`OnNightToDay` 훅)에 구현. **완화**: (1) 지급을 밤 종료 이벤트에 결합해 밤 종료 트리거 주체 이관(WL-018)과 무관하게 생존, (2) GDD §5.6이 이 매-웨이브 마나 트랙을 §6.6 3택1 보상 트랙과 "별도 트랙"으로 명시 — 3택1을 선점·대체하지 않음. 잔여: §5.6 보상 시스템 착수 시 "웨이브 종료 보상" 소유권을 경영 정산 훅 인라인에서 교체 가능한 보상 훅으로 분리해야 3택1 특수효과 확장과 재작업 없이 맞물림 | §5.6 보상 시스템 착수 시 웨이브 종료 보상을 별도 진입점(보상 컨트롤러)으로 분리
- **WL-044** | OPEN | PR#109 | Magic 타워가 별도 `AuraTower` MonoBehaviour로 구현(정상 분기)되나 `AuraTower.AuraLoop`가 DayNightManager 밤 게이팅 없이 OnEnable부터 상시 틱 → PR#104가 `Tower.cs:38-39`에 넣은 밤 게이팅(WL-019)이 Magic 타워에서 재발. 두 타워 컴포넌트의 페이즈 규칙 불일치 | AuraTower 오라 루프에 `CurrentPhase==Night` 게이팅 적용(Tower.cs:38-39와 동일)
- **WL-045** | OPEN | PR#109 | 정식 전투 로직(AuraTower/StatusEffectHandler)이 `Assets/Personal/SUNGSOO/Scripts/`에 위치 — 팀 계약 #8(스크립트 정본=Assets/Scripts) 위반, 배치/데이터 파이프라인 정식 통합 차단. 네임스페이스도 `NorthLand.Sungsoo`(사람 이름) | `Assets/Scripts/CombatSystem`으로 이동 + `NorthLand.Combat` 편입, 테스트 스캐폴딩만 Personal 유지
- **WL-046** | OPEN(계획됨) | PR#114 | 씬 배치: 영구 이름 씬 `Assets/Scenes/Branches/SweetLand.unity`(본진 select/hover 구성)가 `SceneWorkflow.md §5`(주간 통째 삭제) 대상 폴더 `Branches/`(번호 스냅샷 `GameScene_N.unity` 전용)에 위치. `EditorBuildSettings`·`GameSceneManager` 미등록으로 인게임 미도달. **작성자 결정(2026-07-18, n0wst4ndup)**: 본 PR + 후속 로컬라이제이션 작업 이후 `SceneWorkflow §4②(정본 확정)` 절차로 `GameScene.unity`에 반영 예정 — **의도된 임시 위치**(위반으로 처리 말 것). 리스크: 반영 전 주간 정리(§5)에서 유실 가능 | 계획대로 `GameScene`에 정본 반영(그때 Branches 스냅샷 소진). 반영 전까지 주간 정리에서 `SweetLand.unity` 제외 주의
- **WL-047** | OPEN(진전, PR#124) | PR#114/#124 | 전역 매니페스트에 `com.unity.terrain-tools` 5.3.2 추가(`Packages/manifest.json` — 팀 공유 자원). GDD는 '그리드 기반' TD이고 경영/전투 공간은 타일(`BattleTile`)·`TerritoryGraph` 노드 그리드 구조 — Unity Terrain(sculpting)은 신규 방향. ~~현재 `SweetLand.unity`는 Terrain 컴포넌트 미사용(패키지만 선반입, 지형 미구성)~~ **전제 변경(PR#124)**: 경영 섬 `Management_Island.asset`이 실제 TerrainData로 사용됨(경영 공간 첫 Terrain 지오메트리, `ManageTerritoryScene.unity`). Terrain Collider가 Ground 레이캐스트 대상이 되면 MouseManager 배치·`TowerPlacer` 풋프린트·`TerritoryGraph` 노드 배치의 지면 전제와 어긋날 소지 | 경영/본진 지면을 Terrain으로 갈지(비주얼 전용 + 배치 판정 지면 분리) 타일/메시로 갈지 팀 합의 후 GDD §5.3 반영
- **WL-048** | 부분 해결(PR#116, 41e3364) | PR#116 | 자원 소비 게이트웨이가 범용(IReadOnlyList<ResourceCost>)으로 신설 — BuildingAsset.Cost도 동일 구조라 건물 배치는 이 API 재사용(별도 차감 로직 발명 금지). CanPlaceFootprint에 CanAfford 반영 완료(연속 배치 중 자원 소진 시 고스트 빨강+로그 피드백) → 조용한 실패 해소. 잔여: 건물 배치가 동일 게이트웨이를 실제 소비하는지 | 건물 배치가 동일 게이트웨이 소비
- **WL-049** | OPEN(변화, PR#104) | PR#87(#17)/PR#104 | 밤 종료(`EndNight`)가 `MonsterSpawn.SpawnRoundAsync` 정상 완료 경로에만 의존 — "웨이브 없음=즉시 클리어"로 완화했으나 맵 상한(`StageBuilder.MaxMapCount`=30) 도달 시·`MonsterMove` 부재 프리팹은 여전히 밤이 안 끝남. **PR#104 변화**: 몬스터가 성문(gatePrefab) 사거리에서 IsStopped로 멈춰 공격하면서 기존 "경로끝 도달-디스폰" 경로가 성문 교전으로 대체됨 → `childCount==0`은 이제 '전원 처치' 또는 '성문 파괴(=GameOver)'로만 도달. 타워 사거리 밖에서 성문을 두들기는 몬스터가 남으면 웨이브 '성공' 경로로는 종료 불가(GameOver로만 수렴). 성문 HP(`PlayerBase.maxHp=1000`)·타워 커버리지 밸런스에 클리어 판정이 강결합 | 밤 종료 판정을 스폰 성공/childCount에서 분리(웨이브 컨트롤러가 소유, 맵 상한도 종료로 처리) — WL-018과 세트
- **WL-050** | OPEN | PR#115 | 타워 런타임 스탯 배율(`Tower.damageMultiplier`/`attackSpeedMultiplier`, `ApplyBuff`)이 muchan 버프 스킬에서 신설됐으나 덮어쓰기(비스택) 방식 — SUNGSOO의 미구현 AuraTower 버프 오라(`AuraTower.cs:125-127`)·GDD §7 haste 지원 타워(WL-026)가 같은 필드를 소비하면 만료 시 상호 원복(=1f)으로 서로의 버프를 지움. 런타임 스탯 modifier를 소스별 스택 계약으로 통일 필요 | 스킬/오라/haste 공통 modifier 계약(가산 스택 or 소스별) 합의 후 Tower에 반영
- **WL-051** | OPEN | PR#115 | "전 타워 버프" 능동 플레이어 스킬(`BuffSkillManager`)이 GDD에 없는 신규 유형 — GDD §3 플레이어 스킬=범위 몬스터 피해, 타워 강화는 §4.2 스킬 건물/§7 haste 지원 타워 소관. 정체성 중복·보상 3택1(WL-043) 설계와 충돌 여지 | GDD §5.5 정식 편입 여부 또는 스킬 건물/haste 흡수 결정
- **WL-052** | OPEN(계획됨) | PR#114 | 씬 배치: 영구 이름 씬 `Assets/Scenes/Branches/SweetLand.unity`(본진 select/hover 구성)가 `SceneWorkflow.md §5`(주간 통째 삭제) 대상 폴더 `Branches/`(번호 스냅샷 `GameScene_N.unity` 전용)에 위치. `EditorBuildSettings`·`GameSceneManager` 미등록으로 인게임 미도달. **작성자 결정(2026-07-18, n0wst4ndup)**: 본 PR + 후속 로컬라이제이션 작업 이후 `SceneWorkflow §4②(정본 확정)` 절차로 `GameScene.unity`에 반영 예정 — **의도된 임시 위치**(위반으로 처리 말 것). 리스크: 반영 전 주간 정리(§5)에서 유실 가능 | 계획대로 `GameScene`에 정본 반영(그때 Branches 스냅샷 소진). 반영 전까지 주간 정리에서 `SweetLand.unity` 제외 주의
- **WL-053** | OPEN(진전, PR#124) | PR#114/#124 | (WL-047과 동일 축 중복 등재) 전역 매니페스트에 `com.unity.terrain-tools` 5.3.2 추가. ~~현재 `SweetLand.unity`는 Terrain 컴포넌트 미사용(패키지만 선반입)~~ **전제 변경(PR#124)**: 경영 섬 `Management_Island.asset`이 실제 TerrainData로 사용됨 — 상세는 WL-047 갱신본 참조 | WL-047과 동일 (경영/본진 지면 Terrain vs 노드 그리드 분리 합의 후 GDD §5.3 반영)
- **WL-054** | OPEN | PR#117 | 본진 `PlayerBase.maxHp` 기본값이 HP UI PR(#100)에서 1000→100으로 축소됨(의도적 변경, HP 바 테스트 가시성 목적 — 되돌릴 계획 없음). WL-037의 밤 종료(웨이브 클리어) vs GameOver 판정이 성문 HP·타워 커버리지에 강결합돼 있어 밸런스 전제값이 바뀜. Combat(SUNGSOO) 소유 파일에 대한 크로스오너 밸런스 변경 — PR 본문 대신 별도로 SUNGSOO에게 직접 확인 요청함(2026-07-19) | SUNGSOO 밸런스 확인 완료 후 최종값 확정 + WL-037 웨이브 판정과 정합 확인
- **WL-055** | OPEN | PR#117 | HealthUI 신규 스크립트(`MonsterHealthBar`/`BaseHealthBar`)가 전역 네임스페이스 + 제네릭 이름 진입(WL-039와 동일 드리프트 축). 몬스터 HP 바가 프리팹(Phantom/Shadow) 자식 캔버스 수동 부착 방식이라 #94 신규 프리팹마다 수동 부착 필요(누락 시 무증상) | UI 네임스페이스 격리 검토 + 프리팹 HP 바 부착 규약 문서화 또는 런타임 스폰 방식 결정
- **WL-056** | OPEN(부분 완화, PR#152) | PR#108/#152 | build2 계획의 "웨이브 클리어 시 마나석 고정 지급"을 PR#152가 `ManagementController.HandleNightToDay`(`OnNightToDay` 훅)에 구현. **완화**: (1) 지급을 밤 종료 이벤트에 결합해 밤 종료 트리거 주체 이관(WL-018)과 무관하게 생존, (2) GDD §5.6이 이 매-웨이브 마나 트랙을 §6.6 3택1 보상 트랙과 "별도 트랙"으로 명시 — 3택1을 선점·대체하지 않음. 잔여: §5.6 보상 시스템 착수 시 "웨이브 종료 보상" 소유권을 경영 정산 훅 인라인에서 교체 가능한 보상 훅으로 분리해야 3택1 특수효과 확장과 재작업 없이 맞물림 | §5.6 보상 시스템 착수 시 웨이브 종료 보상을 별도 진입점(보상 컨트롤러)으로 분리
- **WL-057** | RESOLVED(PR#120) | PR#120 | `poison_tower.asset` Cost 미설정(빈 리스트) → Debuff 타워 무료 무한 배치 가능하던 문제 → **해소**: Cost(나무40+철20) 부여로 cannon과 동일 자원 게이트 적용 | (종료 — 최종 밸런스 수치는 WL-015/밸런싱 축에서 계속 튜닝)
- **WL-058** | RESOLVED(PR#120, 잔여 정리 PR#126) | PR#120/126 | CSV `slow_tower`→`poison_tower` 교체로 생긴 `slow_tower.asset` 고아(Get 시 null) → **해소**: CSV에 `slow_tower` 행 복원으로 Get 정상화(삭제 대신 데이터 유지 택 — slow/poison 디버프 2종 공존). 잔여였던 "`TowerTableTest.cs`가 poison_tower 미조회(테스트 커버리지)"도 PR#126에서 poison_tower 로컬라이제이션 이관과 함께 조회 배열에 추가로 정리 | (종료)
- **WL-059** | 진전(PR#128) | PR#124/#128 | 경영 공간 씬 정본 이원화 재발 위험: 경영 지형/물 비주얼이 개인 씬 `Assets/Personal/muchan/Scene/ManageTerritoryScene.unity`에 구축되는 반면 경영 시스템(ResourceWallet/ManagementController/TerritoryGraph, n0wst4ndup)은 이미 정본 `GameScene`에 통합됨(WL-028 RESOLVED 축). PR#128에서 개인 씬 v2(`ManageTerritoryScene_v2.unity`) + 신규 노드 프리팹(`TerritoryNodeV2.prefab`)으로 divergence가 프리팹 계층까지 확대됐으나, **같은 브랜치에서 SceneWorkflow §4 절차로 정본 반영 완료**: v2의 Territory(AreaRadius 450·MinNodeSpacing 140·seed 12345, `_nodePrefab`=TerritoryNodeV2)·Management(성벽 장식 11종 추가분)·Water를 `GameScene.unity`에 병합(선별 병합 — GameScene 쪽이 최신인 Canvas/MapBuilder/MonsterSpawner는 유지) + 카메라 튜닝 수치 복사, 스냅샷 `Branches/GameScene_7.unity` 생성. 튜닝 세트(간격 ↔ 산 스케일 0.5·소용돌이 지름 60·콜라이더 45)는 프리팹에 실려 함께 반영됨 | 잔여: muchan 경영 개인 씬 사본(ManageSpace/ManagePrefabScene/ManageTerritoryScene v1·v2 등) 소진 + n0wst4ndup에 `_nodePrefab` 교체 공유(크로스오너 확인)
- **WL-060** | RESOLVED(PR#126) | PR#126 | Localization 테이블명 상수 대소문자 트랩: `LocalizationHelper.k_TowersTable`="NorthLand_towers"/`k_EnemiesTable`="NorthLand_enemies"가 실제 컬렉션명 `NorthLand_Towers`/`NorthLand_Enemies`(대문자)와 불일치. Unity Localization의 이름 기반 `TableReference` 조회는 대소문자 구분이라 후속 전투 공간(TowerInfoUI) 배선 시 조용히 조회 실패할 뻔함(등록 당시 `TowerTableTest`/`EnemyTableTest` 디버그만 사용해 잠복) → **해소**: 두 상수를 실제 컬렉션명(대문자)으로 정정(buildings/default는 원래 일치). **재발 감지 트랩**: 새 테이블 상수 추가 시 컬렉션명 대소문자 정확 일치 필수 | (종료)
- **WL-061** | OPEN | PR#126 | BuildingTable 정의 축소/개명이 로컬라이제이션 PR 범위 밖 데이터 변경으로 섞여 들어옴: origin/main 9행 → 6행으로 `training_camp`(병사 생산)·`church`(병사 회복)·`military_school`(병사 강화) 삭제 + `headquarters`→`castle` 개명. 삭제 3종은 GDD §5.4/§6.5·§9 병사 시스템(도입 미확정, §9 체크박스 미완)과 직결된 건물이고, SO(`church.asset`/`training_camp.asset`/`military_school.asset`)는 고아로 잔존, `headquarters.asset` 파일명↔`BuildingID:castle` 불일치. 참조 씬/프리팹이 `Get("church")` 등에서 null 수신 가능(툴팁 fallback으로 크래시는 아님, 이름 미표시). `BuildingTableTest`의 삭제 ID 참조 NRE(SystemMap §2 null 가드 계약 위반)는 PR#126에서 조회 목록 6개 갱신 + `if (data == null) continue;` 추가로 해소 | 건물 정의 삭제/개명은 로컬라이제이션과 분리된 별도 결정으로 데이터 파이프라인 소유자(muchan)와 합의: 삭제 3종 잠정 제외 여부·고아 SO 처리(삭제 or 유지)·`castle` 키와 GDD '본진' 용어 관계 확정
- **WL-062** | RESOLVED(PR#128) | PR#128 | 정본 공용 스크립트(`Assets/Scripts/ManagementSpace/Territory/View/TerritoryNodeStateVisual.cs`)가 Imported 벤더 타입 `ithappy.BlendShapeAnimator`를 컴파일 타임 직접 참조하던 문제 — 저장소 전체에서 `ithappy` 참조는 이 파일이 유일했음. asmdef 부재(전부 Assembly-CSharp)로 인해 Imported(중첩 repo) 미동기화 팀원은 씬/프리팹 소프트 레퍼런스 깨짐(WL-040 기존 수준)이 아니라 **Assembly-CSharp 전체 컴파일 실패 → 전 시스템 작업 차단**이었음. WL-040(Imported 사각지대)의 하드-결합 확대판. (리뷰 봇은 WL-060으로 제안했으나 번호 중복으로 WL-062 등재) → **해소**: 같은 PR에서 덕타이핑으로 교체 — `GetComponentsInChildren<MonoBehaviour>(true)` 순회 + `GetType().Name == "BlendShapeAnimator"` 필터로 비활성/파괴. 벤더 미동기화 환경에서도 컴파일 보장. **재발 감지 트랩**: 정본 `Assets/Scripts/`에서 벤더 네임스페이스(`ithappy` 등) 컴파일 타임 직접 참조 금지 — 필요 시 덕타이핑 또는 asmdef 격리 | (종료 — Imported 씬/프리팹 GUID 소프트 의존은 WL-040에서 계속 추적)
- **WL-063** | OPEN | PR#144 | 몬스터 발 높이 단일 출처 부재: CombatMapTileSpawner.monsterWaypointYOffset(=6f)가 Road 프리팹 반높이 큐브·맵 해수면 Y와 독립된 매직 상수로, 경로/스폰 Y를 결정하는 유일한 값이나 TileHeight/프리팹 지오메트리에서 파생되지 않음. 프리팹·맵 Y 변경 시 수동 재튜닝 의존(불일치 시 몬스터 파묻힘/부양의 조용한 오정렬) | 프리팹 상단 높이/TileHeight에서 오프셋 파생 또는 단일 출처 문서화 + SUNGSOO 성문/사거리 높이 정합 확인
- **WL-064** | OPEN(진전, PR#143) | PR#143 | 경영 엣지 뷰가 LineRenderer→배(SweetBoat/TerritoryEdgeShip) 왕복 연출로 교체(뷰-온리, 모델은 순수 질의 `TerritoryGraph.IsOwned` 추가). **(1) GameScene 배선 회귀 해소**: 정본 `GameScene`의 `TerritoryGraphView`에 `_shipPrefabs` 5척 배선 + `_drawEdgeLines=false` 유지(SceneWorkflow §4, WL-059 세트) — 엣지 미표시/엣지별 LogWarning 회귀 제거 확인. **(2) 잔여(🟡)**: 엣지 표시 규칙을 `IsRevealed`(Owned+Selectable, §4.2)→`IsOwned`(Owned 전용)로 좁힌 게 이슈#93 완료기준④·§4.2와 갈리는 **보이는 정보 규칙 변경**(작성자 명시) — n0wst4ndup/팀 사인오프 필요 | 팀 사인오프 후 §4.2 본문을 "배=Owned 전용 표시, 노드 공개=revealed 유지"로 정정하고 WL-063 RESOLVED(PR#143) 전환
- **WL-065** | OPEN | PR#147 | 정식 타워/투사체 프리팹이 개인 WIP 폴더 `Assets/Personal/SUNGSOO/SweetLand Prefab/`에 상주하면서 프로덕션 SO(`Resources/ScriptableObjects/Towers/archer_tower.asset`·`cannon_tower.asset`)가 런타임에 이를 참조 — CLAUDE.md/SystemMap §4(Personal=WIP 전용)·WL-045와 동일 축. 개인 폴더 정리/이동 시 프로덕션 참조 깨짐 위험. 구 플레이스홀더(ArcherTowerTest)도 Personal 상주였던 기존 패턴의 연장 | 공용 경로(Assets/Prefabs/Tower)로 이관 후 SO 재배선(폴더명 공백 제거 포함)
- **WL-066** | RESOLVED | PR#147 | 타워 Ghost(배치 프리뷰) 프리팹에 live·enabled `Tower`(+일부 `TowerReloadVisual`) 컴포넌트가 실려 고스트 인스턴스가 `Tower.Active`(BuffSkillManager 소비)에 자가 등록. 구 플레이스홀더 고스트에도 있던 pre-existing(실害 낮음 — 고스트는 낮 배치 중만, Tower.Update 밤 게이팅) | Ghost 프리팹에서 Tower/TowerReloadVisual 스트립(시각 전용화)
- **WL-067** | OPEN | PR#151 | 전투맵 타일 종류 계약 이원화: 스킬 조준(MouseManager, muchan)은 신맵 `CombatMapTileView.TileType`(CombatTileType Road/Grass/Water)을 질의하나, 타워 배치검증(`TowerPlacer`, n0wst4ndup)은 구맵 `BattleTile.Kind`(TileKind Grass/Road/Lava)를 질의. 정본 GameScene은 신맵(CombatMapInitializer/CombatMapTileSpawner)으로 이관됐고 `BattleTile`은 어떤 프리팹에도 미부착 → TowerPlacer 배치검증 사실상 무효. WL-004(BattleTile 배치검증 RESOLVED)가 맵 이관으로 재개봉된 셈 | TowerPlacer를 CombatMapTileView 질의로 이관해 타일 종류 계약 단일화(신맵을 단일 출처로)
- **WL-068** | OPEN | PR#151 | 스킬 AoE 시전 Y와 몬스터 부양 높이 불일치: SkillManager.CastAt이 hit.point(도로 표면)에 OverlapSphere(radius 기본 3f)를 치는데 몬스터는 monsterWaypointYOffset(=6f, WL-063)만큼 떠 있어 수직차>반경 → 적중 0 우려. #134 완료기준(피해)이 이 PR에서 미검증 | 시전 Y에 몬스터 오프셋 반영(단일 출처 파생) 또는 Y 무시 수평 판정 + Skill_v2에서 실피해 검증 · **#200 결정(2026-07-26)**: 스킬/타워 코드 무변경 방향 확정 — 공중 유닛/부양 몬스터에 '지면까지 닿는 길쭉한 트리거 콜라이더'(Trigger+Enemy 레이어) 계약을 주면 선정이 콜라이더 OverlapSphere라 스킬·타워·스플래시가 자동 포함(투사체는 이미 transform 비행). 공중 유닛 제작 시 적용하고, 현행 부양 몬스터도 콜라이더 하단이 구 반경까지 닿는지 확인
- **WL-069** | 부분 해소(feat/balance) | PR#151 | 도로 전용 스킬 시전이 GDD §5.5("원하는 위치를 클릭")·이슈#103("전투 바닥으로만 제한")을 넘는 신규 지형 게이팅이던 문제 → **도로 전용 게이팅 제거**: 전투 타일 위이면 종류 무관 시전(유효/무효 색도 제거, MouseManager `UpdateSkillTargeting`). #103의 "전투 바닥으로만 제한"과는 일치. 다만 전투 타일 한정이라 GDD §5.5의 "원하는 위치 어디나"와는 차이 잔존 | 전투 타일 밖 시전 허용 여부·§5.6 장판형 보상과의 관계 팀 사인오프
- **WL-070** | RESOLVED(PR#170) | #139/#166 | 경영 영토 의미 변경(GDD v0.3) **재편 완료(#166/PR#170)**: 영토 = "고유 효과 지급 노드" → "미개척 영지, 확보 시 고유 자원을 매일 자동 수급". 구 효과 SO 계층(`TerritoryEffect`/`GrantResourceEffect`/`GainResidentEffect`/`ProductionMultiplierEffect`/`TerritoryEffectContext`) **삭제**, `TerritoryDefinition`을 자원 영지 SO(`Kind`/`IslandPrefab`/`Min·MaxDaily`)로 리셰이프. 확보 즉시 지급(`OnNodeClaimed`→`ApplyAll`) 제거 → `ManagementController.HandleNightToDay`가 Owned 노드 순회 `ResourceWallet.Add(Kind, DailyYield)`(주민 배치 무관, 시드 결정성 롤). WL-030(효과 Apply 배선) 대상 코드 소멸로 종결. GDD §5.3·TerritoryGraph.md §0/§5·Resources.md §5.5 정본화 완료 | (종료 — ProductionModifiers ×1 잔존·보상 훅 분리(WL-043/056)·자원 카테고리 데이터화(WL-074)는 별건)
- **WL-071** | OPEN(진전, PR#170) | #139/#166 | 상단 자원 UI(top bar) 4종 고정 문제를 **지갑 표기의 패널 고정 행 이관**으로 완화(#166): 탑바 자원 HUD 4종 비활성화, 보유량은 각 행 `ProductionLineView._balanceText`로 표시. 패널은 고정 행(기본 3종 +/- · 마나 · 특수 4종) + 미개방 회색 + 활성 우선 재정렬. **잔여**: (a) 탑바 HUD 오브젝트 완전 삭제(현재 비활성만), (b) 특수 자원이 4종 초과로 늘 때 스크롤/카테고리 UI, (c) 지갑 표기가 Imported ProdRow `_balanceText` 배선에 의존(WL-040) | 탑바 HUD 삭제 + 동적 자원 목록 UI(스크롤/카테고리) 방향 논의 후 GDD §4.1·Resources.md 반영
- **WL-072** | CLOSED | PR#158 | 타워 정보 패널(TowerInfoUI) 정본 GameScene.unity 미반영으로 인한 타워 선택 시 NRE 위험 — d5b7e77 "테스트 게임씬 반영"에서 정본 씬에 TowerInfoPanel 반영 완료(GUID 649570270374 1건, \_towerInfoText 자식 참조 배선·활성 상태 확인). Instance non-null로 NRE 시나리오 소멸, #153 완료기준③ 충족 | 해결
- **WL-073** | OPEN | PR#165 | 우클릭이 카메라 드래그(CameraController2)와 배치/스킬 조준 취소(MouseManager)에 이중 점유 — 조준 모드 중 우드래그 시 취소+카메라 이동 동시 발생. raw Mouse 폴링 이원화 | MouseManager 상태 질의로 조준 중 카메라 드래그 억제 or 카메라 드래그 중버튼 이전
- **WL-074** | OPEN | PR#170 | 자원 카테고리(기본/통화/특수) 지식이 `ResourceData`(CSV)가 아니라 뷰(`ManagementPanelView.SpecialKinds()`)에 이름 하드코딩(`kind != Wood/Iron/Food/Mana`) — 계약 #2(수치·분류=데이터 소유) 축. GDD가 자원 종류·해금 순서를 TBD로 둔 상태(§9)에서 신규 기본/통화 자원 추가 시 특수 자원 행으로 조용히 오분류됨. 또 표시명 키 규약 `ResourceNameKey(kind)=game.resources.{종류소문자}`가 `ManagementPanelView`·`TerritoryNodeView` 2곳에 중복 + `ResourceTable.csv`의 `NameKey` 정본과 3중 표현(WL-060 계열 조용한 조회 실패 트랩) | `ResourceData`에 `Category` 필드(CSV 컬럼) 도입해 뷰가 질의 + 표시명 키 단일 출처화(muchan/DataTable 합의)
- **WL-075** | OPEN | #169 | 버프 스킬 데미지 배율 미적용: `Tower.TryAttack`(Tower.cs)이 투사체 `Init`에 배율 적용 프로퍼티 `AttackDamage`가 아니라 에셋 원값 `atk.AttackDamage`를 전달 — `ApplyBuff`의 damageMultiplier(1.3x)가 실데미지에 무효(공속 배율은 `AttackInterval` 프로퍼티 경유로 정상 적용). WL-050(배율 스택 계약)·WL-051(버프 스킬 정체성)과 별개의 실효성 버그 | `atk.AttackDamage` → `AttackDamage` 한 단어 수정(Combat 소유 — SUNGSOO 협의 후)
- **WL-076** | OPEN(진전, PR#198) | #194/#195 | 타워 합성 실행부가 선택 UI(#183) 없이 임시 `TowerWallet`로 독립 구현됨. **해결분**: 매칭 규칙 단일 출처화(`TowerFusionMatcher.BuildRequired`/`CanFuse` 공개) + `TowerWallet.OnChanged`(Add/Remove/Clear). **잔여**: (a) 레시피 카탈로그(전체 열거) 출처 미정 — #194 완료기준 미충족 상태로 `Closes #194`, (b) 지갑 항목이 외부 파괴(철거·사망)될 때 `OnChanged` 미발행 → #183 버튼 상태 stale | 카탈로그 단일 출처(`Resources.LoadAll<TowerRecipe>`) 확정 + `TowerWallet.Prune()` 추가 후 #183 인계
- **WL-077** | OPEN | PR#198 | 합성 결과 타워의 배치 위치·페이즈 규칙 미정: 재료 소모가 배치 확정 시점이라 재료가 점유한 타일에는 결과를 놓을 수 없고(`TowerPlacer.IsBuildable`), `TryFuse`에 낮/밤 게이팅이 없어 밤 중 합성 시 타워 순간이동이 가능. #183 명세는 낮 전용을 요구, GDD §8은 TBD | 결과 배치 위치 규칙과 낮/밤 허용을 GDD §5.8/§8에 확정 후 `TryFuse` 진입 게이팅 추가

- **WL-078** | OPEN | PR#199 | 호버 툴팁 뷰 이원화: 이슈#141이 지정한 재사용 자산(`TooltipUI.Instance.Show(TooltipContent)`, #38)을 쓰지 않고 타워 전용 `TowerTooltipView`를 신설 — 전용 오버레이 캔버스(`sortingOrder=short.MaxValue`)를 런타임 코드로 자체 생성. 분리 근거인 아이콘 슬롯은 `TowerAsset`에 Sprite 필드가 없어 아직 플레이스홀더. 결과: (a) UIManager 흡수 대상이 6종째이자 유일한 비-씬(코드 생성) 뷰 → 흡수 시 프리팹 재작성 별도 작업(WL-002 축), (b) 건물 툴팁(팔레트 SO 색) vs 타워 툴팁(하드코딩 색)의 룩 이원화, (c) 저장소 최초의 코드 생성 UI로 "씬 authored UI + SceneWorkflow §4 정본 확정" 컨벤션과 갈림(동기는 WL-072 실패 모드 차단으로 정당) | 툴팁 뷰 1개 유지 여부를 n0wst4ndup과 확정. 2개 유지 시 스타일 소유권·색 규약을 SystemMap §2에 등재. 코드 생성 UI를 채택하면 SystemMap §6에 허용 조건 명문화 + 색/폰트/오프셋을 SerializeField/설정 SO로 외부화
- **WL-079** | OPEN | PR#199 | 타워 스탯 표시 규칙 다중화: `TowerType→AttackFields` 해석이 `Tower.cs:95`(`Attack`)와 `TowerTooltipView.cs:251`(`AttackOf`) 2곳, `game.tower.attack_damage/range/speed` 라벨 조립이 `Tower.cs:83-92`·`AuraTower.cs:76-90`·`TowerTooltipView.cs:213-229` 3곳에 복제. 같은 성질의 중복을 `TowerAsset.MagicRadius`(TowerAsset.cs:28-35)가 이미 SO 단일 출처로 해결한 선례가 있음. 설명 텍스트(`DescriptionKey`) 표시는 `a35bdca`에서 추가돼 이슈#141 완료조건③은 충족 — 잔여는 Magic 타워 효과 요약(`AuraTower.BuildStatsText`의 DoT·배율 서술)이 툴팁에서 재사용되지 않고 사거리 한 줄로 대체되는 것. WL-026(GDD 미기재 택소노미)이 UI 표시 로직까지 확대되는 경로 | `TowerAsset.CommonAttack` 프로퍼티(muchan 합의) + 스탯/효과 텍스트 포매터 1곳으로 승격 후 `Tower`/`AuraTower`/툴팁이 공유
- **WL-080** | OPEN | PR#201 | 스킬 런 종료 취소가 `SkillManager`/`SkillBomb`에 개별 하드코딩돼 GDD §5.5(GDD.md:184)·`SkillEffect.cs:9`의 "새 효과 추가 = 파생 1개 + 부착, 스킬·매니저 무수정" 확장 계약과 축이 어긋남 → 지연·지속 성질의 새 효과(#169 원안 정지, GDD §5.6 장판형) 추가 시 취소 누락 재발. 이번 PR로 폭탄 인스턴스당 종료 신호 구독은 2개(OnNightToDay+OnResultDecided)로 증가. 부수: 조준 모드 취소는 `OnDayStart`만 구독해(PhasePanelSwitcher.cs:23) 승리 시 고스트 잔존(무해) | `SkillEffect`에 `OnWaveEnd()` 훅 + 베이스/`SkillEffectManager`가 종료 신호 1회 구독해 브로드캐스트, `BombEffect`가 생성 폭탄 정리. 조준 취소도 같은 종료 신호에 묶기
- **WL-081** | OPEN | PR#203 | 타워 스탯 modifier 축 이원화: 소스별 합산 원장(`Tower.activeBuffs`/`RecomputeBuffs`, #164 — 공격력·공속 배율만)과 타일 버프(`TowerTileBuff`, Flat+Percentage — 사거리·공격력·공속)가 별개 경로로 병존. 합성 규칙은 코드로 정의됨(`Tower.cs:138-140` `(기본+Flat)×(1+%)×damageMultiplier`, `:190-195` 공속 배율 곱)이나 `Tower.ApplyBuff`는 여전히 사거리를 다루지 못함 → 이슈 #187 완료기준 ③(3종 일반화) 미충족. 버프 스킬(#103)·버프 오라 타워(#164)가 사거리 버프를 추가하는 순간 두 경로를 재설계해야 함 — WL-050(공통 modifier 계약)에서 멀어지는 방향 | `BuffEntry`에 사거리 축 추가 후 타일 버프를 `ApplyBuff(duration<=0)` 경로로 흡수(또는 현행 합성 규칙을 SystemMap §2에 명문화) — SUNGSOO 사인오프
- **WL-082** | OPEN | PR#203 | 버프 타일 전용 프리팹 4종이 버전 관리 밖(`BT_NormalGrass`/`BT_Range_1~3`의 `prefab` GUID `01a0e597…`/`45e75e66…`/`2ec32517…`/`552d9107…`가 추적 파일에서 미해결, `.gitignore:162`가 `Assets/Imported/` 제외). `CombatMapTileSpawner.cs:227`이 `?? grassTilePrefab`으로 조용히 폴백해, Imported 미동기 환경에서는 버프 타일이 일반 잔디와 시각적으로 구분되지 않은 채 사거리만 붙음. WL-045/065(프로덕션 SO가 비추적/개인 폴더 에셋 참조)와 동일 축 | `GetPrefab`에 프리팹 미해결 LogWarning 추가 + 전투 타일 프리팹 소유 위치(Imported vs `Assets/Prefabs`)를 SystemMap §4에 명문화
- **WL-083** | OPEN | PR#203 | 맵 생성 SO `Validate`의 fatal 판정 범위 설계: `TileBuffRuleSettings.Validate`에 AttackSpeed+Flat 거부를 추가하고 에셋 행을 안 지워 전투맵 생성이 통째로 막힌 사례(c963964 → baa11a0에서 해소), `BuffTileSpawnPool.Validate`가 버프 타일 `Prefab == null`을 fatal로 처리해 Imported 미동기 환경에서 맵 0타일이던 사례(6d9b287에서 해소 — 프리팹을 선택 사항으로 확정). 두 사례 모두 컴파일·에디터 검사로 안 잡히고 Play해야 드러남 | SO `Validate`에 제약을 추가하는 커밋은 참조 에셋을 같은 커밋에서 함께 수정 + "데이터 오류는 fatal, 환경 차이는 경고+폴백" 기준을 SystemMap §6에 명문화. `CombatMapGenerationSettings` 계열 EditMode 검증 테스트 1본(팀 최초 테스트 후보 — `TileBuffCalculator`가 씬 비의존이라 가장 싼 출발점)
- **WL-084** | OPEN | 전역 | TMP 정본 `Assets/TextMesh Pro/Fonts/PretendardJP SDF.asset`이 기능 PR마다 동적 아틀라스 증분으로 오염(PR#203에서 누적 +2025/-5까지 갔다가 되돌림). `m_AtlasPopulationMode: 1` + `m_ClearDynamicDataOnBuild: 1`이라 **빌드 시 버려지는 데이터**인데, 1024×1024 아틀라스의 `_typelessdata` 1MB 단일 라인이 diff에 포함돼 동시 편집 시 수동 병합 불가. WL-041(폰트 머지 충돌)의 정본 버전 | 정적 아틀라스 전환 검토 + 폰트 파일은 의도적 폰트 작업 PR에서만 커밋하도록 SystemMap §4에 명문화
- **WL-085** | 갱신 제안 | PR#218 | 순수 View 유틸 거주지 문제에 `OutlineHighlight`/`OutlineKind`(`Assets/Scripts/GameManager/MouseManager/Highlight/`) 추가 — 소비자가 4곳(TowerGroupSelectable·TowerMergeCoordinator·드라이버·**TerritoryNodeView(#213 §5.4 구현 완료)**)으로 확정. WL-085의 "3번째 소비자 합류 전 중립 위치 결정" 조건 충족 | `RangeCircle`과 함께 중립 위치(`Assets/Scripts/View/`)로 이전 + SystemMap §2 공개 API 등재
- **WL-086** | OPEN | PR#210 | 모드 전환 시 선택 미해제: `MouseManager.BeginPlacement`(`:104-109`)/`BeginSkillTargeting`(`:123-131`)이 `ClearHover()`는 호출하나 `_selected`는 유지 — `OnDeselected()`는 Idle의 `Select()`(`:178-185`)에서만 발화. PR#210이 선택 상태를 월드 공간(초록 사거리 원)에 가시화하면서 "배치·조준 중 이전 타워의 선택 원 + 정보 패널 잔존"으로 드러남(이전엔 텍스트 패널뿐이라 무해). WL-011(선택 통지 이중 경로) 축 | **배치·페이즈 해소(PR#226)**: `ClearSelection()`(= `Select(null)` + `OnPrimarySelect(null)`)을 public으로 올려 Esc·`BeginPlacement`·`PhasePanelSwitcher.ShowNight` 3곳이 공유한다 — 단일뿐 아니라 그룹까지 비워야 합성 패널·그룹 초록도 함께 내려간다. 합성 경로는 이 해제 **전에** 소모 대상을 스냅샷해 핑크를 유지(InteractionOutline.md §5.3). 페이즈 표면은 자동 리뷰(PR#226)가 짚었다: 코디네이터 `HandleDayToNight`만 표시를 내리면 `_selected`가 남아 **밤에 그 타워를 재클릭해도 안 뜨고**(`Select`의 중복 제거가 삼킴) 초록만 잔존 → §4-4 불변식 위반. **잔여: `BeginSkillTargeting`은 여전히 호버만 해제** — 같은 한 줄이면 종결(n0wst4ndup)
- **WL-087** | CLOSED | PR#210 | 선택 훅 비대칭이 월드 시각물을 잔존시킴: `TowerMergeCoordinator.RefreshPanel`(`:157-178`)이 `count==1`에서 `t.OnSelected()`를 직접 호출(`:173`)하나 `count==0`/`count>=2` 전이에서 `OnDeselected()`를 부르지 않고 `TowerInfoUI.HideInfo()`만 한다. #183 시점엔 선택 표시가 정보 패널뿐이라 무해했으나, PR#210이 같은 훅 쌍에 사거리 원 `Show/Hide`를 묶으면서 "Shift로 담았다 뺀 타워의 초록 사거리 원이 월드에 잔존"으로 드러남 — Shift 경로는 `MouseManager._selected`를 안 건드리므로 이후 빈 곳 클릭(`Select(null)`)도 `_selected==null`이라 해제하지 못한다. **PR#210 브랜치에서 Shift 담기→빼기로 실측 재현 확인(2026-07-27, KIM-SUNGSOO)**. WL-011(선택 통지 이중 경로)·WL-086과 동일 축 | 종결. 제안대로 `RefreshPanel`에 `_infoShownFor` 슬롯 추가(대상 변경/0·2+ 전이 시 `OnDeselected()`) + **Shift 토글 발행 직전 `Select(null)`**(MouseManager.cs) 2단 수정. 후자는 스위처가 `Tower`만 알아서 못 잡는 표면(`AuraTower`·건물을 선택해 둔 채 Shift로 타워를 담는 경로)을 덮는다. 잔여 동일 축은 WL-086(배치·조준 모드 진입 시 미해제)뿐
- **WL-088** | CLOSED | PR#214 | 전투맵 "본진/스폰 좌표" 계약 미명시 — `CombatMapData.BasePosition` 공개 프로퍼티 신설 + `GrassEroder.IsNearBase`/`BaseProtectionRange` 개명 + 소비처 5파일 전환으로 본진 측 해소. 스폰 측은 모호한 `CombatMapData.SpawnPosition`을 신설했다가 삭제해(9fab566) 창구를 `CombatMapRevealController.CurrentSpawnPosition`(`:54,173`) 하나로 통일 | 종결. 잔여는 SystemMap §2 등재뿐 → WL-091
- **WL-089** | OPEN | PR#214 | 본진 보호 밴드가 침식(`GrassEroder`)에만 적용되고 물 생성(`WaterGenerator`)은 이를 모름 — 파이프라인상 Water가 침식 뒤에 돌고(`CombatMapGenerator.cs:257→268`), `GenerateWaterCandidates`는 `IsNearRoad(clearance=2)`만 제외(`WaterGenerator.cs:142`)해 본진 맨해튼 5 이내·Road에서 3~5칸 잔디가 Water 후보로 남음. 이슈#189 완료기준②(본진 주변 끊김 없는 연결)가 시드 의존적으로 미달하고, Water 타일 배치 불가(WL-067)로 본진 앞 방어 면적이 시드마다 흔들림. 보호 반경 외부화(`CombatMapGenerationSettings.BaseProtectionRange` + `Validate`)는 해결됨 | 보호 판정을 `CombatMapData.IsInBaseProtection()`으로 승격해 Water/버프타일이 공유 + `RunStabilityTest`에 "밴드 내 non-Grass/non-Road == 0" 카운터 추가(검증 수단이 없으면 수정 확인 불가 — 한 이슈로 묶을 것, SUNJIN)
- **WL-090** | CLOSED | PR#214 | 경로 생성 규칙의 좌표 리터럴 결합(`nextPosition.x == 0`) — `IsBlockedBaseEdgePosition`(`RouteGenerator.cs:328-351`)이 본진 좌표에서 가장자리를 파생하고 `position == goal` 예외 포함, 모서리 본진 시 축을 하나만 선택하는 조기 반환으로 봉쇄 경로도 해소 | 종결. 모서리에서 수직 축이 우선한다는 점과 본진이 내부 좌표면 규칙이 비활성화된다는 점은 주석 2줄 권고(강제 아님)
- **WL-091** | OPEN | PR#214 | 리뷰 기준선에 `CombatSpace/Map` 시스템 부재: SystemMap §1의 SUNJIN 행이 구 `CombatSpace/MapBuilder`(7×7 블록)만 등재하나 정본 `GameScene`이 돌리는 것은 `CombatSpace/Map`의 `CombatMapGenerator` 파이프라인이며, 그 공개 표면(`CombatMapData`·`CombatMapGenerator.CurrentMap`·`CombatMapTileSpawner.CurrentWorldEnemyRoute`·`CombatMapRevealController`)이 §2에 한 줄도 없음 — 이미 MonsterSpawn·MouseManager·TowerPlacer가 소비 중. 기준선에 없는 시스템은 자동/수동 리뷰가 접점을 못 짚고, PR#214의 본진 좌표 계약 공백이 정확히 이 사각지대에서 발생(WL-003 잔여) | 팀 계약 #7에 따라 §1에 `CombatSpace/Map(절차적 전투맵 생성, SUNJIN)` 행 신설 + §2에 위 4개 공개 API 등재(별도 문서 PR 가능)
- **WL-092** | 재개봉 | PR#218 | `ProjectSettings/EditorBuildSettings.asset`의 App UI config object(guid `99f9c949…`, 대상 에셋 저장소에 없음)가 `5b0a114`에서 다시 등록됨 — PR#212가 `origin/main` 버전으로 원복해 해소했던 dangling 참조의 회귀(WL-084 churn 축) | 해당 한 줄 원복 + 에디터가 자동 기록하는 전역 ProjectSettings 필드는 커밋 전 확인
- **WL-093** | RESOLVED(PR#212) | PR#212 | `Enemy.cs:50`이 `IMovementAgent`를 루트에서만 탐색(`GetComponent`)해, `MonsterMove`를 자식에 둔 프리팹에서 `movement==null` → 보스 속도 가감속 무동작 + `Awake`의 `SetMoveSpeed` 누락으로 `fallbackMoveSpeed=3f` 이동(WL-036 경로 재개봉). 예외·로그 없는 조용한 실패 | **해소**: `Enemy.cs:50`을 `GetComponentInChildren`로 통일(line 61·`MonsterSpawn.cs`·`MonsterStateMachine.cs`와 일치) + `movement` 미발견 시 `LogWarning` 추가
- **WL-094** | OPEN(진전, PR#212) | PR#212 | 보스 패턴 밸런싱 수치(HP 임계 0.3 / 회복 MaxHp 20% / 배수 ×2·×0.5 / 램프 1.5s / 대기 2·5s)가 CSV·SO가 아니라 BT 그래프 에셋 `MidBossBehavior.asset` 노드 입력에 authoring — WL-015(타워)·WL-027(몬스터 스탯)과 동일 축의 3번째 확대. GDD §9(`GDD.md:261` 스테이지/보스 구성 미확정)에 강결합. **진전(PR#212)**: `EnemyAsset.BossFields.BehaviorTree`를 `Object` placeholder → `Unity.Behavior.BehaviorGraph`로 타이핑 확정(muchan 소유 필드) + `Enemy.Awake`가 보스일 때 `BehaviorGraphAgent`를 확보(없으면 부착)해 SO의 그래프를 주입 → 보스↔그래프 연결이 tracked 파일(`ogre_king.asset`)로 이동, 크로스 repo(Imported) 취약성 해소. 잔여: 밸런싱 수치를 그래프 Blackboard 변수로 승격해 그래프 1본을 보스 N종이 공유 | 수치 Blackboard 승격(후속) + GDD §9 보스 구성 확정 시 재검토
- **WL-095** | OPEN | PR#212 | 보스 BT 실행 경로(`Enemy.cs:82-91`: 보스면 `BehaviorGraphAgent` 확보 후 SO의 `BehaviorGraph` 대입)의 Play 검증 근거가 없다 — PR 본문 테스트 절은 프리팹 인스펙터 배선 경로(커밋 `0422373`) 기준이고 데이터 주도 리팩터는 `0bec613`. 리스크 2축: (a) `AddComponent`가 에이전트 `OnEnable`을 그래프 null 상태로 동기 실행하므로 `Graph` setter가 재초기화하지 않으면 패턴이 무증상 무동작, (b) `Enemy.cs:90`이 `Resources` 공유 SO의 그래프 서브에셋을 클론 없이 대입 — 에디터 플레이 중 `MidBossBehavior.asset` mutate → git churn(WL-084/028 축), 그리고 보스가 2마리 이상이 되면 블랙보드 상태(회복 1회 플래그·램프 진행) 공유. **검증(PR#212, 패키지 소스)**: `BehaviorGraphAgent.Init`(`:368`)이 `BehaviorGraph.AcquireInstance`→`ScriptableObject.Instantiate`(BehaviorGraph.cs:152)로 **에이전트가 자동 클론**하므로 (a)(b) 모두 구조적으로 닫힘 — 실행은 클론 대상, 상태는 per-agent 독립. 수동 `Instantiate` 추가는 이중 클론·누수라 불필요. 잔여: Play 1회로 실제 동작 실증 | 7웨이브 Play로 패턴 동작 + 종료 후 `MidBossBehavior.asset` non-dirty 확인해 본문 테스트 절 갱신
- **WL-096** | OPEN | PR#212 | 중간보스가 **최종 웨이브(7)** 에 등록됨(`MonsterWave 7.asset:30-31`, `count: 1`) — `MonsterSpawnWaveProvider.cs:105` `FinalWaveNumber=max`=7이고 `WaveCompletionCoordinator.cs:52-61`이 최종 웨이브에서 보상 3택1·`EndNight`를 건너뛰고 `TriggerVictory()`를 호출하므로, 중간보스가 GDD §7(`GDD.md:95`,`:132`)의 최종 보스 자리를 잠정 점유하고 중간보스 처치 보상(`GDD.md:200` 이벤트성 지급 TBD)이 구조적으로 붙을 수 없는 위치가 됨. 웨이브 구성·승리 판정 소유는 SUNJIN. 더불어 웨이브 SO가 개인 WIP 폴더(`Assets/Personal/SUNJIN/ScriptableObjects/Wave/`)에 상주하는데 다른 소유자가 편집 — WL-045/065와 동일 축 | 중간보스를 중간 웨이브로 이동(또는 최종 보스용 웨이브 8 추가) — SUNJIN 사인오프 + 웨이브 SO 7본을 `Assets/Resources/ScriptableObjects/Wave/`로 이관
- **WL-097** | RESOLVED(PR#212) | PR#212 | 보스 `BehaviorGraphAgent`가 `Awake` 지역 변수여서 게임 종료·사망 시 그래프 틱을 멈출 핸들이 없었음(팀 계약 #5, WL-019/044 축) | **해소**: `Enemy.cs:37` 필드 승격 + `:154-157`(`Result != Playing`)·`:211-214`(`Die`) 두 지점에서 `enabled = false`. 부수 해소: 사망 후 `SetSpeedMultiplier` 호출 경로가 끊겨 메서드별 `isDying` 가드 불필요
- **WL-098** | OPEN | PR#217 | 전역 시간 스케일 컨트롤러의 공개 창구·문서 미확립: `GameSpeedController`(`Assets/Scripts/UI/GameSpeedController.cs`)가 `Time.timeScale` 단일 쓰기 지점(`:166`,`:232`)이 되고 `static Instance`(`:30`)·`HashSet<GamePauseReason>`(`:28`, WL-050 실패 모드 회피)까지 갖췄으나, (a) `OnSpeedChanged` 이벤트가 없고 유일한 소비처가 여전히 SerializeField 단독(`WaveRewardSelectionUI.cs:38,95`)이라 `Instance` 소비자 0건, (b) SystemMap §1/§2 미등재 + §1 WaveReward 행(`SystemMap.md:17`)이 아직 "timeScale 0 정지"를 보상 UI 소유로 기술(팀 계약 #7, WL-032/WL-091 축) | `OnSpeedChanged` 노출 + SystemMap §1/§2 등재 및 WaveReward 행 정정
- **WL-099** | OPEN | PR#217 | 결과 화면(게임오버/승리) 시간 정지 부재: 저장소 전체 `Time.timeScale` 쓰기가 `GameSpeedController.cs:166`,`:232` 2곳뿐이라 `GameManager.TriggerGameOver()`/`TriggerVictory()` 이후에도 밤 전투가 현재 배속(최대 x4)으로 계속 진행됨. `GamePauseReason.GameOver` enum 값은 이미 준비돼 있음 | 결과 판정 시 `GameSpeedController.Instance.SetPaused(GamePauseReason.GameOver, true)` 경유 — GameManager 소유자(muchan/SUNGSOO) 사인오프, WL-018/WL-096 승리 판정 경로와 세트
- **WL-100** | OPEN | PR#217 | 배속 적용 범위 미결정: x1/x2/x4가 전체 게임에 적용되어 낮(경영) 공간 연출도 스케일된다(`ManagementSpace/Territory/View/TerritoryEdgeShip.cs:68,87`, `VortexVisual.cs:91,99`, `CombatSpace/Map/CombatMapTileSpawner.cs:451`). 현재는 무해하나 GDD §4.1 경영 페이즈에 시간 기반 생산(주민 작업 진행도·자원 채집 틱)이 붙으면 배속이 곧 생산 배율이 되어 밸런싱을 4배로 흔듦 | 이슈 #190 열린 결정("전체 vs 밤만") 결론을 GDD §4에 명시 + 시간 기반 생산은 프레임 시간이 아닌 페이즈 이벤트/틱 카운트로 구현하도록 규약화 — 경영 시스템 담당자 사인오프
- **WL-101** | RESOLVED(PR#223) | PR#216, PR#223 | 씬 collateral 재저장: 기능과 무관한 `GameScene.unity` 값이 PR에 딸려와 다른 팀원 작업을 되돌림 | **해소**: PR#223이 정본 씬 diff를 의도 hunk만 남도록 정화(레이아웃 구동 RectTransform 재직렬화 20여 건 제거, TMP `m_fontSize` 원복) + `Docs/Core/SceneWorkflow.md` §4에 커밋 전 씬 diff 셀프 체크 절차 문서화. 단 TMP `m_fontSize`는 `m_enableAutoSizing: 1`인 오브젝트에서 오토사이징 캐시값이므로 회귀 후보에서 제외(이 축은 무해). 재발 시 재개봉
- **WL-102** | RESOLVED(같은 PR#216) | PR#216 | 마법 연구소 레벨 데이터가 두 파일로 이원화돼 있었음: 레벨 개수·비용은 `magic_lab.asset`(SO, n0wst4ndup 축), 레벨→스킬 배율은 `GameScene.unity`의 `SkillManager`/`BuffSkillManager` 인스펙터 리스트(muchan 축) — 서로 검증하지 않아 SO에 레벨을 추가하면 스킬 배율이 조용히 base(x1.0)로 되돌아가는 위험이 있었음(범위 밖 clamp로 1차 방어 후, 근본 해결로 데이터 자체를 이관) | **해소**: `BuildingAsset.SkillUpgradeLevel`에 배율 필드 7종(`DamageMultiplier`/`RadiusMultiplier`/`CooldownMultiplier`, `BuffDamageMultiplierScale`/`BuffAttackSpeedMultiplierScale`/`BuffDurationMultiplier`/`BuffCooldownMultiplier`)을 `Cost`와 같은 리스트에 추가해 단일 authoring화. `SkillManager`/`BuffSkillManager`는 씬 리스트 없이 `_magicLabAsset.Skill.UpgradeLevels`를 직접 읽음 — 레벨 개수 불일치 자체가 구조적으로 불가능해짐, 씬 파일도 더 이상 배율 데이터를 갖지 않아 WL-101 재발 경로 하나가 줄어듦
- **WL-103** | OPEN | PR#218 | WL-062 재발(2번째, **유지 결정**): 정본 `Assets/Scripts/Editor/OutlineSmoothMeshBaker.cs:159`가 벤더 네임스페이스 `FlatKit.MeshSmoother`를 컴파일 타임 직접 참조 — `.gitignore:162`가 `Assets/Imported/` 제외라 벤더 트리 미동기 환경에서 Assembly-CSharp-Editor 컴파일 실패 → Unity가 Play 모드 진입 차단(산출물이 커밋돼 있어도 도달 불가). 소유자 판단으로 유지, 근거·해소 경로는 `Docs/Core/InteractionOutline.md §2.5(1)`. 단 해소 경로(베이커를 `Assets/Imported/@NorthLand`로 이동)는 1급 툴을 비추적 중첩 저장소로 내보내므로 WL-045/065/082 축을 새로 만든다 | 이동 시점까지 리플렉션 덕타이핑(`TerritoryNodeStateVisual.cs:200-207` 선례)으로 임시 차단, #148에서 이동과 함께 종결
- **WL-104** | OPEN | PR#220 | 페이즈 전환이 경영 UI 갱신 신호를 발행하지 않음: `ManagementController.SubscribeDayNight`(`:509-519`)가 `OnNightToDay`만 구독하고 `OnDayToNight`를 구독하지 않아 낮→밤 전환에서 `OnChanged`가 발행되지 않는다. `IsDay`를 게이트로 쓰는 모든 버튼 상태(`CanUpgrade`/`CanUpgradeBuilding`/신규 `CanExchange`)가 밤에도 활성인 채 남고, 눌러야 로그와 함께 실패한다(자원 누수는 없음). #211에서 상점이 3번째 소비자로 합류하며 `Resources.md`의 "밤에는 버튼도 함께 비활성화" 명세와 어긋남 | `SubscribeDayNight`에 `OnDayToNight += () => OnChanged?.Invoke()` 추가 + `OnDestroy` 대칭 해제(muchan)
- **WL-105** | OPEN | PR#226 | `TowerPlacer.keepPlacing`(`:32`)이 일반 배치와 합성 결과 배치가 **공유하는 단일 인스펙터 필드** — true면 `KeepPlacingAfterConfirm`(`:205`)에 그대로 실려 합성 확정 후에도 배치 세션이 끝나지 않는다. `_onConfirmed`는 확정 시 1회 소비되므로(`:322-324`) 재료는 한 번만 소모되고, 이후 클릭은 재료 없이 `ExtraCost`만으로 결과 타워를 반복 생성한다. #195부터 있던 복제 구멍이나, PR#226이 핑크 고정 해제(`_onEnded`→`EndMergeCommit`)를 같은 경로에 얹어 **소모된 재료 자리에 핑크까지 잔류**한다. 배치 확정 경로가 시각·소모 상태를 함께 지고 있는데 그 수명이 인스펙터 플래그 하나에 달려 있다는 점에서 WL-011(선택 통지 이중 경로) 축 | `BeginTowerPlacement`에 연속 배치 여부 인자를 받아 합성 경로는 항상 false 고정(한 줄) + 정본 `GameScene`의 `keepPlacing` 실제 값 확인. 자동 리뷰(PR#226) 지적 (n0wst4ndup)
- **WL-106** | 관찰 | PR#226 | `MouseManager.OnPrimarySelect`의 의미 확장: 선언 주석(`:25-28`)은 "평클릭·Esc·빈 곳 클릭"(사용자 입력)이라 못 박는데, `ClearSelection()` 승격으로 **모드 전환(`BeginPlacement`)·페이즈 전환(`ShowNight`)** 도 같은 채널에 `null`을 태운다. 구독자가 코디네이터 1곳이고 반응(집합 비우기)이 정확해 현재는 무해. SystemMap §1 계약이 예고한 **병사 배치**가 같은 모드 축으로 들어오거나 GDD §5.8 보상/재시도 UI가 붙으면 "사용자가 빈 곳을 클릭했다"(게임플레이 의미 있음)와 "시스템이 정리했다"(의미 없음)를 구분해야 하는 구독자가 생긴다 | 지금 쪼개지 말 것 — SystemMap §2에 "`ClearSelection()`이 선택 해제의 유일한 창구"를 명문화해 두었고(PR#226), **3번째 소비자 합류 시점**에 `OnSelectionCleared` 분리를 결정한다(WL-085와 동일한 판단 시점 패턴). 자동 리뷰(PR#226) 지적
- **WL-107** | OPEN | PR#223 | UI 전역 정렬 표준(`Docs/Core/UIZOrder.md` §3: UICanvas 100 / RewardCanvas 500 / ResultCanvas 900)이 **씬 authored Canvas만 다루고 코드 생성 Canvas를 포함하지 않음** — `Assets/Scripts/UI/TowerPanel/TowerTooltipView.cs:89`가 `sortingOrder = short.MaxValue`(32767)로 루트 Canvas를 런타임 생성해 결과 화면(900) 위에 렌더된다(저장소 전체에서 `sortingOrder` 코드 쓰기는 이 1곳뿐, WL-078의 코드 생성 UI 축 연장) | §3에 "코드 생성 오버레이"(예: 950) 행 추가 + `TowerTooltipView` 값 이관(n0wst4ndup 합의)
- **WL-108** | RESOLVED(PR#223) | PR#223 | 씬 번호 스냅샷 이중 저장 | **해소**: `Assets/Personal/SUNJIN/Scenes/GameScene15.unity`(16,667줄) 삭제, `Assets/Scenes/Branches/GameScene_15.unity`만 유지. 개인 폴더의 `GameScene13/14`는 이 PR 이전부터의 잔재로 별도 정리 대상
- **WL-109** | 관찰 | PR#223 | `Assets/Personal/SUNGSOO/Prefabs/ResultUI.prefab:1051`의 authored `m_LocalScale: 0` — 프리팹 루트가 스스로 Canvas라 프리팹 컨텍스트에서는 구동되어 무해하지만, `ResultCanvas` 아래 중첩된 지금은 씬의 `m_LocalScale = 1` 오버라이드 1건만이 결과 UI 가시성을 지탱한다. 오버라이드를 Revert하면 게임오버 화면이 scale 0으로 접혀 사라지고, 씬 diff에서는 정상값으로 보여 리뷰로도 안 잡힌다(PR#223의 🔴이 정확히 이 구조에서 발생) | 프리팹 authored 스케일 `1` 정정 + 루트 Canvas/CanvasScaler/GraphicRaycaster 제거해 순수 패널 프리팹화(SUNGSOO, WL-065 축과 동반 처리)
- **WL-110** | RESOLVED(PR#236) | PR#236 | 건물 상세 패널의 닫기 버튼이 `MouseManager` 선택 상태를 남김: `StorePanelUI`/`CastlePanelUI`가 `_closeButton.onClick`에 `Hide`(=`SetActive(false)`)만 연결해 `_selected`가 잔존, `Select`의 중복 제거(`MouseManager.cs:239`)에 삼켜져 같은 건물 재클릭 시 패널이 다시 열리지 않았음 | **해소**: 두 패널 모두 `HandleCloseClicked` → `MouseManager.Instance.ClearSelection()` 경유로 교체(`CastlePanelUI.cs:122`, `StorePanelUI.cs:115`), `Instance == null`일 때만 `Hide()` 폴백. `onClick.AddListener(Hide)` 패턴은 저장소에서 소진(잔여 0건). 잔여 구멍: 대상 파괴로 `IsAlive` 가드에 걸리면 `HideAll`이 안 불려 패널이 열린 채 남는다 — 건물 철거 기능 도입 시 `ClearSelection()` 뒤 `Hide()` 무조건 호출로 방어
- **WL-111** | OPEN | PR#236 | 주민 증가 게이트웨이의 index 도메인 불일치: `TryIncreaseVillagers(BuildingAsset)`/`NextVillagerCost(BuildingAsset)`가 건물을 인자로 받지만 진행도는 전역 단일 `_bonusVillagers`(`ManagementController.cs:65`). 같은 컨트롤러의 업그레이드 트랙은 `UpgradeIndexOf`(`:241`)로 per-building 상태를 유지 — 시그니처는 per-building, 상태는 global로 갈렸다. 주민 증가 데이터를 가진 두 번째 건물이 생기는 순간 조용히 어긋난다 | 파라미터 제거(전역임을 명시) 또는 asset→index 매핑 도입 중 택1 (muchan)
- **WL-112** | OPEN | PR#236 | 주민 상한 페이싱이 영토 시스템에 하드 종속: `castle.asset` 6~8회차 비용이 금/루비/다이아 **전용**(기본 자원 0)이라 주민 6명 이상은 해당 종류 영지 확보에 전적으로 의존한다(수급 경로는 `SettleTerritorySupply` 단독). 확장은 하루 1회 게이팅 + 프론티어 인접성 의존이라 진입 시점이 영토 RNG에 좌우된다. GDD §5.1에 "영토 확장이 사실상 필수"로 신규 명문화됨 | 후반 회차에 기본 자원 혼합 여부 + §5.3 페이싱을 muchan·n0wst4ndup 합의로 확정(수치 placeholder 단계에서 결정)
- **WL-113** | 해소 유지(PR#237) | PR#237 | (기존 내용 유지) 프리팹 자기배선이 정본 이관에서 효과 확인 — 정본 반영 시 수동 오버라이드가 `GameScene` 3건·`TitleScene` 2건으로 축소. 잔여: 프리팹이 `Assets/Personal/SUNJIN/Prefabs/` 상주(WL-045/065/096 축)
- **WL-114** | OPEN | PR#237 | (기존 내용 유지) 설정창 키보드 토글 부재 — 이슈 #221 완료기준 #1과 PR 본문은 여전히 ESC 표기이고 `Closes #221`로 자동 종료 예정. `Docs/Core/UIZOrder.md:132`의 "중앙 Cancel 우선순위 확정 후" 보류 근거만 문서에 남아 있음
- **WL-115** | OPEN | PR#237 | (기존 내용 유지) `GameSceneManager` 위임 래퍼 6중 복제(`SettingUI.cs:77-115` / `ResultUIManager.cs:64-83,95-104`)
- **WL-116** | OPEN | PR#237 | 정본 씬 반영 시 인스펙터 참조 유실: `GameScene.unity:11111-11114`가 `SettingUI.gameSpeedController`를 `{fileID: 0}`로 덮어써 정본 씬에서만 설정창 정지가 동작하지 않음(브랜치 `GameScene_15.unity:16474`는 정상 연결). `SettingUI.cs:63,74`가 `?.`, `Awake`(`:38`)가 검사 면제라 로그 없이 무시됨 — 컴파일·diff·런타임 어디에도 신호가 없는 유형 | 정본 씬 참조 연결 + `SettingUI`에 "정지 사용" 의도 플래그를 두고 플래그 켜진 채 참조가 비면 `Awake`에서 `LogError` + SceneWorkflow §4② 절차에 "정본에서 1회 실측" 추가
- **WL-117** | OPEN | PR#237 | 로컬라이제이션 키 중복·표기 드리프트: `game.setting.close`("닫기")·`game.setting.replay`("다시하기")가 기존 `game.btn.close`·`game.btn.retry`와 값까지 동일하고, `game.setting.title`("타이틀")은 같은 동작인 `game.btn.title`("처음으로")과 문구가 이미 갈림. `game.setting.Laguage`는 오타이자 유일한 대문자 시작 키. String Table 4개 로케일 + Shared Data는 팀 전역 공유 자원(소유 n0wst4ndup) | 설정창 버튼을 기존 `game.btn.*`에 연결하고 중복 3개 제거, `game.setting.language`로 정정 (n0wst4ndup 사인오프)
- **WL-118** | 해소(PR#238) | PR#238 | BT 이동 소유권(`Enemy.MovementOwnedByBehavior`)이 초기 구현에서 `MonsterStateMachine.hasTarget`을 **해제하지 않고 통지만 차단**해, 교전 중(hasTarget=true, Attack 상태, `MonsterMove.canMove=false`) 소유권을 잡으면 `canMove`가 false로 고착돼 `EnemyAccelerateAction`이 `IsStopped=false`를 써도 보스가 움직이지 않았다. 원인은 `IsStopped`(`MonsterMove.cs:147`)와 `canMove`(`:159`)가 **독립 게이트**인데 노드가 전자만 만진다는 것 — 이동 owner가 `Enemy`·`MonsterStateMachine` 둘로 갈린 구조의 첫 노출. 실패가 조용하고(로그 없음) `EnemyMarkPatternUsedAction`이 이미 1회 래치를 소모하며 P4는 병렬로 계속 돌아 겉보기 정상이었다 | **해소**: 소유권 브랜치에서 `SetHasTarget(false)` 호출(`Enemy.cs`). 세 번째 owner를 만들지 않고 `Enemy` 안에 가두는 방향 유지. #235 Play 검증에서 "교전 중 P1 진입" 케이스 확인
- **WL-119** | OPEN | PR#238 | 보스 BT 노드 타이밍이 전부 스케일드 타임(`Time.deltaTime`/`Time.time`) — `GameSpeedController`(`:166`,`:232`)의 최대 x4 배속에서 P3 예고 `Duration`(프로토타입 0.5초)이 0.125초가 되어 **예고가 인지 불가능**해진다. 예고 드리프트 대응이 이 값에 의존하고 있어 배속이 대응을 무력화한다. BT 내장 `Wait`도 같은 성질이라 노드만 고쳐서는 해결되지 않는다. WL-099(결과 화면 시간 정지 부재)와 같은 배속 정책 축 | 배속 정책 확정 — "플레이어가 봐야 하는 구간"(예고·준비 모션)만 unscaled로 분리할지 결정(`GameSpeedController` 소유자 사인오프)
- **WL-120** | 관찰 | PR#238 | 보스 BT 리프 노드 14종 + 보조 타입 4종이 **전역 네임스페이스**를 점유(`EnemyAgent`/`EnemyNodeQuery`/`EnemyPatternMemory`/`EnemyUnitFilter`/`EnemyRelativeDirection`/`EnemyTargetKind` 등). SystemMap §5 현황("`NorthLand.Combat`만 존재, 나머지 전역")과 모순되진 않으나, 같은 폴더의 정본은 `NorthLand.Combat`이고 기존 MiniBoss 노드 4종은 `NorthLand.Combat.Boss`로 정상 동작한다 — 패키지 제약이 아니라 자발적 규약(`BossNodeReference.md` 「작성 규약」). asmdef 없이 전부 Assembly-CSharp이라 이름 충돌은 타 브랜치 머지 시점에 컴파일 에러로 터지고 원인 파일이 이 PR 밖으로 보인다 | 보스 2호 착수 전 `NorthLand.Combat.AI` 이관 여부 결정(노드 수가 늘수록 비용 증가). 현재는 의도된 규약이므로 관찰만
- **WL-121** | OPEN | PR#240 | `IDamageable.HitPosition`(Transform) 계약이 non-null을 명문화하지 않은 채 도입됨. `Enemy`는 `Awake` 폴백(`Enemy.cs:70-75`)으로 non-null을 보장하지만 인터페이스(`IDamageable.cs:7`)에 그 약속이 없고, 구현체 5종 중 2종(`DummyDamageable.cs:14`, `DebugDamageTarget.cs:16`)은 `throw new NotImplementedException()`이다. 커밋 `85972a7`이 `Projectile`의 `as MonoBehaviour` 3곳을 `HitPosition`으로 교체하면서 `HitPosition`이 Combat의 **대상 좌표 질의 단일 창구**로 승격 — 무가드 역참조가 `:86`(Ballistic) `:93`(Homing Init) `:211`·`:222`(Chain)로 늘었다. 특히 `:222`의 `next`는 `FindNearestUnhit`가 `OverlapSphere`로 고른 임의 구현체이고 `DummyDamageable`은 `Awake`에서 자기 레이어를 `Enemy`로 지정(`:18`)해 정확히 사냥감이 된다. `lightning_tower.asset:18`이 `TowerType: 2`(Chain)이므로 죽은 코드가 아니라 값 기입만 남은 예약 경로. 부수 축: `Soldier.cs:33`/`PlayerBase.cs:42`의 `=> transform`은 대상 파괴 후 `MissingReferenceException`(현재 `goblin_archer` 웨이브 미편성으로 미도달) | 스텁 2종을 `=> transform`으로 교체 + `IDamageable.cs:7`에 "null 반환 금지" 계약 주석 + SystemMap §2(`SystemMap.md:54`) `IDamageable` 행에 `HitPosition` 등재
- **WL-122** | OPEN | PR#240 | 범위 판정 원점이 피벗 → 몸통(`HitPosition`)으로 상승하면서, **밸런싱 결과값이 버전 관리 밖(`Assets/Imported/`) 프리팹의 자식 Transform 로컬 Y에 좌우**된다. Area: `OnHit(targetPos)`(`Projectile.cs:137`)가 조준점을 그대로 `ApplyArea`의 `OverlapSphere` 중심으로 넘김 — `cannon_tower.asset:39` `SplashRadius: 4`가 실사용 값. 몬스터는 `monsterWaypointYOffset=6f`만큼 부양(WL-063)돼 있고 `hitPosition` 로컬 Y는 적 프리팹마다 다르다. (Chain 홉의 좌표계 혼합은 커밋 `0944c0c`에서 `FindNearestUnhit`의 정렬 기준을 `HitPosition`으로 통일해 **해소**. 잔여: 선정은 콜라이더 오버랩, 정렬은 `HitPosition`이라는 비대칭) WL-015/WL-027(밸런싱 수치 소유권)이 프리팹 계층까지 번진 새 표면 | "연출용 착탄점(HitPosition)과 범위 판정 원점 분리" 또는 "몸통 높이를 `EnemyAsset`에 authored 값으로 승격" 중 택1 — `SplashRadius`/`ChainRadius` 값 기입 전에 muchan(데이터 소유)·SUNGSOO 합의