# WatchList Archive — 해소·폐기 항목 (재발 감지용)

> `WatchList.md`에서 해소·폐기 판정된 항목의 보관소. **리뷰 시 통독하지 않는다** —
> 활성 항목이 과거에 해소된 이슈의 재발인지 의심될 때만 해당 번호·키워드를 Grep해서 조회한다.
> 번호는 영구 보존한다(재사용 금지). 재발이 확인되면 활성 파일에 같은 번호로 되살리고
> 상태에 "재발(N회)"을 표기한다.
>
> 각 항목은 "무엇이 어떻게 해소됐는가 + 재발감지 키워드" 형식으로 압축돼 있다.

- **WL-225** | RESOLVED(#543, `PlacementRequest.OnGhostPositionUpdated`) | 고스트 프리뷰와 실제 배치의 적응형 받침대 표현이 달랐던 문제 해소 — `MouseManager`가 고스트 위치 갱신 직후 생성 인스턴스를 요청 콜백으로 전달하고, `TowerPlacer`가 같은 풋프린트 최저·최고 높이로 `AdaptiveTowerFoundation.Fit`을 호출한다. `SniperTower-GHOST`·`TwinMissileTower-Ghost` 루트에도 컴포넌트와 받침대 참조를 배선했으며, 평지·높이차 타일 사이 이동과 배치 전후 일치를 플레이 모드에서 확인했다. 재발감지: 2x2 고스트 루트의 `AdaptiveTowerFoundation` 누락, `OnGhostPositionUpdated` 연결 제거, 고스트와 실제 배치가 서로 다른 높이 계산 경로 사용

- **WL-172** | RESOLVED(2026-08-12 설계 철회) | KIM-SUNGSOO·muchan918 | 몬스터 이동속도 ×0.6 및 HP ×1.5 조정안은 2026-08-12에 철회됐다. 현재 `EnemyAsset.MoveSpeed`와 HP를 전투 밸런스의 정본으로 유지하고 타워 수치를 이 값에 맞추는 방향으로 확정했으며, 철회된 조정안은 실제 몬스터 에셋에 적용된 적이 없다. 재발감지: 전체 전투 재검산 없이 이동속도 ×0.6을 다시 적용, 속도 6·HP 60을 현재 검산 기준으로 사용, 철회된 계획을 미적용 작업으로 활성 WatchList에 재등재

- **WL-003** | RESOLVED(StageBuilder.cs:74) | 몬스터 순서 경로가 MapBuilder 정본으로 확정 — `StageBuilder.MonsterRoute`(IReadOnlyList<Vector3>) 읽기 전용 공개, 소비 측은 `IMovementAgent.SetRoute`로 수신. 재발감지: StageBuilder 밖에서 경로 좌표를 재구성하는 코드, MonsterRoute를 우회한 자체 경로 리스트
- **WL-004** | RESOLVED(BattleTile.cs:4-9 + TowerPlacer.cs) | 배치 검증 공백 해소 — `TileKind`(Grass/Road/Lava)+Occupied 마커를 타일 프리팹에 부착하고 TowerPlacer가 스냅·풋프린트·점유·맵밖 셀을 검증. 재발감지: CanPlaceAt이 무조건 true로 회귀, TileKind 조회 없는 신규 배치 경로 (tileSize 동기화는 WL-034)
- **WL-007** | RESOLVED(StageBuilder.cs:221,246 → StageMonsterRouteTracker.cs:45) | grid→world 인라인 복제 제거, `ToWorldPosition`/`GetWorldPosition` 단일 창구로 통일(SpawnFinalCenterObject 포함). 재발감지: StageBuilder/스포너 내부에 tileSize 곱셈이 다시 등장 — 신맵 CombatMapTileSpawner.GridToWorldPosition·TowerPlacer tileSize는 각각 신맵 경로/WL-034로 별도 추적
- **WL-010** | RESOLVED(저장소 grep 히트 0, WatchList-Archive.md만 잔존) | 오타 폴더 `Scirpts`가 Assets/Scripts 통합으로 소멸하고 시스템 단위 폴더로 대체됨(코드·.meta 참조 0건). 재발감지: `Scirpts`, Assets/Personal/*/Scripts 하위 신규 스크립트
- **WL-013** | RESOLVED(TowerInfoUI.cs:31 + CSV 헤더) | 표시 문자열 소유권이 String Table로 완전 이관 — CSV는 NameKey/RoleKey/DescriptionKey만 보유하고 원문은 NorthLand_* 테이블(ko/en/ja). 기록된 잔여였던 전투 공간 배선도 `LocalizationHelper.Get(k_TowersTable, descriptionKey)`로 완료. 재발감지: CSV에 한글/영문 표시 원문 컬럼 추가, 코드 내 UI 문자열 리터럴
- **WL-016** | RESOLVED(ManagementController.cs:50-51) | per-instance 레벨/주민당량을 공유 SO가 아닌 컨트롤러 런타임 배열(`_level`/`_amountPerVillager`)이 소유하고 BuildingAsset은 읽기 전용 기준값(BaseAmountPerVillager·UpgradeLevels)만 제공 → 공유 SO 덮어쓰기 없음. 재발감지: BuildingAsset/TowerAsset 필드에 런타임 쓰기, SO에 인스턴스 상태 추가
- **WL-022** | RESOLVED(CanEndDay 심볼 제거) | '전원 배치라야 밤 전환' 하드 게이트 소멸 — 유휴 주민은 낮 종료 확인 팝업의 경고로만 표시되고(ManagementController.cs:114 HasIdleVillagers → ManagementEndDayConfirmPopup.cs:128) [계속]이 EndDay를 그대로 호출한다. 재발감지: 배치 완료 여부로 EndDay/밤 전환을 return false 하는 게이트. 경고 상시화 문제는 WL-109.
- **WL-028** | RESOLVED(GameSceneManager.cs:13-14 + EditorBuildSettings.asset:20-25) | 부트 씬이 사본을 지목하던 경로 소멸 — 씬 이름 상수가 정본 고정(TitleScene/GameScene)이고 빌드 리스트에서 그 두 씬만 enabled:1(개인 사본은 전부 enabled:0). '개인 사본 폐기' 잔여는 SceneWorkflow.md §3이 개인 복사본을 정식 워크플로로 승격해 요구 소멸. 재발감지: 빌드 리스트에서 Personal 경로 씬 enabled:1 전환, GameSceneManager 씬 상수 변경
- **WL-030** | RESOLVED(설계 교체 · WL-070 승계) | 효과 SO 계층(TerritoryEffect/GainResidentEffect)이 삭제되고(심볼 0건) 영지 보상이 매일 자동 수급으로 대체됨 — TerritoryController.cs:180 DailyYield 롤 → ManagementController.cs:884-890 `_wallet.Add`(계약 #3 준수) + :898 SupplyDaily 프리뷰. 이 항목의 대상 코드는 더 이상 존재하지 않음. 이후 추적은 WL-070. 재발감지: 영지 보상을 다시 SO Apply 경로로 되돌리는 변경
- **WL-031** | RESOLVED(SceneWorkflow.md §4 재작성 + GameSceneManager.cs:12-14) | 병합 규약이 '번호 스냅샷(Assets/Scenes/Branches/) 추가 → 정본 파일명에 덮어쓰기'로 바뀌어 병합마다 씬 상수 편집·재컴파일이 필요 없어졌다. 규약대로 GameScene_1~17/500/501 스냅샷 운용 중. 재발감지: SceneWorkflow가 다시 '활성 씬 상수 수동 갱신'을 요구하거나 GameSceneManager 상수가 번호 파일을 지목하는 변경
- **WL-032** | RESOLVED(SystemMap.md §1·§2·§3) | GameManager/GameSceneManager 공개 API 미등재 해소 — §1에 두 시스템 행, §2에 `Result`/`OnResultDecided`/`TriggerGameOver`/`TriggerVictory`/`LoadMainMenu`/`LoadManageSpace`/`QuitGame` 등재("최초 결과만 인정, Playing으로는 확정 불가" 계약 + `OnResultDecided` 소비처 5종 명시), §3에 `PhasePanelSwitcher ↔ GameManager` 접점 행 추가(#391). 재발감지: `OnResultDecided` 신규 구독처가 §2 소비처 목록에 반영되지 않은 채 늘어남, GameManager 공개 멤버 추가 후 §2 미갱신
- **WL-033** | RESOLVED(MouseManager.cs:74-83) | 씬 재로드 시 `_selected`/`_hovered`를 알림 없이 직접 null로 리셋 + `CancelPlacement()`로 고스트/모드까지 정리(:80 주석에 근거 명시). 통지 경로에도 IsAlive 가드 추가(:240,:249). 재발감지: HandleSceneLoaded에서 상태 리셋 누락, 또는 리셋을 OnDeselected() 경유로 되돌리는 변경
- **WL-035** | RESOLVED(CanAssignVillagers 심볼 제거) | 주민 배치 불리언 게이트 소멸 — CanEditLine은 밤 게이팅만 검사(ManagementController.cs:921-930, #219 주석 명시)하고 UI도 `editable = _controller.IsDay`(ProductionLineView.cs:139). HasExpandedToday는 1일 1회 영지 확장 제한 용도로만 남아 배치·EndDay와 무결합. 재발감지: 배치/낮종료 조건에 HasExpandedToday·확장 여부를 다시 끼우는 변경
- **WL-036** | RESOLVED(MonsterMove.cs:60-70 + Enemy.cs:60-65) | 이동속도 단일 소스 확립 — Enemy가 `Stat.MoveSpeed`를 `IMovementAgent.SetMoveSpeed`로 주입하고, 3f는 무효값 전용 `fallbackMoveSpeed`(:22)로 격하(폴백 시 경고, minMoveSpeed 0.15f 하한으로 정지 없음). Awake 순서 비결정성은 MoveSpeedComposer 지연 초기화(`speedComposer ??=`)로 흡수. 재발감지: MonsterMove가 속도를 직접 상수로 읽거나 SetMoveSpeed 우회
- **WL-037** | RESOLVED(TowerPlacer.cs:127-181) | `TowerSelectPanelView.cs:126`이 `TowerAsset`을 `BeginTowerPlacement`로 직접 주입하고, TowerPlacer가 SO에서 TowerPrefab/GhostPrefab/GridWidth·Height/사거리를 해석한다. 재발감지: TowerPlacer에 SO 밖 배치 필드 재도입, 미사용 `dummy*` 필드(:43-45)·"게이트웨이(예정)" 주석(:183-188)의 부활
- **WL-042** | RESOLVED(ManagementController.cs:41·473-497) | 지갑 인스턴스는 `private ResourceWallet _wallet`으로 외부 노출이 없고, 획득이 열린 유일한 진입점은 차감+지급이 한 트랜잭션인 `TryExchange`뿐. 본진·건물 업그레이드는 `TrySpend`(:351/:435/:808) 순수 소비. 재발감지: `ResourceWallet` 인스턴스를 public으로 노출, `_wallet.Add` 직접 호출부 신설
- **WL-044** | RESOLVED(Tower.cs:354-363) | `AuraTower` 폐기 → 단일 `Tower` + `ITowerBehaviour` 조립. 페이즈 게이팅은 호스트 한 곳에서만 하고 행동은 `TowerActivePhase`로 요구만 선언(Attack·DebuffAura=NightOnly, BuffAura=Always 의도적 예외). 재발감지: 행동 구현체가 `DayNightManager`를 직접 폴링, `AuraTower` 타입 재등장
- **WL-045** | RESOLVED(2026-07-30 재확인) | 전투 로직 정본은 `Assets/Scripts/CombatSystem`(`NorthLand.Combat`). `Personal/SUNGSOO/Scripts/`에 남은 것은 `AuraTowerTestDriver.cs`·`DebugDamageTarget.cs` 2개(해소 조건이 허용한 테스트 스캐폴딩)뿐. 재발감지: Personal/*/Scripts에 런타임 전투 컴포넌트 추가, `NorthLand.Sungsoo`가 정본 코드에 등장
- **WL-055** | RESOLVED(#447, `EnemyStateUILayer.cs` + Imported `01bae4aa9`) | HealthUI 2종이 `NorthLand.UI`로 이관됐고(`BaseHealthBar`·`EnemyStateUI`), 몬스터 HP 바의 **프리팹 수동 부착 방식 자체가 사라졌다** — `Enemy.Spawned`(static) 신호를 받아 씬 배선 없는 `EnemyStateUILayer`가 `Resources/UI/EnemyStateUI`를 스폰해 붙인다. 몬스터 프리팹 9종의 중첩 바(+ `Tank`의 통 복제본)를 제거해 손튜닝 스케일 오버라이드가 0건이 됐다(제거 전 월드 폭 실측 3.00~10.00 = 편차 3.3배, HP와 무상관). 신규 몬스터 프리팹은 아무것도 하지 않아도 바가 붙으므로 "누락 시 무증상" 통로가 닫혔다. 재발감지: 몬스터 프리팹에 `EnemyStateUI` 컴포넌트나 중첩 캔버스 재등장, 바를 몬스터 자식으로 parent하는 코드, `Assets/Scripts/UI/HealthUI`에 namespace 없는 신규 스크립트
- **WL-057** | RESOLVED(poison_tower.asset Cost 기입) | Debuff 타워 무료 무한 배치 구멍 해소 — Cost 부여로 cannon과 동일 자원 게이트 적용. 재발감지: 신규 타워 SO의 Cost 빈 리스트(배치 비용 0)
- **WL-058** | RESOLVED(slow_tower 완전 제거로 확인, 2026-07-30) | `slow_tower` 고아 SO(Get 시 null) 문제 해소 — 기록된 "CSV 행 복원"이 아니라 **CSV 행·`.asset`·코드 참조가 모두 없는 상태**로 정리됐다(Towers 폴더 9종에 slow_tower 없음). 재발감지: CSV에서 타워 ID를 교체할 때 대응 `.asset` 잔존
- **WL-060** | RESOLVED(LocalizationHelper.cs:18-19 실측) | Localization 테이블명 대소문자 트랩 해소 — `k_TowersTable="NorthLand_Towers"`/`k_EnemiesTable="NorthLand_Enemies"`로 실제 컬렉션명과 일치. 재발감지: 신규 테이블 상수 추가 시 컬렉션명 대소문자 불일치(이름 기반 TableReference는 대소문자 구분)
- **WL-062** | RESOLVED(TerritoryNodeStateVisual.cs:198 주석만 잔존) | 정본 스크립트의 벤더 타입(`ithappy.BlendShapeAnimator`) 컴파일 타임 직접 참조 제거 — 덕타이핑(`GetType().Name` 필터)으로 교체해 Imported 미동기화 환경에서도 Assembly-CSharp 컴파일 보장. 재발감지: `Assets/Scripts/`에서 벤더 네임스페이스 직접 참조(Imported 소스 의존은 WL-040)
- **WL-066** | RESOLVED(#164, Ghost 전수 검증 + 구조적 방어선) | 고스트 프리팹의 live `Tower`/`AuraTower` 자가 등록 해소 — 5종 전부 게임플레이 컴포넌트 제거 후 전수 0개 검증, `Tower.Active` 등록 시점을 `OnEnable`→`Build`로 이동해 미조립 인스턴스는 집계되지 않고 오라는 런타임 AddComponent 전용이 됐다. 규약은 SystemMap §6 "Ghost=시각 전용"으로 승격. **교훈: "프리팹 전수"가 조건인 이슈를 정본 1종 확인으로 RESOLVED 처리하지 말 것**. 재발감지: 신규 Ghost 프리팹에 Tower/AuraTower/TowerReloadVisual
- **WL-068** | RESOLVED(#398, SkillHitScan.cs:20·32-59) | 스킬 AoE 시전 Y와 몬스터 부양 높이 불일치 해소 — 감전·폭탄·전기장이 `SkillHitScan.CollectEnemies` 한 곳을 통과하고, 수직 폭은 `VerticalRange = 12` 상수 하나가 소유한다(수평 반경 = 인디케이터 원은 불변이라 유효 면적은 안 넓어진다). 시전면 불일치 자체는 **`SkillButtonView._castHeight` 스크립트 기본값을 씬과 같은 2로 맞춰**(기존 20) 없앴고, 캡슐을 위아래 양쪽으로 여는 것은 그 위의 여유분이다 — 양방향만으로는 시전면이 20일 때 아래 도달이 y=8에 그쳐 몬스터 발(씬 `monsterWaypointYOffset` 3.2)에 닿지 않는다. 기록돼 있던 "지면까지 닿는 트리거 콜라이더 계약" 방향은 **폐기** — 적 프리팹에 Rigidbody가 없어 `OnTrigger*`가 아예 발동하지 않고 저장소 전 범위 판정이 `Physics.Overlap*NonAlloc`이다(`PlayerSkill.md` §5, `CombatBalance.md` §4.6). 재발감지: 스킬 범위 판정이 `SkillHitScan`을 우회해 `OverlapSphere`로 돌아감, 스킬별 수직 상수 재도입, 공중 유닛(`Flying_Bat`) 미적중 재보고
- **WL-070** | RESOLVED(#166/PR#170, 효과 SO 계층 삭제 실측) | 영토 의미 재편 완료 — "고유 효과 지급 노드"에서 "확보 시 고유 자원을 매일 자동 수급"으로 전환, 구 `TerritoryEffect`/`GainResidentEffect` 등 SO 계층 파일이 저장소에 없음을 확인. `ManagementController.HandleNightToDay`가 Owned 노드 순회로 지급. 재발감지: 확보 즉시 지급(OnNodeClaimed→Apply) 경로 부활
- **WL-072** | RESOLVED(d5b7e77 정본 씬 반영) | TowerInfoUI 정본 GameScene 미반영으로 인한 타워 선택 시 NRE 위험 해소 — TowerInfoPanel 반영 + 자식 참조 배선 확인, Instance non-null 보장. 재발감지: 정본 씬에서 UI 패널 오브젝트 누락(씬 병합 시 선별 병합 사고)
- **WL-079** | RESOLVED(#164 타워 통합 리팩토링) | `TowerType→AttackFields` 해석이 `TowerBehaviourFactory.ResolveAttackFields`(:15) 단일 출처로 통합돼 소비처 3곳이 전부 경유한다(행동 조립 `AttackBehaviour:46` / 배치 전 툴팁 `TowerTooltipView:284` / 프리뷰 반경 `TowerPlacer:161`). 라벨·서식은 `TowerStatsFormatter`가 소유. 재발감지: `switch (asset.TowerType)`로 Attack 필드를 직접 고르는 신규 코드, `game.tower.attack_*` 키 문자열 재조립
- **WL-081** | RESOLVED(#164 타워 통합 리팩토링) | 스탯 modifier 두 경로가 원장 `TowerStats` 하나로 합쳐졌다 — 타일 버프는 `TileBuffSourceId` 고정 소스키의 지속형 항목(`Tower.ApplyTileBuff` :215-229, 변환 지점 1곳)이고 사거리·공격력·공속 3축을 대등 취급(#187 ③ 충족). 오라 수치도 `Evaluate` 경유(`DebuffAuraBehaviour:47`), 표시는 `BuildDotLine(float,float)`로 SO 직독 차단. ⚠ `TowerPlacer`는 타일 버프를 `Tower.Build` **앞에** 적용해야 한다. 재발감지: 스탯 게터가 SO 필드를 직독하는 신규 코드
- **WL-087** | RESOLVED(PR#210 후속) | 선택 훅 비대칭 해소 — 코디네이터가 `_infoShownFor` 슬롯을 들고 대상 변경·0/2+ 전이 시 대칭 `OnDeselected()`를 호출하며(`TowerMergeCoordinator.cs:37,279-280`, 2→1 복귀는 :294에서 `OnSelected()` 재발화), Shift 토글 발행 직전 `Select(null)`(`MouseManager.cs:186`)이 스위처가 못 잡는 표면을 덮는다. 재발감지: `OnSelected()`를 직접 부르고 대칭 해제가 없는 신규 경로
- **WL-088** | RESOLVED(PR#214) | 전투맵 좌표 계약 창구 확정 — 본진은 `CombatMapData.BasePosition`(:22) 공개 프로퍼티(소비처 5파일 전환 완료), 스폰은 `CombatMapRevealController.CurrentSpawnPosition`(:54,173) + `TryGetSpawnPosition`(:291) 하나로 통일(모호한 `CombatMapData.SpawnPosition`은 신설 후 삭제). 잔여 SystemMap §2 등재는 WL-091이 승계. 재발감지: `RouteStartPosition`/`EnemyRoute[0]` 직접 참조
- **WL-090** | RESOLVED(PR#214) | 경로 생성 규칙의 좌표 리터럴 결합 제거 — `IsBlockedBaseEdgePosition`(`RouteGenerator.cs:328-352`)이 `map.BasePosition`에서 가장자리 축을 파생하고 `position == goal` 예외를 포함하며, 본진이 내부 좌표면 규칙이 비활성화된다(:351). 모서리 본진에서는 수직 축이 우선. 재발감지: 경로 규칙에 `== 0`/`== Width - 1` 같은 좌표 리터럴 재등장
- **WL-093** | RESOLVED(`Enemy.cs:60`) | `IMovementAgent` 탐색이 `GetComponentInChildren`으로 통일돼 `MonsterMove`를 자식에 둔 프리팹에서도 잡히고, 미발견 시 `Enemy.cs:70` `LogWarning`으로 조용한 실패가 드러난다(MonsterSpawn·MonsterStateMachine과 탐색 범위 일치). 재발감지: `GetComponent<IMovementAgent>`, `fallbackMoveSpeed`
- **WL-097** | RESOLVED(`Enemy.cs:41`) | 보스 `BehaviorGraphAgent`가 지역 변수 → 필드로 승격되고, 결과 확정(`:195`)과 사망(`:284`) 두 지점에서 `enabled = false`로 그래프 틱이 멈춘다(팀 계약 #5). 부수로 사망 후 `SetSpeedMultiplier` 경로가 끊겨 메서드별 `isDying` 가드가 불필요해짐. 재발감지: `Awake` 안의 지역 `BehaviorGraphAgent`, `isDying` 가드 재등장
- **WL-101** | RESOLVED(`SceneWorkflow.md:41-68` §4-4) | 기능 무관 씬 collateral 재저장. 커밋 전 `git diff -- Assets/Scenes/GameScene.unity`로 RectTransform 구동값(`m_AnchorMin`/`m_AnchoredPosition`/`m_SizeDelta`)·TMP 글꼴·Canvas 재저장을 검사하는 절차가 문서에 박혔고 PR#223이 기존 오염 hunk를 정화했다. TMP `m_fontSize`는 오토사이징 캐시라 회귀 후보 제외. 재발감지: 기능과 무관한 레이아웃 구동값 대량 diff
- **WL-102** | RESOLVED(`BuildingAsset.cs:304-321`) | 마법 연구소 레벨 데이터 이원화(SO 비용 vs 씬 인스펙터 배율). 배율 7종이 `SkillUpgradeLevel`에 `Cost`와 같은 리스트로 들어가 단일 authoring이 되고, `SkillManager.cs:118`·`BuffSkillManager.cs:85`가 `_magicLabAsset.Skill.UpgradeLevels`를 직접 읽어 씬 파일이 배율 데이터를 갖지 않는다 — 레벨 개수 불일치가 구조적으로 불가능. 재발감지: 씬 인스펙터 배율 리스트 재등장
- **WL-108** | RESOLVED(파일 삭제 확인) | 씬 번호 스냅샷 이중 저장. `Assets/Personal/SUNJIN/Scenes/`에 `GameScene15.unity`가 없고(현재 `GameScene13/14`·`MonsterAnimation`만) 15번 스냅샷은 `Assets/Scenes/Branches/GameScene_15.unity` 하나뿐이다. 남은 `GameScene13/14`는 이 항목 이전 잔재로 `SceneWorkflow.md` §5 주간 정리 대상. 재발감지: 개인 폴더에 `GameSceneNN.unity` 재등장
- **WL-110** | RESOLVED(CastlePanelUI.cs:150 / StorePanelUI.cs:118 실측) | 건물 패널 닫기 버튼이 MouseManager 선택 상태를 남겨 재클릭 시 안 열리던 문제 해소 — 두 패널 모두 `ClearSelection()` 경유(OnDeselected→HideAll)로 교체. 잔여 구멍: 대상 파괴로 IsAlive 가드에 걸리면 HideAll이 안 불려 패널이 열린 채 남음(건물 철거 도입 시 대응). 재발감지: 선택 바인딩 패널에서 onClick→Hide 직결
- **WL-118** | RESOLVED(PR#238, Enemy.cs SetHasTarget(false)) | BT 이동 소유권 획득 시 교전 중이면 `canMove`가 false로 고착돼 돌진 노드가 보스를 못 움직이던 문제 해소 — 소유권 브랜치에서 `SetHasTarget(false)` 호출. 세 번째 이동 owner를 만들지 않고 Enemy에 가두는 방향 유지. 재발감지: `IsStopped`만 만지고 `SetMoveEnabled`/`canMove` 게이트를 놓치는 신규 노드
- **WL-124** | RESOLVED(PR#242 + TankGraphSpec.md:86-89 불변식 명문화) | 보스 돌진 감속 파훼 불변식이 수치 튜닝(P1_MaxFactor 3→7)으로 깨졌던 건 — `P1_MinSpeed` 상향으로 복원하고 `MoveSpeed × MaxFactor × slowFactor^n < MinSpeed`를 문서에 명문화. 파훼 가능성은 밸런스가 아니라 설계 불변식. 재발감지: P1_MaxFactor/MinSpeed 튜닝 시 이 부등식 미확인
- **WL-128** | RESOLVED(코드 5곳→3곳 정합) | mover 탐색 지점 전부가 `GetComponentInChildren<IMovementAgent>`(`StatusEffectHandler.cs:77`·`Enemy.cs:60`·`EnemyAgent.cs:63`)이고 루트 전용 `GetComponent<IMovementAgent>`는 0건 — MonsterMove가 자식 GO에 있어도 CC(슬로우/스턴)가 정상 적용된다. 재발감지 키워드: `GetComponent<IMovementAgent>`(children 없는 형태), "이중 mover".
- **WL-129** | RESOLVED(산 SO 주입) | `TowerPlacer._activeAsset`(:61/:153)을 배치 확정 시 `Tower.Build`에 넘기고(:339), `Build`는 프리팹이 문 SO와 다르면 LogWarning 후 산 SO로 재조립한다(`Tower.cs:167-177`). 프리팹 `data`는 씬/테스트 폴백뿐(`Tower.cs:91`), `Tower` 없는 프리팹은 LogError(:343). 재발감지: `Instantiate(so.TowerPrefab)` 뒤 `Build` 미호출.
- **WL-131** | RESOLVED(타입 통합) | `AuraTower`가 폐기되고(저장소에 `class AuraTower` 0건) 마법 타워도 단일 `Tower`가 되어 `sel is Tower`(`TowerMergeCoordinator.cs:196`)·`TowerGroupSelectable.Tower`(:206, `GetComponent<Tower>`)가 무수정으로 참이 된다. 배치 타워 전부가 `TowerGroupSelectable`을 받으므로(`TowerPlacer.cs:313`) 그룹 초록 미표시·평클릭 그룹 해제도 소멸. 능력 질의는 `Tower.Has<T>()`. 잔여는 코드 아닌 기획(레시피 재료 채택 여부).
- **WL-139** | RESOLVED(원장 0 하한) | `TowerStats.Evaluate`가 `return result > 0f ? result : 0f;`(`TowerStats.cs:85`)로 음수를 막는다 — 배율 모드 합산 중첩(보스 P3 봉인 다중)으로 데미지가 음수가 되어 적을 회복시키던 표면이 닫혔다. 원장이 단일 출처라 3축·전 소비처(`AttackBehaviour`/`BuffAura`/`DebuffAura`의 `Evaluate` 호출)가 함께 덮인다. 재발감지: `Evaluate` 우회하는 새 스탯 합성 경로, 하류 클램프 없는 `currentHp -=`.
- **WL-140** | RESOLVED(DisplayRange 계약) | `ITowerBehaviour.DisplayRange`(`ITowerBehaviour.cs:60`)를 공격=교전 사거리(`AttackBehaviour.cs:41`)/오라=반경(`Buff·DebuffAuraBehaviour.cs:31`)으로 구현하고, 호스트가 행동들의 최대값으로 원을 그린다(`Tower.cs:287-296` → `:280 SetRadius`). 오라 전용 타워(poison/haste/choco)에서 `AttackRange`가 0이어도 선택 시 사거리 원이 정상 표시된다. 재발감지: `ShowRangeCircle`이 `AttackRange`를 직접 참조.
- **WL-099** | RESOLVED(PR#254, GameSpeedController.cs:98/231/238) | 결과 확정 후 시간 정지 부재 해소 — `GameSpeedController.Start`가 `GameManager.OnResultDecided`를 구독해 `HandleResultDecided`에서 `SetPaused(GamePauseReason.ResultDecided, true)`를 적용하고 `:245`에서 해제한다. GameManager는 UI 참조 0개를 유지해 씬 배선이 불필요하고 결과 UI 없는 씬에서도 성립. 재발감지: 결과 확정 경로에서 timeScale 정지가 특정 UI 존재에 의존
- **WL-104** | RESOLVED(`ManagementController.cs:829-831`,`:937`) | 페이즈 전환이 경영 UI 갱신 신호를 발행하지 않던 문제. `SubscribeDayNight`가 `OnNightToDay`·`OnDayToNight`·`OnDayStart` 3종을 구독하고 `HandleDayToNight`/`HandleDayStart`가 `OnChanged`를 재발행하며 `OnDestroy`(`:670-672`)에서 대칭 해제된다 — `IsDay` 게이트 버튼이 전환 즉시 비활성화되고, #350에서 추가된 `OnDayStart` 구독으로 밤을 거치지 않는 `SkipDay` 경로에서도 웨이브 표시(`ManagementPanelView.cs:135`가 `CurrentWave`를 pull)가 즉시 갱신된다. 재발감지: `SubscribeDayNight`에 `OnNightToDay`만 남는 회귀, `HandleDayToNight`/`HandleDayStart` 삭제
- **WL-107** | RESOLVED(UILayer.cs + UIZOrder.md §3) | UI z-order 매직넘버 해소 — `Assets/Scripts/UI/UILayer.cs`가 `sortingOrder` 단일 소스(SelectionBox 50 / Hud 100 / Modal 500 / Result 900)이고, 새 값을 추가할 때 "UIZOrder.md §3 표를 먼저 갱신 후 상수 추가" 규약을 파일 헤더에 명시한다(#261의 SelectionBox=50이 이 규약을 따른 첫 사례). `TowerTooltipView`는 HUD 캔버스를 `sortingOrder == UILayer.Hud`로 찾아 자식으로 붙고 없을 때만 같은 값으로 생성 — `short.MaxValue` 사용은 저장소 0건. 마지막 잔여였던 UIZOrder.md §3 표·본문의 `32767` 오기도 `100`으로 정정됐다. 재발감지: `sortingOrder`에 리터럴 대입, `short.MaxValue`, UILayer 상수 없이 새 루트 Canvas 추가
- **WL-143** | RESOLVED(#261 리뷰, MouseManager.cs SetMode) | BoxSelect 이탈이 종료 통지를 우회하던 문제 — `CancelPlacement`/`CancelSkillTargeting`이 `_mode = Idle`을 먼저 대입해 뒤따르는 `ResetGesture`의 `_mode == BoxSelect` 검사가 항상 거짓이었고, 그 결과 `OnBoxSelectEnd` 미발행 → 구독자 유예 플래그가 켜진 채 남아 우측 패널이 영구 정지할 수 있었다(`PhasePanelSwitcher`는 Cancel을 직접 불러 `ResetGesture`를 아예 건너뛰었다). 모드 전환을 `SetMode` 단일 창구로 수렴시켜 호출 순서와 무관하게 통지 1회를 보장하고, `UpdateBoxSelect`의 이탈 판정을 `wasReleasedThisFrame`에서 `isPressed` 상태로 바꿔 뗀 프레임을 놓쳐도 다음 프레임에 빠져나오게 했다(고착 시 모든 클릭·호버가 죽던 경로). 플레이 모드 실측: 두 Cancel 경유 이탈 모두 endFired=1, 이미 Idle이면 0. 재발감지: `_mode =` 직접 대입, BoxSelect 종료를 `wasReleasedThisFrame`으로만 판정, 구독자 유예 플래그를 푸는 경로가 단일 통지에만 의존
- **WL-144** | RESOLVED(#261 리뷰, MouseManager.cs UpdateIdle) | press→release 이동(#261)이 만든 클릭 유실·중복 확정 — ①누른 프레임에서 곧바로 `return`해, 누름과 뗌이 한 프레임에 함께 보고되면(저프레임·히칭) 다음 프레임의 `!isPressed` 방어가 제스처를 버려 클릭이 통째로 사라졌다 → 누른 프레임에서 뗌까지 그 자리에서 소화. ②Esc 분기가 `_pressActive`를 안 건드려 누른 채 Esc 후 뗄 때 `CommitClick`이 한 번 더 돌아 `OnPrimarySelect(null)`이 중복 발행됐다(WL-106의 3번째 구독자 분리 판단과 같은 채널) → Esc가 `ResetGesture`로 제스처를 폐기. `ISelectable` 전 소비처(타워·건물·영지 노드·상점)가 지나는 경로이며 자동 회귀 테스트는 여전히 0건. 재발감지: press 분기의 무조건 `return`, Esc/모드 전환 경로에서 제스처 상태를 남기는 코드
- **WL-130** | RESOLVED(#274 Phase 3, `TowerAsset.OnValidate`) | `TowerAsset` authoring-time 검증 단일 출처가 생겼다 — 구 `TowerAssetEditor.cs`의 HelpBox는 `MagicEffectType=None` 1종뿐이었고 그마저 `TowerType` 기반이라 enum 삭제와 함께 파일째 사라졌다. 지금은 `OnValidate`가 **프리팹의 `Actions`와 SO 수치의 불일치**를 저장 시점에 잡는다: 액션↔수치 한쪽만 있음(`AttackAction`은 있는데 공격 수치가 빔, 역방향도) · 같은 타입 액션 중복(`TowerAction.SourceId` 충돌로 스탯·상태이상 슬롯을 서로 덮어씀) · `[SerializeReference]` null 항목(클래스 rename 시 발생) · `Impact=Area`인데 `SplashRadius<=0` · `Impact=Chain`인데 `MaxChainTargets<=1` · 오라 액션인데 `Radius=0`. 전부 **예외도 경고도 없이 조용히 아무 일도 안 일어나는** 조합이라 예전엔 플레이해보기 전엔 알 수 없었다(WL-001의 `lightning_tower` 전 필드 0이 정확히 그 패턴). 소비되지 않는 modifier 문제는 `AuraModifiers` 환산 순수 함수 공유로 이미 해소돼 있었다. ⚠ 한계: `TowerPrefab`이 null이면 검증을 건너뛰므로 `lightning_tower`는 여전히 침묵한다(저작 도중 경고 폭주를 막기 위한 의도적 선택). 재발감지: `TowerAsset`에 `OnValidate` 없이 새 필드 그룹·액션 종류를 추가, 액션↔수치 불일치를 런타임 `LogWarning`으로만 잡는 신규 코드, `TowerType` 같은 종류 enum의 재등장
- **WL-116** | RESOLVED(PR#257) | PR#237 | 정본 씬 반영 시 인스펙터 참조 유실: `GameScene.unity:11111-11114`가 `SettingUI.gameSpeedController`를 `{fileID: 0}`로 덮어써 정본 씬에서만 설정창 정지가 동작하지 않음 | **해소**: 제안된 "의도 플래그 + LogError" 대신 **실패 모드 자체를 제거**했다 — `SettingUI`의 `[SerializeField] GameSpeedController` 필드를 삭제하고 `GameSpeedController.Instance` 경유로 전환(`SettingUI.cs:108-111,128-131`), 추가로 `OnDestroy`(`:45-55`)에서 `SetPaused(Settings,false)`로 열린 채 파괴되는 경로까지 닫았다(`HashSet` 기반이라 미설정 사유 해제는 무해). 씬 배선이 사라졌으므로 병합 유실 경로가 원천 소멸 — WL-099가 `GameManager`에서 쓴 것과 같은 처방. 고아 오버라이드도 정리되어 씬 배선 및 dead 직렬화 잔여 없음
- **WL-146** | RESOLVED(bee58c0 이전 23a8755) | muchan918 |Combat 상태이상 4종을 *Status로 개명 + [MovedFrom] 부착, 스킬 쪽 *Effect와 이름이 더 이상 겹치지 않음, 기존 SO 6개 재직렬화 확인. 재발감지: NorthLand.Combat 네임스페이스에 *Effect 접미 신규 클래스 추가
- **WL-051** | RESOLVED(PR#301 독립 확인) | muchan918 | 버프 스킬의 GDD 편입이 실재함을 확인 — `GDD.md` §5.5(WL-051 결정 명기)·`PlayerSkill.md` §1 표·SystemMap PlayerSkill 행 3곳 모두에 등재돼 있고, 마법 연구소 기본 스탯 축과 §5.6 3택1 특수효과 축이 독립 스택으로 분리돼 정체성 중복도 해소됐다. #206도 새 스킬 유형을 추가하지 않고 기존 연구소 축의 두 번째 효과(착탄 이펙트 교체)로 붙여 이 경계를 유지했다. 재발감지: GDD에 없는 신규 스킬 유형 추가, `SkillManager` 외 두 번째 시전 매니저 등장. ⚠ **추신(#315)**: 이 항목이 확인했던 "버프 스킬 GDD 편입"은 그 후 뒤집혔다 — 버프 스킬이 제거되고 스킬은 감전 1종이 됐다(GDD §5.5). 해소 판정 자체는 유효하나(당시 문서 3곳 정합은 실재했다) 서술 대상은 더 이상 없다
- **WL-162** | RESOLVED(PR#308, 3d29a92) | KIM-SUNGSOO | Enemy.Killed·TowerAction.OnWaveEnd·RampAction·RampProfile을 SystemMap.md §1 Combat 행과 §2 공개 API에 등재. §3 접점 매트릭스의 낡은 서술(DamageInfo.Source가 null이어도 안전)도 함께 정정 — #300 이후 그 null은 "처치가 어느 타워에도 귀속되지 않음"을 뜻한다. 재발감지: RampAction, RampProfile, Enemy.Killed, TowerAction.OnWaveEnd가 SystemMap에서 사라지거나, 새 공개 API가 등재 없이 추가
- **WL-180** | RESOLVED(PR#362, TitleCue.cs:22-32) | N0WST4NDUP | 씬 전환 시 BGM을 멈추는 주체가 없어 직전 게임 BGM(밤 트랙 포함)이 타이틀 화면에서 계속 루프했다 — `AudioManager`가 `DontDestroyOnLoad`인데 `StopBgm` 호출부가 0개였고 `TitleScene`에 큐가 없었다. 타이틀 복귀 경로가 넷(`ResultUIManager.cs:71`, `RunSaveManager.Persistence.cs:49`·`:112`, `SettingUI.cs:158`)이라 호출부를 하나 고치는 방식으로는 닫히지 않았다. **해소**: 정지 주체를 씬에 뒀다 — `SoundCue` 계층을 만들고 `TitleScene`에 `SoundCue/TitleCue`를 배치, `titleClip`이 비면 `StopBgm()`을 부른다(빈 클립을 `PlayBgm`에 넘기면 매니저가 무시해 직전 트랙이 살아남으므로 명시적 정지가 필요하다). 경로 수와 무관해졌고 타이틀 곡이 생기면 필드만 꽂으면 된다. Play 검증: GameScene에서 낮 BGM 재생 → `LoadMainMenu()` → TitleScene에서 두 소스 모두 `isPlaying=false`. 재발감지: 큐(`SoundCue` 파생) 없는 새 씬 추가, `TitleCue` 삭제, 빈 클립을 `PlayBgm`에 넘겨 "정지"를 표현하려는 코드
- **WL-195** | RESOLVED(TitleScene 육안 확인, PR#432) | sunjin1222 | 타이틀 설정 모달의 "다시하기" 슬롯 가드 우회 우려가 해소 — 타이틀 씬 설정 모달에 해당 버튼이 노출되지 않음을 확인해 도달 경로가 없다. `SettingUI.RetryGame()`(SettingUI.cs:170-180)은 여전히 `MainMenuUI.EnsurePlayerSlotSelected()`를 거치지 않지만 게임 씬에서만 도달 가능하고 그 시점엔 슬롯이 이미 선택돼 있다. 원인으로 지목했던 커밋 7a2c104의 TitleScene 오버라이드 삭제는 다시하기 버튼과 무관한 것으로 판명. 재발감지: TitleScene의 SettingCanvas Variant에 다시하기 버튼을 켜는 m_IsActive 오버라이드, RetryGame() 신규 호출부, 타이틀 경로에서 LoadManageSpace() 직접 호출
- **WL-184** | RESOLVED(#445, `AssetDatabase.FindAssets("t:TowerAsset")` 전수 실측) | muchan918 | `TowerAsset.Icon` 미기입으로 타워 선택 패널 7칸 중 6칸이 빈 테두리였던 상태가 사라졌다 — 정본 폴더 `Assets/Resources/ScriptableObjects/Towers/` **19종 전부** 기입, 정본 씬 `TowerSelectPanelView._towers` 7칸도 **7/7**(archer·cannon·poison·haste·choco·soda·flame_field). 미기입 2건(`Assets/Personal/SUNGSOO/debuff_tower.asset`·`lightning_tower.asset`)은 개인 WIP 폴더 소속이라 팔레트·레시피 어디에도 오르지 않는다. 절차 축도 함께 닫혔다 — `TowerAddGuide.md` §3.5 SO 체크리스트에 `Icon` 항목이 들어가 신규 타워마다 반복되던 누락 경로가 없어졌다(개정 이력 3차). **소비처가 넷으로 늘어난 것이 #445다**: 팔레트 버튼(`TowerButtonView.Set`) · 합성 후보 버튼(`TowerMergePanelView`가 같은 `Set`을 경유 — 도입 시 라벨만 채우던 경로를 교체) · 정보 패널 "합성 가능" 칸(`TowerMergeTargetSlot.Set`) · 호버 툴팁 헤더(`TowerTooltipView.Show`, 구 플레이스홀더 슬롯 바인딩 + 회색 틴트→흰색). 재발감지: 새 `TowerAsset`이 `Icon` 빈 채로 팔레트·레시피에 등재됨, `TowerAddGuide.md` §3.5의 `Icon` 체크박스 소멸, 아이콘 미할당을 흰 사각형/색판으로 그리는(=`enabled = icon != null` 규약을 깨는) 신규 슬롯, 아이콘 슬롯 `Image.color`에 흰색 아닌 틴트 authoring
- **WL-205** | RESOLVED(#406, AudioManager.cs `StopSfxExclusive` + SoundCue.cs `Awake`) | N0WST4NDUP | **WL-180과 같은 축이 SFX 채널에서 재현될 뻔했다.** `PlaySfxExclusive`(#406 신설)는 `PlayOneShot`이 아니라 `clip` + `Play()`라 소스가 살아 있는 한 계속 울리는데, `AudioManager`가 `DontDestroyOnLoad`이고 정지 API가 `StopBgm` 하나뿐이라 9.5초짜리 주민 증가 팡파레가 타이틀 복귀 네 경로(`ResultUIManager.cs:71`, `RunSaveManager.Persistence.cs:49`·`:112`, `SettingUI.cs:158`)를 넘어 타이틀 화면에서 계속 울릴 수 있었다. **해소**: WL-180과 같은 방식으로 정지 주체를 씬에 뒀다 — `SoundCue.Awake`가 `StopSfxExclusive()`를 부르므로 "씬마다 큐가 하나씩 있다"는 기존 계약에 얹혀 경로 수와 무관해진다. 재발감지: `PlaySfxExclusive` 소비처가 늘었는데 정지 대응이 없음, `SoundCue.Awake`를 파생 큐가 가려 정지가 안 불리는 변경, 매니저에 "씬을 넘어 이어지는" 새 재생 경로(루프·긴 원샷) 추가
- **WL-206** | RESOLVED(#406, SystemMap.md §4.1 넷째 예외) | N0WST4NDUP | 포인터 입력을 읽는 창구가 둘이 됐는데(팀 계약 #1) 예외로 등재되지 않아, 다음 사람이 §4.1을 근거로 되돌리거나 반대로 이 선례로 예외 없는 세 번째 폴링을 추가할 수 있었다 — `UiClickSfx.Update`가 `Mouse.current.leftButton.wasPressedThisFrame`을 직접 읽는다. **`MouseManager`로 라우팅하는 것이 정답이 아닌 사례다**: 그쪽은 `GameScene` 배치라 `TitleScene`에 아예 없는데 타이틀 버튼에도 클릭음이 필요하다. **해소**: §4.1에 넷째 예외로 등재하되 **"게임 상태를 바꾸지 않는다"를 조건으로 명시**했다 — 조건 없이 열면 예외가 번진다. 재발감지: 상태를 바꾸는(선택·배치·모드 전환) 폴링이 이 예외를 근거로 추가됨, §4.1 예외 목록을 지우고 폴링만 남기는 변경
- **WL-208** | RESOLVED(#406, InGameCue.cs `HandleBuildingAction`) | N0WST4NDUP·muchan918 | 같은 사건의 연출 트리거가 이벤트와 손배선으로 갈려 있었다 — `ManagementController.OnBuildingAction`을 **VFX는 구독**하는데(`BuildingFeedback.cs:56`) #406의 **소리는 UI 호출부 2곳**에 손으로 배선했다. 진입점이 늘면 파티클만 따라오고 소리는 조용히 빠지는 구조였고, 실제로 `Test/BuildingsUpgradeHelper.cs:86`이 `TryUpgrade`를 직접 불러 **파티클은 나고 소리는 안 나는** 경로였다. 같은 PR이 버튼 클릭음에는 "잊으면 그것만 조용히 무음"을 이유로 전역 훅을 택하고 건물 사건에는 같은 논리를 적용하지 않은 비대칭이기도 했다. **해소**: 성공음을 씬 큐(`InGameCue`)의 `OnBuildingAction` 구독으로 옮겼다 — 그 클래스가 이미 `DayNightManager`를 같은 패턴(구독 대상을 필드로 붙잡고 `OnDestroy`에서 해제)으로 구독하고 있어 새 씬 오브젝트가 필요 없었다. **실패(거절음)는 호출부에 남는다** — 이벤트는 성공만 알린다. 재발감지: `OnBuildingAction`으로 알려지는 사건인데 연출/소리를 버튼 핸들러에서 직접 내는 코드, `BuildingAction` 값 추가 후 `InGameCue.HandleBuildingAction`·`BuildingFeedback` 한쪽만 갱신
- **WL-209** | RESOLVED(#406, TowerMergePanelView.cs `RefreshCandidates`) | N0WST4NDUP·muchan918 | 합성 후보 버튼의 표시 조건이 **재료 매칭뿐**이라 자원이 모자라도 버튼이 눌렸고, 자원 판정은 누른 뒤 `TryFuse` 4단계에서야 일어나 조용히 반려됐다. #406이 거절음을 붙였지만 재료 부족과 코스트 부족이 **같은 클립을 공유**해 사유를 구분할 수 없었고, 소리가 붙은 만큼 근본 수정의 동기가 줄어드는 것이 위험이었다. **해소**: 표시(`SetActive` = 재료)와 활성(`interactable` = 코스트)을 갈랐다. 판정 경로는 `뷰 → TowerMergeCoordinator.CanAffordMerge → TowerFusionController.CanAfford → ManagementController.CanAfford`로, 뷰가 경영 시스템을 직접 부르지 않고(계약 #6) 코스트 규칙은 `TryFuse`와 같은 자리에 남는다. 패널이 열린 채 자원이 변하는 경로(되돌리기 환불)를 위해 `OnAffordabilityChanged`로 후보만 다시 칠한다 — 없으면 "자원은 충분한데 회색"이라 `interactable=false` 때문에 거절음조차 나지 않는다. 재발감지: 뷰에서 `ManagementController.CanAfford` 직접 호출, `CanMerge`만 보고 `interactable`을 켜는 회귀, `TryFuse`의 코스트 식과 `CanAfford`가 갈라짐
- **WL-212** | RESOLVED(#504, TowerAsset.cs:64 + DayNightManager.cs:33) | KIM-SUNGSOO·muchan918 | 데이터 SO가 씬 싱글톤을 직접 pull하던 유일한 사례 — `TowerAsset.IsUnlocked(DayNightManager = null)`이 인자가 비면 `DayNightManager.Instance`를 봤다(#504 초안). 부작용 둘: ① 판정 입력이 갈렸다(패널은 캐시한 참조를 push, 툴팁은 무인자로 pull) ② `Data` → `DayNight` 역방향 엣지가 생겨 WL-120·WL-152의 asmdef 격리 때 되돌릴 대상이 됐다. **해소**: `IsUnlocked(int currentWave)` 순수 시그니처로 바꾸고, 웨이브 조달과 permissive 규약("매니저 없는 씬은 웨이브 제한 없음")을 `DayNightManager.CurrentWaveOrMax`(static) 한 곳으로 옮겼다 — 규칙(SO)·규약(매니저)·입력(조회창)이 각각 한 곳이고 SO는 씬을 모른다. 부수로 `??=` fake-null 함정도 사라졌다(`Instance != null`은 Unity 오버로드라 파괴된 오브젝트를 null로 본다). ⚠ **활성 원장에 등재되기 전에 닫혔다** — 리뷰 제안 단계에서 해소돼 번호만 보존한다. 재발감지: `ScriptableObject` 파생 클래스 안의 `.Instance`·`FindFirstObjectByType`, `IsUnlocked(DayNightManager ...)` 형태 시그니처 부활
- **WL-201** | RESOLVED(#537, KeyboardManager.cs:50 `AcceptsModifiers` + :91 `exactModifiers`) | n0wst4ndup | 보조키 판정이 **전역 정확 일치** 단일 정책이라 `KeyModifier.None`으로 등록한 바인딩은 Ctrl·Shift·Alt 중 하나라도 눌려 있으면 발화하지 않았다 — 이 프로젝트에서 Shift는 그룹 선택 토글로 쥔 채 조작하는 키라(`MouseManager.cs:353-354`) 무보조키 단축키가 "가끔 안 먹는" 증상이 될 자리였고, 원인이 한 줄이라는 것이 증상에서 멀었다. **해소**: `Bind`에 `exactModifiers`(기본 `true`) 인자를 더해 판정을 **바인딩별로** 고르게 했다(`Binding.AcceptsModifiers`). 기본값이 종래 정책이라 Ctrl+Z는 그대로 정확 일치이고, 무보조키 단축키만 `false`로 등록해 "요구 보조키 포함, 나머지 무시"가 된다. 전역 정책을 뒤집지 않은 이유는 같은 키를 보조키로 갈라 쓰는 조합이 나중에 들어와도 서로를 조용히 겸하지 않게 하기 위함이다. 예고대로 **두 번째 바인딩이 붙는 시점**에 닫혔고, 그 첫 소비처가 배속 토글 스페이스바다(`GameSpeedController.cs:96`). ⚠ 이관 후보였던 ESC 2건(`SettingUI.cs`, `MainMenuUI.cs`)은 여전히 자체 폴링이라 미이관 — 그쪽은 WL-199의 소유자 기반 해제와 함께 볼 일이다. 재발감지: 무보조키 단축키를 `exactModifiers` 없이 `Bind`하는 신규 호출, `AcceptsModifiers`를 우회한 `binding.Modifiers != held` 직접 비교의 부활, 전역 정책을 "포함 판정"으로 뒤집는 변경
- **WL-229** | RESOLVED(#550 잔여, TowerButtonView.cs `OnDisabledClick` + StoreOfferRow.cs `OnDisabledClick` + UiClickSfx.cs `GetComponentInParent`) | N0WST4NDUP·muchan918 | 비활성(회색) 버튼의 클릭 피드백 훅(`IDisabledClickFeedback`)은 #550이 깔았으나 소비처가 2개뿐이라 남은 회색 버튼이 **완전 무음**이었다 — `interactable == false`면 `onClick`도 `UiClickSfx`의 전역 클릭음도 지나지 않는다. **해소**: ① 구현 자리는 예고대로 뷰별이 아니라 `TowerButtonView` **한 곳**이다 — `TowerSelectPanelView`·`TowerMergePanelView`가 둘 다 `SetSelectable`을 지나기 때문. 걸림돌이었던 "비활성 사유를 그 뷰가 모른다"는 **사유를 인자로 넘겨** 풀었다(`SetSelectable(selectable, blockedByCost)`) — 사유 판정은 게이트를 계산한 패널이 소유하고 뷰는 소리를 낼지만 정한다(뷰가 재계산하면 판정이 두 벌이 되고 게이트가 늘 때 한쪽만 고치면 어긋난다). ② 같은 방식으로 `StoreOfferRow`(연금술사 교환 행)가 세 번째 축으로 함께 닫혔고, 이때 **훅 탐색이 눌린 `Selectable`에서 부모로 올라가도록** 바뀌었다 — 교환 행은 `StoreOfferRow`가 행 루트에 있고 `Btn_Exchange`는 자식이라 버튼 오브젝트만 봐서는 구현체를 못 찾았다(`UiClickSfxIgnore`가 이미 쓰던 규칙). ③ 예고대로 #550 잔여(타워 코스트·합성 코스트 부족)가 함께 종결됐고, 소리는 `Rejected`가 아니라 신설 큐 `Sfx.InsufficientResources`로 나간다. ④ **원장의 "본진·건물 업그레이드 버튼은 자원이 모자라도 눌린다"는 기록은 틀렸다** — `CanUpgrade`·`CanUpgradeBuilding`·`CanIncreaseVillagers`가 셋 다 `CanAfford`를 포함하고 패널이 그 결과를 그대로 `interactable`에 꽂아, 그 버튼들도 자원 부족이면 **회색이고 완전 무음**이었다(핸들러 안 거절음은 도달 불가였다). 그래서 이들도 이 항목의 대상이었고 `BuildingInfoUI`·`CastlePanelUI`가 다섯째·여섯째 구현체로 함께 닫혔다 — 둘은 **버튼 위가 아니라 패널 루트**에 있어 부모 탐색으로 닿으며, `CastlePanelUI`는 회색 버튼이 둘이라 훅이 `OnDisabledClick(Selectable pressed)`로 눌린 버튼을 받도록 확장됐다. 재발감지: 「자원이 모자라도 눌린다」류의 전제를 `interactable` 계산식 확인 없이 주석·원장에 적음, 재발감지: `SetSelectable`/`SetInteractable` 호출부가 `blockedByCost`를 계산 없이 `false`나 `!selectable`로 넘김, 구현체가 사유를 스스로 재계산, 훅 탐색을 `TryGetComponent`(자기 GO 한정)로 되돌리는 변경, `IDisabledClickFeedback` 구현체가 소리 외 상태를 건드림
