# 타워 획득 경로 정리표 (TowerRoster)

> **상태**: 획득 경로(직접배치/합성 전용) 표만 우선 작성 — 스탯·역할 전수 카탈로그(Cost/Attack/Impact/BuffAura/DebuffAura/Effects 열 포함)는 [#282](https://github.com/muchan918/NorthLand/issues/282) 완료 기준의 남은 범위로 별도.
> **작성 계기**: [#298](https://github.com/muchan918/NorthLand/issues/298) 리뷰에서 개틀링·스나이퍼가 직접배치 로스터에서 제외된 것이 #282가 사인오프한 결정 범위 밖임이 지적됨(WL-156) — 팀이 확인할 수 있도록 획득 경로 결정 현황을 한 곳에 모은다.
> **관련**: `Docs/GDD.md` §5.8(레시피 족보·합성 비용 TBD), `Docs/Core/TowerMerge.md`, `Docs/Review/WatchList.md`(WL-156)

## 획득 경로 표

| TowerID | 이름(가칭) | 획득 경로 | 재료(합성 전용만) | 근거 |
|---|---|---|---|---|
| `archer_tower` | 아처 | 직접배치 | — | 최초 9종 |
| `cannon_tower` | 캐논 | 직접배치 | — | 최초 9종 |
| `poison_tower` | 포이즌 | 직접배치 | — | 최초 9종 |
| `haste_tower` | 헤이스트 | 직접배치 | — | 최초 9종 |
| `choco_tower` | 초코(슬로우) | 직접배치 | — | 최초 9종 |
| `soda_tower` | 소다(스턴) | 합성 전용 | 초코×1 + 캐논×1 (`Recipe_SodaTower`) | 최초 9종 → **#441에서 2티어로 승격.** 스턴을 넣고 딜은 다른 타워에 맡기는 유틸로 성격이 바뀌어(간격 1.5 → 2.2 · 발당 20 → 30, DPS는 13.3 → 13.6으로 사실상 동일) 단독으로 길을 막는 타워가 아니게 됐다. 재료는 "늦추는 것(초코) + 때리는 것(캐논) = 멈추는 것"이며 초코(둔화) → 소다(정지)의 CC 족보를 만든다. `Cost 40`·`UnlockWave 5`는 남아 있지만 이제 읽히지 않는다 |
| `flame_field_tower` | 화염지대 | 직접배치 | — | #282 확정 — 장판형만 기본 배치 유지 |
| `flame_archer_tower` | 화염궁수 | 합성 전용 | 아처×1 + 화염지대×1 (`Recipe_FlameArcherTower`) | #282 "획득 경로 결정" 표로 확정. #336에서 `_towers`에서 제거돼 **코드가 이 표와 일치**하게 됐다(그전까지 코드는 직접배치를 유지) |
| `incendiary_cannon_tower` | 소이캐논 | 합성 전용 | 캐논×1 + 화염지대×1 (`Recipe_IncendiaryCannonTower`) | 위와 동일 — #282 확정, #336에서 `_towers` 제거 실행 |
| `triple_shoot_tower` | 산탄 | 합성 전용 | 아처×3 (`Recipe_tripleshootTower`) | #298 신규 — 아처 개수 = 펠릿 수(총알 3발 동시 발사). 아래 "아처 계열 재료 개수 규약" 참조 |
| `boomerang_tower` | 부메랑 | 합성 전용 | 아처×1 + 캐논×2 (`Recipe_BoomerangTower`) | #298 신규 — 원안(아처×2+캐논×1)이 기존 `Recipe_Example_Sniper`와 겹쳐 비율을 뒤집음 |
| `multi_inferno_tower` | 멀티인페르노 | 합성 전용 | 개틀링×2 + 포이즌×1 (`Recipe_multiinfernoTower`) | #298 신규 |
| `rampup_tower` | 램프업 | 합성 전용 | 사냥꾼×1 + 헤이스트×1 (`Recipe_RampUpTower`) | #300 신규 — 명중할수록 공속이 오르는 성장형. 획득 경로는 이슈 제안대로 **사인오프 완료**(2026-08-06). **#441에서 재료를 개틀링 → 사냥꾼으로 교체** — 종전 조합(개틀링=동시 사격 + 헤이스트=공속 버프)은 "공속 재료 둘 → 공속 성장"이라 **성장한다는 축 자체가 재료에 없었다.** 지금은 사냥꾼이 성장 메커니즘을 공급하고 다른 재료가 그 성장을 어느 축에 붙일지 정한다. 사거리도 18 → 25로 올렸다 — 재료(사냥꾼 20)보다 좁아 사거리만 후퇴하던 것을 없앤다 |
| `killstack_tower` | 사냥꾼 | 직접배치 | — | #300 신규 · **#441에서 기본 타워로 하향**(비용 40 · 5웨이브 — 소다가 비운 슬롯을 그대로 이어받아 해금 계단에 구멍이 없다. `Recipe_KillStackTower` 폐기). 트리거가 **처치 → 명중**으로 바뀌어 성장 축을 공급하는 재료 역할을 맡는다(램프업 행 참조). 명중은 처치보다 훨씬 흔해 폭을 다시 잡았다 — 스택당 +15% → +8% · 감쇠 없음 → 2초 · 상한 2.2배 → 1.64배. 사거리 30 → 20 · 간격 1.6 → 1.1 · 발당 20 → 14. **웨이브 종료 시 초기화**는 그대로. ⚠ `TowerID`는 `killstack_tower`로 유지한다(레시피·세이브가 참조) — 이제 처치를 뜻하지 않으므로 이름만 보고 트리거를 짐작하지 말 것 |
| `single_inferno_tower` | 단일 인페르노 | 합성 전용 | 멀티인페르노×1 + 스나이퍼×1 (`Recipe_SingleInfernoTower`) | #300 신규 — 한 대상만 지지되 조준 유지 시간만큼 피해가 커진다(**8 → 104 DPS / 6초**, 상향 이력은 아래 미해결 항목 참조). **합성 산물을 재료로 쓰는 첫 사례**(다단 합성) |
| `missile_tower` | 미사일 터렛 | 합성 전용 | 아처×1 + 캐논×1 + 화염지대×1 (`Recipe_MissileTower`) | #336 신규 — 착탄 지점에 화염 구역을 남기는 첫 타워. 재료 3종은 각각 유도 사격(아처)·포격(캐논)·지속 장판(화염지대)을 뜻한다 |
| `twin_missile_tower` | 2연발 미사일 터렛 | 합성 전용 | 미사일 터렛×2 (`Recipe_TwinMissileTower`) | #336 신규 — 한 사이클에 2발을 시간차로 쏘는 연발(`Attack.BurstCount`). 착탄 구역이 발수만큼 생기고 겹치면 중첩된다. **단일 인페르노에 이어 두 번째 다단 합성** |
| `gatling_tower` | 개틀링 | 합성 전용 ⚠ | 아처×2 (`Recipe_Example_Gatling`) | 아처 개수 = 동시 사격 문수(2기가 함께 쏘는 개념 → 공속 상승). **획득 경로**는 #298(커밋 1b69373)에서 직접배치 제외로 전환 — #282 표엔 없는 신규 판단, 팀 사인오프 미완료(WL-156). ⚠ **#336에서 `GameScene`의 `TowerSelectPanelView._towers`에서 실제로 제거됐다** — 그전까지 이 표만 "합성 전용"이었고 코드는 직접배치를 유지하고 있었다(문서 선행 · 코드 후행). **사인오프 상태는 여전히 미완료다** — 코드가 표를 따라간 것이지 팀이 승인한 것이 아니다 |
| `Sniper_tower` | 스나이퍼 | 합성 전용 ⚠ | 아처×1 + 캐논×1 (`Recipe_Example_Sniper`) | 위와 동일(WL-156, #336에서 `_towers` 제거 실행) |
| `lightning_tower` | 번개사슬 | **없음(획득 불가)** | — | #282 후보#3 — SO만 존재, 레시피·프리팹 미배선. 별도 착수 필요 |

## 아처 계열 재료 개수 규약 — 부분집합 관계는 의도된 것

`Recipe_tripleshootTower`(아처×3)는 `Recipe_Example_Gatling`(아처×2)의 **진부분집합**이다.
`TowerFusionMatcher`가 포함 매칭이므로(선택 ≥ 필요면 성립) 아처 3기를 선택하면 개틀링·산탄
버튼이 **동시에 뜨고, 플레이어가 고른다.** 이는 버그가 아니라 아래 규약의 결과다.

**아처 재료 개수 = 결과 타워의 "동시성" 수치**

| 레시피 | 개수 | 의미 |
|---|---|---|
| 개틀링 | 아처×2 | 아처 2기가 **함께 쏜다** → 공격 속도 상승 |
| 산탄 | 아처×3 | 아처의 총알이 **3발 한 번에** 나간다 → 펠릿 3발 |

재료 개수가 결과물의 메커니즘과 직접 대응하므로, 겹침을 피하려고 개수를 바꾸면 이 대응이
깨진다. 부메랑이 원안(아처×2+캐논×1)에서 비율을 뒤집은 것과는 판단이 다른데, 그쪽은
`Recipe_Example_Sniper`와 **완전히 동일**해 둘 중 하나가 영원히 선택 불가가 되는 경우였다.
여기는 재료 수가 달라 두 버튼이 함께 뜰 뿐 양쪽 다 도달 가능하다.

> ⚠ 아처 계열 레시피를 추가할 때 이 규약을 따를 것 — 개수를 임의로 정하면 대응이 무너진다.
> 아처×4 이상을 쓰는 레시피는 개틀링·산탄 버튼도 함께 띄우게 되므로, 선택지가 늘어나는 것이
> 의도인지 먼저 확인한다.

## 미해결 항목

- **WL-156**: 개틀링·스나이퍼의 합성 전용 전환은 화염아처·소이캐논과 달리 팀(muchan918/밸런싱)의 명시적 사인오프가 없다. 이 표를 근거로 리뷰·확인 필요 — 승인되면 이 줄의 ⚠ 제거, 반려되면 두 타워를 `TowerSelectPanelView._towers`에 복귀.
- **`lightning_tower`**: #282 완료 기준의 필수 항목(후보#3)인데 아직 레시피·프리팹이 없어 획득 불가 상태로 방치돼 있다. 별도 이슈로 진행 필요.
- **성장형 3종(#300) 밸런싱 미확정**: 수치는 전부 구현자 추정이며 **팀(muchan918) 사인오프가 없다**(WL-015 축).
  기능 검증은 완료됐고 수치만 남았다.
  - **단일 인페르노 상한을 32 → 104 DPS로 상향**(커밋 `9a1f6ba`, `LockRamp` `PerStack 0.5→1.5` ·
    `MaxStacks 6→8` · `StackInterval 1→0.75`. 램프 완주 시간은 6초로 동일).
    **근거**: 종전 ×4 상한은 보스처럼 오래 사는 대상에게 "일정한 DPS"로 수렴해 독 타워와 구분되지 않았다 —
    "물고 있으면 녹는다"는 정체성이 성립하려면 고점이 시작의 수십 배여야 한다(CoC 인페르노 축).
    새 곡선은 8 → 32(1.5초) → 104(6초)로, **종전 최대치를 1.5초에 지나간다.**
    **⚠ 검증 필요**: 104는 다음으로 센 타워(스나이퍼 20 / 킬스택 만렙 30)의 **5배**다. ① 대상 1기에만
    ② 6초 끊기지 않는 조준 ③ 다단 합성 산물이라는 세 조건으로 방어하는 수치이며, **실제 보스전 Play 실측
    없이 정해졌다.** 과다 교정 여부를 보스 HP·생존 시간과 대조해 확인할 것.
  - **단일 인페르노 사거리(20)가 짧다** — 스나이퍼(36) 대비 이득이 작다는 지적은 **미해소**.
  - 램프업(+8%×10, 감쇠 2초, 사거리 12 → 18 → **25**)·사냥꾼(#441에서 +15%×8 → **+8%×8** · 감쇠 없음 →
    **2초** · 사거리 30 → **20** · 간격 1.6 → **1.1** · 발당 20 → **14**)의 폭도 함께 재검토 대상.
    **#441 수치도 전부 구현자 추정이며 사인오프가 없다.** 특히 ① 사냥꾼 비용 40은 소다 슬롯을 승계한
    값이고 ② 램프업이 사냥꾼을 사거리·연사·피해·성장 상한 **전 축에서 상회**하는 것이 의도된 승급
    보상인지 확인이 필요하다(도달 비용 70 vs 40에 DPS 약 2배).
