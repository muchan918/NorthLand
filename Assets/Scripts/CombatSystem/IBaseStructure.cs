namespace NorthLand.Combat
{
    /// 「본진 역할」 마커(#453). 자폭병이 조준·자폭 대상으로 인정하는 유일한 축이다.
    /// 값·메서드를 갖지 않는다 — 정체성만 표시하고 피해 계약은 `IDamageable`이 그대로 소유한다.
    ///
    /// 왜 구체 타입(`is PlayerBase`)이 아닌가: 조준 필터는 프로젝트 전역 **미합의 축**이고
    /// (레이어 vs 태그 — `Enemy.cs` 상단 TODO·`SystemMap.md` §5), 여기에 런타임 타입 검사를
    /// 세 번째 방식으로 얹으면 부(副)성문·파괴 가능 방벽처럼 **"본진 역할"을 다른 `IDamageable`이
    /// 맡는 순간 자폭병이 그것을 조용히 무시한다** — 예외도 경고도 없고 증상은 "자폭병이 그냥
    /// 지나간다"뿐이라 원인에서 멀다.
    ///
    /// 역할 술어로 올려두면 본진 역할이 늘어도 필터 수정이 따라오지 않는다(`Faction`과 나란한 축).
    public interface IBaseStructure : IDamageable
    {
    }
}
