/// 좌버튼으로 **집어서 끌 수 있는** 대상이 구현한다(Docs/Core/MouseManager.md §5.4 — 이름까지 예약돼 있던
/// 그 인터페이스다). 현재 유일한 구현체는 주민(`ResidentSelectable`)이고, 첫 사용처는 주민을 들어 생산 건물에
/// 떨어뜨려 배치하는 경로다(Docs/ManagementArea/Resident.md §8).
///
/// ── 이 마커가 가르는 것 ──────────────────────────────────────────────
///
/// 좌버튼을 누른 채 움직이는 제스처는 **사각형 선택**과 **유닛 끌기** 둘 다에 해당한다. 구분 기준은 §5.4가
/// 하나로 못박아 뒀다 — *누른 순간 커서 밑에 무엇이 있었는가.* 이 마커를 잡았으면 유닛 끌기, 아니면 사각형이다.
/// 두 모드는 진입 시점에 배타적으로 갈리므로 이후 로직이 섞일 여지가 없다.
///
/// 대가는 §5.4에 이미 적혀 있다: **주민 위에서는 사각형 드래그를 시작할 수 없다.** 빈 땅에서 시작하면 되고,
/// 사각형이 지나가며 주민을 담는 것은 정상이다(다수 RTS가 같은 대가를 치른다).
///
/// ── 멤버가 없는 이유 ────────────────────────────────────────────────
///
/// `IGroupSelectable`과 달리 훅(OnPickedUp/OnDropped 같은 것)을 두지 않았다. MouseManager가 이 타입에
/// 요구하는 것은 **"끌 수 있다"는 사실 하나뿐**이고, 들린 뒤에 무슨 일이 일어나는지는 전부 도메인 소비처
/// (`ResidentDragCoordinator`)가 정한다 — 매니저는 도메인을 모른다(§1 원칙 2).
///
/// 훅을 미리 만들어 두면 `ResidentSelectable`의 호버·선택 훅처럼 **비어 있는 채로 남아**, 나중에 읽는 사람이
/// "여기 채우면 되나" 하고 잘못된 자리에 연출을 건다(WL-087 계열의 함정이 그것이다).
public interface IDragHandle
{
}
